# 2026-09-17

3090이 GSP RPC 실패로 두 번째 하드 행을 겪고 판매 검사 99.9%에서 전원이 끊겨 빠졌고, 프롬프트 캐시는 DeepSeek 파서가 user 턴을 못 알려 13,167토큰 접두사를 매번 다시 태워 첫 에이전트 턴이 7분 31초 걸렸다. 오후엔 mistral.rs와 ik_llama.cpp를 같은 A6000·GGUF로 맞댔다: 싱글스트림 ik 130 tok/s가 앞서지만 4스트림 합계는 mistral.rs 283 tok/s로 뒤집혔고, 정체는 ik가 MoE 디코드에서 배치 이득을 0으로 잃는다는 것(dense 대조는 정상 스케일). 오프로드는 반대로 갈린다 — 층 하나 호스트 이동 값이 ik 토큰당 0.20 ms, mistral.rs 442 ms, 2,200배 차이이고 원인은 mistral.rs가 토큰마다 expert 블록 전체를 F32로 풀어버리는 것(PR #2430 제출).

<a name="the-3090-comes-out"></a>
## 3090이 빠지다

**06:28–07:23.** 24 GB 카드가 그날 아침 빠졌다. 09-16 21:56:59 두 번째 하드 행 — DDP 학습 중 한 번 넘어진 뒤, Qwen3.6-35B-A3B 통째 서빙(22.3 GB) 중 두 번째 프로세스(`CUDA_VISIBLE_DEVICES` 없는 torch 직접 구동)가 같은 카드에 CUDA를 올려 `unspecified launch failure`, GSP RPC 실패(`rpcSendMessage failed 0xf`), Xid·AER 없음, 레지스터 클린, `nvidia-smi -r` 실패, 파워사이클만 살렸다.

> ~~두 행의 공통 지문: sustained 메모리대역폭 부하 + 카드에 두 번째 CUDA 컨텍스트 도착.~~ **09-18에 두 번 이의가 제기됐다.** 09-18 재현은 카드당 CUDA 컨텍스트 하나라 두 번째 컨텍스트 절반은 일반이 아니고, `rpcSendMessage failed 0xf`는 Xid 79를 올리는 BAR0 읽기 없이도 나올 수 있어 이 행과 아침 행이 같은 드라이버급인지 미확정. [소스 추적](2026-09-18.md#b-the-fault-reproduced).

슬롯 문제는 아니다(A6000 이동이 Gen4 correctable 483초 27개를 고쳤지만 3090 링크는 이미 클린이었다). 사용자는 재현 대신 판매를 택했다.

`tools/gpu-sale-check.sh` 06:28:38 단독 검사: cuda_memtest 24 GB 3패스 에러 0, gpu_burn 30분 **99.9%**(42,968회, 26.2 TFLOP/s)에 에러 0, 06:59:26 카드 뽑히며 전원 오프로 중단. 1,842초 증인 전력 최대 421 W, 온도 최대 77 °C, Xid·NVRM·AER 없음. `INTERRUPTED`로 기록, 99%+에러0은 pass.

러너 요약 줄이 memtest 뒤 `errors reported: 1`을 찍었다 — 카운터지 메모리가 아니다. cuda_memtest가 GeForce에서 `serial=unknown (NVML runtime error)`을 찍고 `error|fail` grep이 잡은 것. 러너·시트에서 고쳤고 이름으로 제외. **Struck**: `sale-3090-1` `run.log`의 `errors reported: 1`.

peer 세션이 `/home/user/gpu-lease` 잡힘에 갇혀 06:27까지 카드 둘 1 MiB·학습 0을 보고했다 — 러너가 scp로 덮어써지며 EXIT 트랩 없이 죽은 탓. 이제 private 복사본 exec, holder pid `kill -0` 무응답이면 임대 아님(`docs/quiet-machine.md`).

opus 리서치: `0xf` = `NV_ERR_GPU_IS_LOST`, GSP stall(Xid 119/175) 아님. 가장 가까운 사례 [open-gpu-kernel-modules #1134](https://github.com/NVIDIA/open-gpu-kernel-modules/issues/1134). 22시간에 하드 행 둘, 회복 불가, 판가름 테스트 다섯 중 하나도 못 돌렸다.

> **2026-09-18 superseded** — 카드가 같은 슬롯에 돌아갔다. [복귀](2026-09-18.md#the-3090-goes-back-in).

원본: `git show c27c3a7:log/2026-09-17-the-3090-comes-out.md`

<a name="where-the-prompt-cache-breaks"></a>
## 프롬프트 캐시가 깨지는 자리

**08:22–09:45, A6000 1장, V4.1-Flash Q3_K_M + DSpark draft, expert 전층 호스트.** 어제 에이전트 턴 하나가 5–7분 걸린 이유.

로깅 프록시([`tools/cache/logproxy.py`](../tools/cache/logproxy.py))로 캡처: 첫 에이전트 턴 7분 31초, 모델 호출 여섯. 첫 호출 13,167토큰(재사용 0) 프리필 **285초** 46 tok/s, 이후는 새 토큰만(1,107→103) 26초→5초. 13,167 중 시스템 텍스트 4,200 + 도구 스키마 23개 ~8,950, user 메시지 20 — 레버는 도구 개수. 동일 요청 두 번째 세션은 1초 만에 첫 토큰(13,167 전부 캐시 적중).

리플레이로 변경 하나씩: 시스템 프롬프트 끝 한 단어만 바꿔도 재사용 **0**, 프리필 13,168, wall 236초. 원인은 V4.1-Flash SWA(`n_swa = 128`)의 **컨텍스트 체크포인트**(5.8 MiB씩, #22929·#24176 설계)가 각 user 메시지 시작·프롬프트 끝에만 있는데, user 시작 13,146 자리 체크포인트가 없었던 것.

원인: DeepSeek V3.2/V4/V4.1 파서(`common/parsers/deepseek.cpp`)가 `message_delimiters`를 비워 둠. 패치: 형제 parity 9줄 + test-chat. FAIL-first `Expected: <｜User｜> Actual:`(빈), rc 134.

재빌드 후 새 user 질문: 재사용 **13,145**, 프리필 **16**, wall **0.8초**(패치 전 12,651/510/13.8초). 시스템·도구 스키마 편집은 여전히 전체 재발신 — 설계지 파서 아님. cold 46 tok/s·13,167토큰은 그대로 4분 45초.

구분자 없는 전용 파서는 deepseek·lfm2·ministral3·gigachat_v3·kimi_k2 다섯뿐, 뒤 둘은 full KV라 불필요. lfm2 미측정. 패치·테스트는 서빙 포크와 업스트림 브랜치 `deepseek-msg-delimiters`에. 인접 PR #24176·#21785·#25452·#21831·#24055·#28302.

원본: `git show c27c3a7:log/2026-09-17-where-the-prompt-cache-breaks.md`

<a name="a-rust-engine-on-the-same-card"></a>
## 같은 카드의 Rust 엔진: 단독 느리고 4스트림 두 배

**14:07–14:31, A6000 1장, Qwen3.6-35B-A3B UD-Q6_K, 테이크 일곱.** mistral.rs 0.9.3 대 ik c10fbbcc, 같은 GGUF·프롬프트 넷. 4스트림 합계는 mistral.rs가 거의 두 배(ik **149** tok/s, mistral.rs **283**), 싱글스트림은 반대(ik **130**, mistral.rs **111**).

mistral.rs는 OpenAI 호환 API뿐이라 [`tools/mrs/mrs-shim.py`](../tools/mrs/mrs-shim.py)로 llama-server 프로토콜 앞단을 씌웠다. `enable_thinking: false`는 존중하지만 아무 언급 없이 48 중 48토큰이 `reasoning_content`로 왔다.

thinking on/off는 어느 엔진도 2% 넘게 안 움직인다. 단독 ik 24% 빠름(도출 386 GB/s), 4스트림 mistral.rs 90% 앞섬. ik 프리필 thinking on **3,357 ms**, off **5,393 ms** 대 mistral.rs **104 ms** — 10배 TTFT 간격 대부분이 여기.

> ~~cuTile 모듈 — 진짜 Rust 작성 커널 — 은 CUDA ≥ 13.2를 요구하고 이 상자는 13.0이라 설치자가 뺐다.~~ **같은 날 Struck.** `mistralrs doctor`: cuTile은 컴파일됐으나 `tileiras` 부재로 놀고, MoE 백엔드가 F16/BF16만 받아 Q6_K는 애초에 안 닿는다. `-v` 로그 `Preloaded 698 Candle CUDA PTX functions`로 candle 커널 확인.

> ~~09-16 ExLlamaV3 arm이 동시성이 sparse MoE에 보통 무익한 이유를 쟀다. 2스트림 두 토큰이 다른 expert에 라우팅되니 스텝당 읽기 바이트가 스트림에 비례하고 합계가 평탄하다. ik의 4스트림 +13%가 그 산술에 맞는다. mistral.rs +170%는 안 맞는다. 후보 설명 둘은 잴 수 있다. 싱글스트림이 고정비 bound라 배치가 분할하거나, indexed-MoE 경로가 expert 합집합을 스텝당 한 번 모으는데 ik가 시퀀스마다 다시 읽는다.~~ **같은 날 Struck.** 격차는 반대쪽 — 2스트림 비용 mistral.rs 11%, ik 50%. 처분: [2026-09-17-b](2026-09-17.md#b-where-the-four-stream-gap-actually-is).

> ~~09-17 히어로 테이크가 thinking off에 4스트림을 녹음했다.~~ **같은 날 Struck.** 요청은 버려졌고 매 스트림 512토큰 내내 생각했다. 재녹음(`--jinja`)은 실제로 꺼져 152–279토큰 EOS, 합계 **144 tok/s** — 149의 4% 안. [`tools/check-take-nothink.py`](../tools/check-take-nothink.py) gate 신설.

> ~~ik에 `chat_template_kwargs` 지원이 없고, mainline llama-server에 있고, 격차가 기여감이다.~~ **30분 안에 Struck.** 포크에 있다(`common/chat.cpp:546`). [`tools/ik/ik-vram-take.sh`](../tools/ik/ik-vram-take.sh)이 `--jinja` 누락, 레거시 템플릿 경로 원인. 재측정 40초, 131.4 tok/s.

카드의 mistral.rs 관련 필드 오독 다수는 toktape 0.2.4가 mistral.rs를 모르는 탓이다.

원본: `git show c27c3a7:log/2026-09-17-a-rust-engine-on-the-same-card.md`

<a name="b-where-the-four-stream-gap-actually-is"></a>
## 4스트림 격차의 실제 자리

*15:07–18:45, A6000 1장, ik c10fbbcc, mistral.rs 0.9.3.* [아침 기록](2026-09-17.md#a-rust-engine-on-the-same-card) 미설명을 판가름.

> ~~mistral.rs 싱글스트림이 고정비 bound라 배치가 분할하거나, indexed-MoE 경로가 expert 합집합을 스텝당 한 번 모으는데 ik가 시퀀스마다 다시 읽는다.~~ **같은 날 Struck.** 둘 다 추측이었다 — ik가 MoE 디코드 배치 이득을 못 받는다.

같은 모델, 프롬프트 여덟, `-c 32768 -n 512`, thinking on, 1/2/4/8 스트림 합계: mistral.rs 105→184(1.75배)→279(2.66배)→**324**(3.09배). ik 129→130(1.01배)→146(1.13배)→**152**(1.18배). ik 스트림당 128.5→65.4로 반토막인데 합계는 그대로(129→130). ik 프리필 무너짐 — 238토큰 하나 218 ms, 여덟 10,071 ms.

`llama-batched-bench` 순수 배치: DeepSeek-V2-Lite Q3_K_M(MoE, MLA) B=1 **202.1**→B=2 **156.1**(0.77배)→B=4 229.9→B=8 317.0. Qwen2.5-7B(dense) B=1 120.7→B=2 195.5(1.62배)→B=8 388.5(3.22배) 정상 스케일. 토큰 둘 스텝이 하나보다 느린 것은 MoE만.

`GGML_CUDA_DISABLE_GRAPHS=1`은 싱글스트림 196.8→182.7(7%)만, 1→2 붕괴는 그대로. `-no-fmoe`는 골고루 나쁨. 남는 후보 배치-2 expert matmul — **가설, 미측정.**

Qwen3.6-35B-A3B는 다른 버그: `single-token chunking` 로그, 프리필 B=1 2,581.9→B=2 **119.7**로 21분의 1. 가드([#1266](https://github.com/ikawrakow/ik_llama.cpp/pull/1266))는 [#2418](https://github.com/ikawrakow/ik_llama.cpp/pull/2418)에서 이미 시퀀스런 경계로 고쳐져 있었다 — **21:12** 코멘트로 실측 남김. 인접 [#1932](https://github.com/ikawrakow/ik_llama.cpp/issues/1932)·[#1933](https://github.com/ikawrakow/ik_llama.cpp/pull/1933), 배치 범위 다른 끝의 같은 집안 문제 [#1785](https://github.com/ikawrakow/ik_llama.cpp/pull/1785). 디코드 발견 미제출.

**2026-09-19 추가:** [WKS-36](2026-09-19.md#verification-does-not-amortise) 기울기 17.28 ms가 라우팅 바이트 몫(34.56%)과 0.13%p 안에서 일치 — 합집합 산술은 맞고 그 배치(`-ngl 99`)의 초과분은 0. CUDA `MUL_MAT_ID`로 좁혀졌으나 미측정.

원본: `git show c27c3a7:log/2026-09-17-b-where-the-four-stream-gap-actually-is.md`

<a name="c-what-mistral-rs-can-and-cannot-offload"></a>
## mistral.rs의 오프로드: 되는 것과 안 되는 것

*18:42–18:59, A6000 48G, mistral.rs 0.9.3.* 이 상자 CPU 오프로드가 mistral.rs에 있는가 — 절반 yes, 그 절반이 정확히 깨져 있다.

제공: `--cpu`, `-n, --device-layers ORD:NUM`(`-ngl`격), `--topology YAML`. **텐서급 배치 없음** — ik `-ot exps=CPU`·`-ncmoe N` 상당 없음. NVMe도 없지만 llama.cpp·ik 둘 다 없어 parity.

`-n 0:8`(Qwen3.6-35B-A3B Q6_K)는 로그대로 매핑되지만 매 요청 깨짐: `dtype mismatch in matmul, lhs: BF16, rhs: F32`. `-n 0:40` 동작, `-n 0:39`(1층 호스트) **깨짐**(에러 58회), dense Qwen2.5-7B `-n 0:8` 동작. GGUF 헤더: 마흔 블록 전부 expert 텐서 — CPU에 놓인 MoE expert 블록 하나면 충분.

원인: `GgufMatMul::quantized_act_type`(`mistralrs-quant/src/gguf/mod.rs:560`)이 CPU 가중치에 `None`을 돌려 indexed 경로 형변환을 끈다. [EricLBuehler/mistral.rs#2430](https://github.com/EricLBuehler/mistral.rs/pull/2430) 1파일 5줄. FAIL-first: 미패치 `d5ae0f1` http 500(2줄), 패치 후 http 200(0줄). `cargo test -p mistralrs-quant` 361 통과 0 실패.

> ~~`indexed_gemv`는 aarch64 전용이라 x86_64 호스트는 항상 풀기 fallback에 닿는다.~~ **제출 전 Struck.** 버리는 테스트가 Apple Silicon Mac에서도 깨져 aarch64도 같은 fallback임이 드러남 — 리포 스타일(고침에 새 테스트 없음)상 PR엔 안 실음.

MoE GGUF 다른 둘은 오프로드 무관 이유로 로드 거부돼 재현 범위는 "하나"로 남음. 중복 조사 무매치([#2072](https://github.com/EricLBuehler/mistral.rs/issues/2072)는 FP8 로딩, 다른 경로).

값: [기록 -d](2026-09-17.md#d-what-offloading-costs-each-engine) — 오프로드 1층 ik 0.20 ms, mistral.rs 442 ms.

원본: `git show c27c3a7:log/2026-09-17-c-what-mistral-rs-can-and-cannot-offload.md`

<a name="d-what-offloading-costs-each-engine"></a>
## 엔진마다 오프로드 값

*21:19–21:37, A6000 48G, ik c10fbbcc, mistral.rs `d5ae0f1`+[#2430](https://github.com/EricLBuehler/mistral.rs/pull/2430). Qwen3.6-35B-A3B UD-Q6_K, 1스트림, thinking on, 238토큰 프롬프트.* [기록 -c](2026-09-17.md#c-what-mistral-rs-can-and-cannot-offload) 노브 확정, 값은 여기: 오프로드 1층 토큰당 **0.20 ms** 대 **442 ms**.

> ~~두 엔진이 같은 기능을 잘하고 못하고가 아니라 이름 같은 다른 기능이다. ik는 연산을 GPU에 두고 가중치만 옮긴다.~~ **같은 날 밤 Struck.** 양쪽 다 연산을 호스트로 옮긴다.

CPU 층 하나가 mistral.rs PagedAttention 전체를 끔을 발견, control 고정(전부 GPU, 켬 111.2·끔 102.7, 7%) — 쌍 축은 VRAM 아니라 가중치 바이트.

mistral.rs: 0블록 111.2(control)→1블록 2.2→4블록 0.6→10블록 0.2. control 빼면 `(453.9-10.2)/1=443.7`, `(1779.1-10.2)/4=442.2`, `(4425.4-10.2)/10=441.5` — **층·토큰당 상수 442 ms**. 원인(#2430 착지 한 줄 위): 매 forward 블록 expert 전체를 F32로 풀고 버림 — Q6_K 630 MB가 토큰마다 3.07 GB F32.

ik: 0→129.0, 1→124.7, 4→116.2, 10→101.8. `(7.9-7.7)/1=0.20`, `(8.5-7.7)/4=0.20`, `(9.7-7.7)/10=0.20` — 선형, **2,200배** 차.

> ~~`CUDA_Host`는 CUDA 커널이 직접 읽는 pinned 호스트 메모리다. 가중치가 카드를 떠나고 matmul은 안 떠난다. 그래서 토큰·층마다 GPU가 라우터가 고른 expert만 읽는다. 그 블록 630 MB 중 약 19.7 MB. PCIe 4.0 x16 직선에 0.79 ms 대 실측 0.20 ms라 전송이 상주층 연산에 대개 겹친다.~~ **Struck, 추론이지 측정 아니었다.** 라우팅 expert는 그 층 라우터가 돌아야 알아 겹칠 시간이 없다. DDR4-3600 19.7 MB가 147.7 GB/s에 **0.13 ms**가 더 맞음. [`tools/ik/ik-where-compute.sh`](../tools/ik/ik-where-compute.sh) 코어 점유: ik 전부상주 1.0코어·96% GPU, 10블록호스트 **26.7**코어·72%. mistral.rs 전부상주 1.0코어·90%, 1블록호스트 1.3코어·**2%**.

양쪽 다 CPU로 옮기지만 ik는 26.7코어에 펴서 카드 72% 바쁘고, mistral.rs는 1.3코어 직렬로 카드 2% idle. 2,200배 = 스레드(21배)×바이트(190배: ik가 읽는 19.7 MB 대 mistral.rs의 3.7 GB — 630 MB 전부 읽고 F32 3.07 GB를 쓴 합).

> ~~그래서 arm은 설계 무효다. 확정하는 것은 ik가 여기 CPU 연산 티어가 없다는 것인데, 이 상자의 `-ot exps=CPU` 레시피가 CPU 연산인 적 없다는 뜻도 된다. 호스트 RAM expert 가중치 맞고, 호스트 연산 아니다.~~ **Struck, 파급 가장 큼.** `-ot exps=CPU`·`-ncmoe`는 같은 pinned 버퍼일 뿐, 공유 배치는 CPU 연산이 맞다.

토큰당 전체 풀기가 #2430보다 큰 업스트림 후보(캐시 또는 라우팅만 풀기) — **미제출**. toktape TTP-107 닫힘(`e4874f2`, v0.2.5), `/props`가 `engine.server_pid` 선언 가능.

원본: `git show c27c3a7:log/2026-09-17-d-what-offloading-costs-each-engine.md`

# 2026-09-23

bloomery의 engram 조회가 실제 토큰 스트림에서 얼마나 같은 행을 다시 찾는지 쟀더니, 토큰당 48행 가운데 새로 읽어야 하는 것이 코드 스트림에서 8.38행, 산문에서 25.72행이었다. 캐시를 두지 않는다는 설계 전제는 그 자리에서 무너졌다. 같은 밤 CUDA 13.3 툴킷을 13.0 옆에 설치했다. 우리 커널은 한 바이트도 바뀌지 않았다.

<a name="engram-row-reuse"></a>
## engram 행은 실제 스트림에서 되풀이된다

DeepSeek-V4.1-Flash의 engram은 토큰마다 두 층에서 24행씩, 합쳐 48행(행당 272 B)을 195 GiB 테이블에서 읽는다. 앞선 설계는 행 id를 균등 난수로 보고 DRAM 캐시를 두지 않기로 했다([09-22 기록](2026-09-22-p-the-engram-path-costs-a-third-of-a-millisecond-and-it-is-syscalls.md)). 이번에는 실제 해시를 썼다. 상수는 GGUF 메타데이터의 `deepseek41.engram.*` 키 아홉 개에서 읽었고, 공식은 ik 포트의 `llama_set_engram_rows`를 직접 읽어 대조했다. 스트림은 박스에 있는 텍스트 두 벌을 ik의 `llama-tokenize`로 토큰화해 만들었다. 코드는 ik_llama.cpp의 `src/*.cpp`(778,951토큰)이고, 산문은 같은 트리의 `docs/**/*.md`와 `README.md`(75,268토큰)다. LRU 적중률은 스택 거리 한 번으로 모든 용량을 정확하게 계산했다. 행 바이트는 읽지 않고 헤더만 읽는다.

    engram-reuse --ids $BLOOMERY_DATA/engram/corpus-code.ids
    engram-reuse --ids $BLOOMERY_DATA/engram/corpus-prose.ids

| 스트림 | 토큰 | 재적중(무한 캐시) 2-gram | 3-gram | 4-gram | 1 GiB 캐시 미스 행/토큰 | 4 GiB | 스트림 전체의 서로 다른 행 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 코드 | 778,951 | 91.97 % | 82.40 % | 73.25 % | 8.45 | 8.38 | 6,529,150행 = 1.65 GiB |
| 산문 | 75,268 | 63.39 % | 42.88 % | 33.00 % | 25.72 | 25.72 | 1,935,713행 = 0.49 GiB |

재적중률은 첫 번째 engram 층의 값이다. 두 번째 층은 소수점 둘째 자리까지 같다. 미스 행 수는 두 층과 24버킷이 캐시 하나를 함께 쓸 때의 값이다. 엔진의 DRAM 예산은 하나이기 때문이다.

4 GiB 줄은 무한 캐시 줄과 같다. 두 스트림의 서로 다른 행이 전부 합쳐 1.65 GiB와 0.49 GiB라 4 GiB 캐시에 다 들어간다. 그러니 남는 8.38행과 25.72행은 처음 보는 n-gram이 만드는 강제 미스다. 이 표에는 용량이 가르는 몫이 없다(1 GiB에서 코드가 8.45행으로 처음 갈린다). 여러 사용자가 섞인 서빙 부하에서 서로 다른 행 집합이 얼마나 빨리 자라는지는 아직 재지 않았다. 해시가 텍스트를 따라가는지는 따로 증인이 있다. 한 차수의 8헤드는 같은 롤링 값을 서로 다른 소수로 나누므로, 서로 다른 행 수를 서로 다른 n-gram 수로 나누면 8에 가까워야 한다. 실제 값은 7.95–7.99였다.

틀렸던 것. 측정 전에 적어 둔 예측은 여섯 칸 모두 15–25포인트 낮았다. 사전 분포를 단어 n-gram으로 잡았는데, 스트림은 BPE 토큰이라 서브워드·공백·구두점이 훨씬 자주 되풀이된다. 이 표를 받고 쓴 bloomery 계획 문장 둘은 헬퍼가 옮길 행이 "8배" 준다고 적었다. 48에서 8.38로 가면 5.7배다(유도). 줄어든 행 수를 배수로 읽은 것이다. 두 문장 모두 선을 그어 정정했다. 결정도 바뀌었다. DRAM 핫 행 캐시 라운드를 `io_uring` 라운드보다 앞에 둔다. 헬퍼 스레드가 옮길 행이 코드 스트림에서 5.7배, 산문에서 1.9배(유도) 줄어드는 자리이고, `io_uring`이 낼 값은 그 캐시 뒤에서 다시 계산해야 한다.

<a name="cuda-13-3"></a>
## CUDA 13.3을 옆에 깔았고, 커널은 한 바이트도 바뀌지 않았다

cutile-rs는 sm_8x에 CUDA 13.2 이상을 요구하고 13.3을 권한다. 박스에는 13.0만 있었다. 드라이버 615.71.09는 이미 UMD 13.4 수준이라, 툴킷만 옆에 설치하면 된다. `apt-get -s install --no-install-recommends cuda-toolkit-13-3`로 먼저 시뮬레이션해 신규 63개·업그레이드 0·제거 0이고 드라이버 패키지가 하나도 없음을 확인한 뒤 설치했다. 2분 걸렸다.

설치 뒤 `/usr/local/cuda`와 `/usr/local/cuda-13` 대안이 자동 모드라 우선순위가 높은 13.3(133)으로 넘어가게 되어 있었다. ik의 CMake 캐시는 `nvcc`를 `/usr/local/cuda/bin/nvcc`로 들고 있고, `ldconfig`도 그 경로를 먼저 본다. 그래서 두 대안을 13.0에 수동으로 고정했다. ik의 `llama-bench`는 전후 모두 RUNPATH로 13.0의 `libcudart.so.13`·`libcublas.so.13`을 문다(`ldd`로 확인). ik 기준선의 조건은 바뀌지 않았다.

우리 엔진은 cuda-oxide가 PTX를 만들고 드라이버가 JIT한다. 그래서 SASS를 만드는 쪽은 툴킷이 아니라 드라이버다. 이 추론을 측정으로 확인했다. 같은 트리를 `CUDA_TOOLKIT_PATH`만 13.0과 13.3으로 바꿔 지으면, 게이트 바이너리 셋의 `.oxart` 절이 모두 md5 `61edb10b6fbb664c958aa44ca00d8895`로 같다. 툴킷이 닿은 곳은 bindgen이 만든 드라이버 바인딩뿐이었다(9,471줄에서 10,189줄로, `cuGraphNodeGetParams`·`cuLaunchHostFunc_v2`·`cuDevSmResourceSplit` 같은 함수가 늘었다). 13.3 환경에서 GPU 게이트 16개를 돌렸더니 판정 줄 416줄이 전환 전 기준과 한 줄도 다르지 않았다. 교사 강제 불일치 40/41, σ 0.3641/0.3518, 그래프 648노드, eager = replay 두 패스까지 같다. 그래서 박스의 빌드 환경을 13.3으로 옮겼다.

바뀐 계기가 하나 있다. PATH의 Nsight Compute가 2025.3.1에서 2026.2.1로, Nsight Systems가 2025.3.2에서 2026.1.3으로 바뀌었다. ~~다음 프로파일 표는 새 계기로 잰 것이니 첫 표를 한 번 교차 확인한다.~~ 같은 새벽 정정: 러너가 부르는 것은 PATH가 아니라 `/usr/local/cuda/bin`의 13.0 래퍼라 둘이 갈린다 — `ncu-gpu.sh`는 바뀌었고 `nsys-gpu.sh`는 그대로다([아래 절](#cuda-13x-features)). 13.1–13.3에서 sm_86에 닿는 새 기능은 ~~조사 중이다~~ 아래 절에 적었다.

<a name="cuda-13x-features"></a>
## 13.1–13.3의 새 기능 중 우리에게 닿는 것은 B2의 합류 수단 하나다

13.0과 13.3의 헤더를 박스에서 나란히 비교하고 릴리스 노트와 PTX ISA 9.3·9.4를 읽었다. 조사이고, 아래 속성 조회만 측정이다. `cuda.h`는 함수가 499개에서 533개로 늘었고 지워진 것은 없다. 우리가 부르는 그래프 진입점은 그대로 풀린다.

새 명령어는 닿지 않는다. PTX 9.1–9.4에서 sm_86에 합법인 추가(`clmad`, `mma_throughput` 프래그마, `.mmio` acquire/release)는 전부 `.version` 9.1 이상을 요구하는데, 우리 PTX를 쓰는 LLVM 21의 NVPTX는 8.8에서 멈추고 실제로 나오는 모듈은 `.version 7.1`이다. 9.2의 바이트 단위 정수 SIMD(`.s8x4`)는 sm_107f·sm_120f 전용이다. 툴킷이 커널에 닿는 유일한 입력인 libdevice는 함수 19개 이상이 바뀌었지만 우리가 부르는 것은 없다. 위 절의 `.oxart` 바이트 동일과 맞는다.

쓸 만한 것은 CPU expert 티어와 GPU 스텝의 합류다. 13.2가 호스트 태스크에 스핀 대기 모드(`cuLaunchHostFunc_v2`)를, 13.1이 스트림 memop에 원자 축소를 더했고, 드라이버는 둘 다 이미 내보낸다. 컨텍스트 없이 되는 장치 속성을 두 카드에서 읽었다(`cuDeviceGetAttribute`, 드라이버 13040):

| 속성 | RTX A6000 | RTX 3090 |
|---|---|---|
| 64비트 스트림 memop (122) | 1 | 1 |
| `WAIT_VALUE_NOR` (123) | 1 | 1 |
| memop 원자 축소 (148) | 1 | 1 |
| DMA-BUF `mmap` (152) | 1 | 0 |
| v1 memop (92, 폐기) | 0 | 0 |

그래서 합류 후보는 넷이다. 블로킹 호스트 노드, 스핀 호스트 노드, 호스트가 쓴 플래그를 `cuStreamWaitValue32`로 기다리기, 원자 축소 카운터. 스핀은 드라이버 스레드가 코어 하나를 먹으므로 고정 CPU 풀에서 빠지는 몫까지 같이 잰다. 그래프 노드당 비용을 낮추는 기능은 없다(PDL은 sm_90 이상).

계기 쪽은 위 절 문장을 고친다. 러너는 PATH가 아니라 `/usr/local/cuda/bin`의 13.0 래퍼를 부른다. `ncu` 래퍼는 가장 새로 설치된 판을 골라서 이제 2026.2.1을 돈다. 그 판에서 러너가 남기는 "Local Memory Spilling Requests" 열은 공유 메모리 스필을 세지 않는다. `nsys` 래퍼는 2025.3.2에 고정돼 있어 그대로다. 그 판에는 호스트 함수 추적이 없어서 합류를 재는 라운드는 2026.1.3을 명시해 불러야 한다. `ptx-scan`의 ptxas는 13.0이고, 13.3의 ptxas는 커널 52개 중 4개를 레지스터 ±1 움직인다. 두 ptxas로 만든 표는 나란히 놓지 않는다.

<a name="ptx-9x-on-sm86"></a>
## LLVM을 올려 PTX 9.x를 쓰면 — 드라이버는 9.4까지 받고, 닿는 명령 하나는 flash를 스필시킨다

위 절이 남긴 질문 둘을 A6000에서 쟀다. 첫째, 드라이버 JIT가 새 `.version`을 받는가. 빈 sm_86 엔트리 하나짜리 PTX를 `cuModuleLoadDataEx`로 올렸다(드라이버 13040, 실행은 없다). 7.1·8.8·9.0·9.1·9.2·9.3·9.4는 전부 적재되고, 9.5는 `Unsupported .version 9.5; current version is '9.4'`로 거부된다. `.pragma "mma_throughput"`은 모듈 범위에 두면 `allowed only within function scope`, 9.2 모듈 안에서는 `requires PTX ISA .version 9.3 or later`로 막히고, 9.3 모듈의 엔트리 첫 문장으로는 적재된다. 막힌 곳은 드라이버가 아니라 PTX를 쓰는 쪽이다. 우리 모듈이 `.version 7.1`인 것도 LLVM 21의 한계(8.8) 때문이 아니다. cuda-oxide는 커널이 요구할 때만 버전을 올리고, sm_86의 바닥이 7.1이다.

둘째, 그 프래그마가 우리 커널에 무엇을 하는가. main 트리의 `generate`(09-22 빌드)에서 flash MMA 모듈을 잘라 `flash_latent_mma`를 ptxas 13.3 `-arch=sm_86`으로 세 번 지었다.

| 변형 | 레지스터 | 스택 | 스필 저장/적재 | SASS 줄 | HMMA |
|---|---|---|---|---|---|
| 원본 `.version 7.1` | 121 | 0 B | 0 / 0 B | 2,560 | 36 |
| `.version 9.3`만 | 121 | 0 B | 0 / 0 B | 2,560 | 36 |
| 9.3 + 프래그마 | 128 | 48 B | 72 / 76 B | 2,680 | 36 |

버전만 올린 판은 SASS가 바이트까지 같다. `.version`은 쓸 수 있는 명령의 목록이고, 최적화는 그 숫자로 바뀌지 않는다. 프래그마는 SASS를 실제로 바꾼다. 레지스터를 512스레드 블록의 상한인 128까지 쓰고도 모자라 스필이 생긴다. 시간은 재지 않았다. 다만 지금 형상에서 로컬 메모리 왕복이 새로 생긴 것은 손해 쪽 신호다. 드라이버 JIT는 이 ptxas와 레지스터 한두 개가 다를 수 있다.

잘 풀린 경우의 상한도 작다[유도]. 깊이 4096의 스텝(5.01 ms)과 깊이 6의 스텝(4.36 ms)의 차이가 flash의 깊이 의존 비용이고, 스텝의 약 13 %다. 커널이 10 % 빨라져도 스텝은 약 1.3 %, 깊이 6에서는 0에 가깝다. sm_86에 합법인 9.x 명령의 나머지 셋(`clmad`, `.mmio`, `ld/st.volatile.local`)은 대역폭에 묶인 gemv나 flash에 닿는 자리가 없다. 그래서 PTX 9.x를 이유로 LLVM(곧 cuda-oxide 핀과 nightly 핀)을 옮기지 않는다. 다시 볼 조건은 둘이다. 업스트림 cuda-oxide가 새 LLVM으로 옮겨 가 핀을 옮길 다른 이유가 생길 때(우리 핀의 소스가 이미 "pinned LLVM 23"을 적고 있다), 그리고 V4.1 커널에서 9.x 명령이 필요한 자리가 나올 때다.

<a name="engram-helper-lease"></a>
## engram 헬퍼가 스텝에서 사라지려면 제출이 대기보다 먼저 와야 한다 — 행 캐시는 스텝 스레드에 4 µs를 얹는다

bloomery B3b의 헬퍼 스레드 팔 셋과 B3e의 DRAM 행 캐시를 CPU 임대 아래에서 쟀다. 질문은 둘이다. 헬퍼를 두면 engram 조회 가운데 스텝 스레드에 남는 몫이 얼마인가, 그리고 캐시가 스텝 스레드에 얹는 비용과 덜어 주는 몫이 얼마인가. 네 번 모두 `just measure-engram`으로, 한 번에 임대 하나씩, 같은 바이너리로 돌렸다. 조건은 2,000토큰, 토큰당 48행이다. A는 `--cold-reset`에 기본 팔 여덟(시드 7, 균등 id), B는 헬퍼 팔 셋에 `--step-gap-us 500`, C는 실제 코드 스트림(`corpus-code.ids`)에서 pipelined 팔과 4 GiB 캐시 팔을 gap 0으로, D는 C에 gap 500을 준 것이다. 증인 블록 여덟 개의 loadavg는 0.58–0.79였다.

| 팔 | A (균등, gap 0) | B (균등, gap 500) | C (코드, gap 0) | D (코드, gap 500) |
|---|---:|---:|---:|---:|
| cold-serial | 3,838.6 | | | |
| cold-prefetch / cold-pipelined | 341.6 / 304.7 | | | |
| helper-touch | 363.1 | 376.9 | | |
| helper-populate | 366.5 | 368.7 | | |
| helper-pipelined | 351.3 | **1.0** | 209.0 | **1.1** |
| helper-cached (4 GiB) | | | 197.2 | 5.4 |

값은 스텝 창의 p50 µs다. cold-serial은 토큰당 major fault 50.94번, fault당 75.4 µs로 09-22의 92 µs보다 싸다. C·D의 캐시 팔은 토큰당 적중 27.50행·미스 20.50행으로, 시뮬레이터(`engram-reuse --limit 2000`)와 정수까지 같다. 드라이브가 읽은 바이트는 두 팔 모두 토큰당 89.3 KB였다. 캐시 팔의 스텝 스레드 조회는 p50 6.2 µs(C)와 2.4 µs(D)였고, D의 p99는 캐시 팔이 286.9 µs, pipelined 팔이 139.7 µs다.

판명된 것. gap 0에서는 어느 헬퍼 팔이든 스텝이 헬퍼 일 전부(351–367 µs)를 기다린다. 이것은 B3b가 구조로 예측한 대로다. 값이 예측 260–330 µs보다 높은 것은 헬퍼의 채우기·복사가 약 121 µs이기 때문이다. 48행의 minor fault가 한 번에 약 2.5 µs씩 든다. gap 500에서는 **pipelined 팔만** 1.0 µs로 사라졌고, touch·populate 팔은 여전히 약 370 µs를 기다렸다. 예측은 세 팔 모두 1–5 µs였는데 틀린 항은 제출 위치다. touch·populate 팔은 스텝이 제출하자마자 기다리므로 gap이 대기 뒤에 온다. 다음 토큰의 행을 gap 앞에 제출하는 것은 pipelined 팔뿐이다. 엔진에서는 스텝 시작에 id를 알고 조회는 1·14층에서 하므로, 시작에 제출하고 그 층에서 기다리는 pipelined의 모양이 자연스럽다.

캐시는 이 한가한 기계에서 거의 본전이다. 캐시 없는 팔도 251 GiB 기계의 page cache에서 같은 재사용을 공짜로 얻는다. 드라이브 바이트가 같은 것이 그 증인이고, 예측한 그대로다. gap 0에서는 캐시 팔이 11.8 µs 빨랐다(209.0 → 197.2). gap 500에서는 스텝 스레드에 4.3 µs를 더 얹었다(1.1 → 5.4). 틀렸던 것은 스텝 스레드 비용이다. 조회를 접근당 DRAM 미스 5–8번으로 잡아 10–40 µs를 예측했는데, 실측은 2.4–6.2 µs였다. 어느 항이 틀렸는지는 이 표로 가르지 못한다. 캐시가 이기는 자리는 여전히 가중치가 호스트 RAM을 채워 page cache가 행을 붙잡지 못하는 엔진의 기계다. 이 실행으로는 그 조건을 만들 수 없다.

<a name="v41-oracle-dump"></a>
## V4.1 오라클 덤프는 6분에 474 GB를 읽었다 — 예측 안이지만, 읽기 속도 가정이 틀렸다

bloomery의 오라클 v3 덤퍼로 V4.1의 중간 텐서 세트를 처음 떴다. ik `c10fbbcc`, CPU 백엔드만(CUDA를 숨김), `-ngl 0 -c 512 -t 32`, 토큰 다섯 개(`671,6102,294,8760,344`, BOS 없음), 기계 전역 CPU 임대 아래 증인 블록 둘. 명령은 `BLOOMERY_MODEL=deepseek41 ./tools/box.sh 'bash tools/ref/dump.sh'`이다. 발사 전 유도 추정은 1.5–8분, 파일 합 476,991,454,912 B를 읽고 page cache가 통째로 뒤집힌다는 것이었다.

| 항목 | 값 |
|---|---|
| 임대 구간(증인 사이) | 356 s |
| NVMe 읽기 | 926,140,912 섹터 = 474.18 GB |
| 평균 읽기 속도 | 1.33 GB/s |
| major fault | +40,923 |
| Cached | 204.5 → 256.6 GB (MemAvailable ~257 GB 그대로) |
| 그래프 | 노드 3,283, split 1 |
| 세트 | 텐서 3,165, skip 40, 그래프 입력 23, 정수 사본 140 |

시간과 바이트는 밴드 안이다. 읽은 양이 파일 합보다 2.8 GB 적은 것은 이미 캐시에 있던 몫이다. 틀린 것은 밴드 아래쪽의 전제다. 그 끝은 6 GB/s를 썼는데, 이 수치는 원 측정 없이 문서에서 인용한 값이었다(잰 것은 쓰기 5.74–5.85 GB/s뿐이다). 실제 populate는 1.33 GB/s로, 09-13 기록의 1.07 GB/s(256 GB에 4분) 쪽에 붙는다. 적재 동안 loadavg가 1–2에 머물렀으니 한 줄기로 읽은 것이다. V4.1을 콜드로 한 번 올리는 값이 약 6분이라는 뜻이다. 여러 줄기로 미리 읽으면 드라이브 속도에 다가갈 수 있다는 것은 아직 가설이다(재지 않았다).

정수 경로는 이번이 첫 실행이었고, 코드 독해로 적어 둔 값과 전부 맞았다. `dsv4_csa_state_write`는 i64 두 개 [0, 1], `dsv4_hca_state_write`는 [0..4](개수 5, 합 10, absmax 4), `csa_write_pos`는 [0, 2], `hca_write_pos`는 [0..4]이고, `inp_tokens`는 다섯 id와 같다. 2^24를 넘는 정수 원소는 230개로 예측 220–230개 안이다(engram 층 둘 × 테이블 24 × 5토큰 = 240 가운데). 노드 행 3,165개도 예측 약 3,631 ±20 % 안이다. skip 40개는 층마다 하나인 `blk.N.attn_output_a.weight (reshaped)`, 양자화된 가중치 뷰다. 체커(`check-int-twins.py`)는 사본 140개 전부의 크기·합·absmax·RNE를 확인했다. V2-Lite 세트는 건드리지 않았다.

<a name="v41-dense-reads-a6000"></a>
## V4.1 한 토큰의 GPU 행렬-벡터 곱은 A6000에서 24.7 ms다 — 큰 런치는 대역을 채웠고, 넘친 몫은 작은 런치에서 나왔다

bloomery의 `bench_v41`(B9)로 V4.1 디코드 토큰 하나가 GPU에서 내는 gemv 20개 사이트를 실제 형상과 장치 크기로 쟀다. 가중치는 합성이고, 층마다 다른 사본을 두어(2,103개, 장치 메모리 14.49 GB) 한 토큰의 읽기가 실제 스텝처럼 L2를 빗나가게 했다. 어텐션 커널·KV·norm은 빠져 있고, 라우팅 expert는 38층 × 6슬롯을 모두 GPU에 둔 천장이다. `just time-gpu-v41`을 한 번 돌렸다. A6000(300 W, 2100 MHz), 증인 load 1.36 → 1.34, IO 압력 avg10 0.00, 다른 카드 유휴. 먼저 사이트마다 첫·끝 사본의 여섯 행을 f64 참조와 대조해 20/20이 밴드 1e-5 안인 것을 본 뒤 쟀다. 사이트마다 64런치 이상을 그래프 하나로 잡아 7라운드 × 4번 재생했고, 토큰 그래프는 7라운드 × 2번 재생했다. µs는 라운드 최솟값이다(평균과 1 % 안). GB/s는 런치가 건드리는 바이트(가중치·활성값·출력) ÷ 최솟값이다.

| 사이트 | 커널 | 런치당 MB | 격자 | 런치 µs | GB/s | 토큰당 런치 | 토큰당 µs |
|---|---|---:|---:|---:|---:|---:|---:|
| `output`(헤드) | q6_K | 543.0 | 16,160 | 775.6 | 701 | 1 | 776 |
| `engram_wkv` | q8_0 | 176.9 | 3,200 | 298.2 | 594 | 2 | 596 |
| `attn_q_b` | q8_0 | 47.2 | 4,096 | 79.1 | 598 | 40 | 3,166 |
| `attn_output_b` | q8_0 | 47.2 | 640 | 83.0 | 569 | 40 | 3,318 |
| `ffn_down_exps` | q4_K `_sel` | 39.8 | 3,840 | 69.0 | 579 | 38 | 2,623 |
| `ffn_gate_exps` / `ffn_up_exps` | q3_K `_sel` | 30.4 | 1,728 | 47.7 / 49.2 | 639 / 619 | 38씩 | 3,683 |
| shared gate / up / down | q8_0 | 13.3 | 288 / 288 / 640 | 26.4 / 26.2 / 23.3 | 505 / 507 / 570 | 40씩 | 3,037 |
| `attn_output_a`, 그룹 8개를 따로 | q8_0 | 4.7 | 128 | 15.3 | 309 | 320 | 4,903 |
| `attn_output_a`, 같은 바이트를 한 런치로 | q8_0 | 37.7 | 1,024 | 66.7 | 567 | 40 | 2,666 |
| `attn_q_a` | q8_0 | 7.4 | 160 | 20.2 | 366 | 40 | 809 |
| `attn_kv` | q8_0 | 2.9 | 64 | 16.8 | 177 | 40 | 673 |
| `ffn_gate_inp`(라우터) | f32 | 7.9 | 48 | 16.4 | 481 | 40 | 656 |
| `indexer.attn_q_b` | q8_0 | 5.9 | 512 | 12.5 | 473 | 8 | 100 |
| 작은 q3_K 넷(압축기·인덱서) | q3_K | 0.03–1.1 | 4–64 | 1.45–5.8 | 14–196 | 19 | 90 |

오늘 엔진이 내는 모양의 토큰 그래프(784노드)는 최솟값 24,658.7 µs·평균 24,693.1 µs, 503 GB/s였다. 사이트 재생 시간의 합 24,431.6 µs보다 227 µs 길다. `attn_output_a`를 같은 바이트 한 런치로 둔 504노드 그래프는 22,414.8 µs(554 GB/s)였다. 빈 `touch` 커널로 같은 노드 수를 채운 그래프는 노드당 0.852 µs(784노드)와 0.871 µs(504노드)로, 3090에서 교정한 0.80 µs보다 조금 비싸다.

첫 실행 전에 적어 둔 유도는 토큰 그래프 22.73 ms였다(클래스 천장과 행 사슬 하한, 3090 상수). 측정은 8.5 % 길다. 큰 런치는 틀리지 않았다. 수십 MB짜리 런치는 567–701 GB/s로, 배치 문서가 A6000에 가정한 575 GB/s 근처이거나 그 위다. q3_K `_sel`은 3090의 0 채움 벤치를 비례 환산한 클래스 천장 491 GB/s보다도 빨랐다. 틀린 항은 작은 런치다. 한 파동이 안 되는 격자는 바이트가 아니라 행 하나를 걷는 적재 사슬에 묶이는데, 그 사슬이 커널마다 다르다. 격자 64·K 5120으로 조건이 같아도 q8_0은 16.8 µs, q3_K는 5.8 µs였다. 가장 크게 빗나간 것은 그룹으로 나뉜 `attn_output_a`다. 유도는 320런치를 한 런치로 합쳐도 노드 비용만 약 0.22 ms 준다고 했는데, 같은 바이트를 한 런치로 두니 2.24 ms가 줄었다. 0.38파동짜리 런치 하나가 15.3 µs였기 때문이다(유도 8.2). 다만 2.24 ms는 같은 바이트를 밀집 행렬 하나처럼 읽은 하한이다. 블록 대각을 한 런치로 도는 커널의 시간은 아니고, 그 커널은 아직 없다.

expert를 뺀 밀집 몫은 사이트 합으로 18.13 ms다. 배치 문서 (a)안이 A6000 밀집에 매긴 14.92 ms(575 GB/s)보다 21 % 길고, `attn_output_a`를 한 런치로 두면 15.89 ms(+6.5 %)다. 작은 격자의 q8_0·f32 사이트가 575 GB/s보다 더 쓴 시간은 합해 약 1.16 ms다[유도: 사이트마다 (측정 − 가중치 바이트/575 GB/s) × 런치 수]. 장치 메모리는 요청한 13,986,785,952 B보다 504,534,368 B(3.6 %) 더 쓰였다(`cuMemGetInfo` 여유의 차, 3090과 A6000에서 같았다). 원인은 가르지 않았다. 배치 (a)·(b)의 A6000 여유가 약 1.09 GB라, 이 차이가 안이 들어가느냐를 가를 수 있다.

<a name="v41-router-coverage"></a>
## V4.1의 라우터는 층마다 치우쳐 있다 — 층당 64개가 선택의 61–74 %를 받고, 코퍼스를 건너면 40–49 %다

배치 문서 §5는 VRAM에 층마다 expert id 접두 [0, n_l)을 두는 규칙을 기본으로 하고, 라우터가 치우쳐 있으면 층마다 가장 뜨거운 집합과 id 재매핑 표로 바꾼다고 적어 두었다. 그것을 가를 측정이다. bloomery의 `router_trace`(B10)는 ik `c10fbbcc`에 링크한 하니스로, 토큰 스트림을 프리필로 흘리며 층마다 `ffn_moe_topk-<층>`을 자기 보폭으로 읽는다. 이 노드는 라우터 확률의 ARGSORT를 [6, n_tokens]로 본 VIEW라, 데이터 포인터의 평평한 메모리는 argsort의 머리일 뿐이다. 설정은 오라클 덤프와 같다(CPU 백엔드, `-ngl 0 -t 32 --defer-experts`, 모든 노드에 콜백을 거는 스케줄). 먼저 오라클의 다섯 토큰으로 40층 1,200개 id가 덤프의 정수 사본과 전부 같은 것을 봤다. 두 코퍼스는 각 50,000토큰을 2,048토큰짜리 독립 문맥 25개로 돌렸다. code는 ik의 `src/*.cpp`를 이은 앞부분, prose는 ik 트리의 `.md`(README·CONTRIBUTING·docs)다. CPU 임대 아래 662.9 s와 751.8 s였다. 이 GGUF에는 해시 라우팅 층이 없다(`deepseek41.hash_layer_count = 0`, `ffn_gate_tid2eid` 없음).

층마다 가장 많이 뽑힌 n개가 받는 선택 몫(40층 평균, %). held-out은 앞 25,000토큰에서 목록을 배워 뒤 25,000토큰에서 잰 값이고, 건넘은 한 코퍼스 전체에서 배운 목록을 다른 코퍼스 전체에 쓴 값이다.

| 층당 n | 균등 | code | code held-out | prose | prose held-out | code 목록 → prose | prose 목록 → code |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 16 | 4.2 | 42.4 | 41.8 | 36.4 | 34.0 | | |
| 32 | 8.3 | 57.3 | 56.8 | 49.3 | 46.1 | 26.0 | 33.3 |
| 64 | 16.7 | 74.1 | 73.5 | 64.9 | 61.3 | 40.1 | 49.4 |
| 128 | 33.3 | 89.4 | 88.9 | 82.4 | 79.2 | 61.3 | 69.2 |
| 192 | 50.0 | 95.8 | 95.5 | 92.1 | 90.1 | | |

40층 전부 치우쳐 있다. held-out hot-64는 층마다 code에서 51.9–83.1 %, prose에서 47.9–73.7 %를 받고, 0–1층이 가장 덜 치우친다. 같은 스트림 안에서는 held-out이 자기 표본과 거의 같아(73.5 대 74.1, 61.3 대 64.9) 목록이 과적합이 아니다. (층, expert) 쌍을 전역으로 고르는 배분은 층마다 같은 n을 주는 것보다 모든 n에서 0.5포인트 이하만 더 준다. 목록에는 토큰이 많이 든다. 앞 2,048토큰으로 배운 hot-64는 뒤 절반에서 51.7 %(code)와 54.7 %(prose)였다. 코퍼스를 건너면 약해진다. 두 코퍼스의 hot-64는 층마다 평균 28.8개만 겹치고, 건넌 목록은 자기 held-out보다 21–24포인트 낮다. 그래도 균등의 2.4–3배다. 두 코퍼스 앞 절반을 합쳐 배운 목록은 자기 목록보다 3–6포인트만 잃었다. 두 코퍼스가 모두 한 프로젝트(ik의 C++와 그 문서)에서 나왔으니, 이 건넘 수치를 채팅·자연어 부하의 하한으로 읽으면 안 된다.

틀렸던 것 하나. 처음 하니스는 top-k 노드에만 콜백을 걸었는데, 그 스케줄로는 오라클과 835/1,200만 같았다. 콜백이 노드 하나만 요청하면 그래프 조각 안에서 ik CPU 백엔드의 선행 융합이 서빙 때처럼 일어나 반올림이 달라지고, 동점에 가까운 뒤집힘이 깊이를 따라 번진다(0–10층은 집합이 같고 11층부터 갈린다). 모든 노드를 요청하면 융합이 일어나지 않아 1,200/1,200이다. 두 스케줄의 prose 2,048토큰 hot-64 목록은 평균 62.5/64개가 겹쳤고 몫은 둘 다 70.7 %였다. 프리필과 단일 토큰 디코드의 라우팅 차이는 재지 않았다. 이 몫은 배치 1 디코드에서 expert 가중치 읽기가 VRAM에서 나오는 비율과 같다. 여러 토큰을 한 ubatch로 돌리면 배치 안에서 겹친 expert는 한 번만 읽힌다.

<a name="qdot-rate-vs-ik"></a>
## CPU qdot 커널은 한 코어에서 ik의 x4 커널보다 2–14 % 느리다

bloomery의 AVX2 qdot 커널과 ik 자신의 x4 커널을 한 코어에서 견줬다. `just measure-qdot-rate 4`는 ik의 `*_rate` 하네스 다섯(ik `c10fbbcc`)과 bloomery `qdot-rate`를 같은 CPU 임대 안에서 코어 2에 고정해 번갈아 돈다. 라운드마다 순서를 뒤집었고 4라운드였다. 형상은 둘이 같다(360,448행 × 6패스, 타입마다 같은 K). 바이트가 무작위라 결과 값은 NaN이고 속도만 본다. 증인 load 0.61 → 1.07, CPU·IO 압력 0, 두 GPU 유휴, 43초.

| 타입 | K | 행 B | ik 평균 s | bloomery 평균 s | bloomery가 느린 정도 | ik GB/s | bloomery GB/s |
|---|---:|---:|---:|---:|---:|---:|---:|
| Q5_0 | 1,408 | 968 | 0.1445 | 0.1645 | +13.8 % ± 2.2 | 14.5 | 12.7 |
| Q4_K | 2,048 | 1,152 | 0.1463 | 0.1607 | +9.9 % ± 2.8 | 17.1 | 15.5 |
| Q5_K | 2,304 | 1,584 | 0.2015 | 0.2162 | +7.3 % ± 2.5 | 17.0 | 15.9 |
| Q6_K | 2,048 | 1,680 | 0.2058 | 0.2128 | +3.4 % ± 2.0 | 17.7 | 17.1 |
| Q5_1 | 10,944 | 8,208 | 1.158 | 1.179 | +1.8 % ± 1.5 | 15.3 | 15.1 |
| Q3_K | 2,048 | 880 | — | 0.1754 | — | — | 10.9 |

±는 관측 산포에서 낸 95 % 구간이다(Welch, 자유도 4–5). 팔마다 라운드 산포가 0.4–1.7 %로, 같은 바이너리 A/A의 0.6 %보다 넓다. ik 하네스는 시간을 ms 단위로만 찍는다.

다섯 타입 모두 bloomery가 느리고, 구간이 0에 닿는 것은 없다. 가장 큰 차이는 Q5_0과 Q4_K(V4.1 라우팅 expert의 down)다. 이것이 스텝의 몫인지는 이 표로 말할 수 없다. 한 코어가 15–17 GB/s를 내니 열 코어 남짓이면 DRAM 147.7 GB/s에 닿는다[유도]. 32스레드로 도는 스텝에서는 커널 속도보다 DRAM이 먼저 벽일 수 있다. CPU 커널 라운드를 열기 전에 스레드 스윕으로 값을 매긴다. V4.1 호스트 expert의 gate·up인 Q3_K는 ik에 짝 하네스가 없어 비교 값이 없다.

<a name="lane-unroll-8"></a>
## q8_0 행보에 적재를 두 배로 띄우니 V4.1 토큰의 GPU gemv가 8.7 % 줄었다 — 작은 런치는 반이 아니라 0.72–0.79배였다

[위 절](#v41-dense-reads-a6000)에서 격자가 작은 q8_0 런치는 바이트가 아니라 행 하나를 걷는 적재 사슬에 묶였다. 워프가 한 번에 띄우는 적재는 32값 청크 넷(144 B)이었다. 여덟 청크를 먼저 띄우면 왕복 수가 반이 되고, 왕복 지연이 그대로면 작은 런치도 반으로 준다고 유도했다(`attn_kv` 16.8 → 8.4 µs, 토큰 −3.2 ms). 레인마다 합산 순서는 그대로이니 출력은 비트 동일해야 한다. bloomery `9ea91c9`(`LANE_UNROLL` 4 → 8)가 그 변경이고, GPU 게이트 넷의 판정 줄이 base와 같았다(gate_e2e의 1,056위치 forced_exact와 σ 포함). 시간은 `just time-gpu-v41`을 base 트리와 변경 트리에서 번갈아 두 번씩 돌렸다. A6000(300 W, 2100 MHz), 증인 블록 여덟 모두 A6000에 다른 프로세스가 없었다(3090에는 두 블록에서 다른 라운드의 260 MiB). base 바이너리는 위 절의 것과 sha256이 같다. µs는 두 라운드 최솟값의 평균이다.

| 사이트 | 격자 | K | base µs | 변경 µs | 비 | 토큰당 Δ µs |
|---|---:|---:|---:|---:|---:|---:|
| `attn_kv` | 64 | 5,120 | 16.67 | 11.98 | 0.72 | −188 |
| `attn_output_a`(그룹 하나) | 128 | 4,096 | 15.27 | 11.92 | 0.78 | −1,070 |
| `attn_q_a` | 160 | 5,120 | 20.27 | 16.05 | 0.79 | −169 |
| `ffn_gate_inp`(라우터, f32) | 48 | 5,120 | 16.39 | 13.49 | 0.82 | −116 |
| shared gate / up | 288 | 5,120 | 26.53 / 26.36 | 26.11 / 25.88 | 0.98 | −36 |
| shared down | 640 | 2,304 | 23.44 | 22.48 | 0.96 | −38 |
| `engram_wkv` | 3,200 | 6,144 | 298.3 | 260.3 | 0.87 | −76 |
| `attn_q_b` | 4,096 | 1,280 | 78.96 | 72.84 | 0.92 | −245 |
| `attn_output_b` | 640 | 8,192 | 83.19 | 77.01 | 0.93 | −247 |

바꾸지 않은 커널(q3_K·q4_K `_sel`, q6_K 헤드, 작은 q3_K 넷)은 0.98–1.06배였다. 오늘 모양의 토큰 그래프(784노드)는 24,741.7 µs에서 22,577.1 µs로 2.16 ms(8.7 %) 줄었다. `attn_output_a`를 한 런치로 둔 504노드 그래프는 22,506.9 µs에서 20,932.7 µs로 줄었다. 같은 팔의 두 라운드는 0.4–0.6 % 안에서 갈렸다.

유도는 토큰에서 68 %만 맞았다. 틀린 항은 둘이다. 첫째, 왕복 지연은 상수가 아니었다. 런치에 고정 항이 없다고 보면 `attn_kv`의 왕복당 지연은 417 ns에서 599 ns로 늘었다. 사이트에 따라 +44–58 %다[유도]. 비행 중 바이트가 두 배가 되면 메모리 쪽 대기도 길어진다. 둘째, 큰 q8_0 런치는 대역에 닿아 있지 않았다. 567–598 GB/s가 615–680 GB/s가 됐고, 토큰 이득 가운데 0.6 ms가 여기서 나왔다. 유도는 이 몫을 0으로 봤다. 라우터 f32는 격자 48이 SM 84개보다 작아 SM 수에 묶일 것으로 봤는데, 0.82배가 됐다. 이것도 사슬이었다. 묶음 `wo_a`를 같은 바이트의 한 런치로 도는 커널은 아직 없다. 그 커널이 걷어 갈 몫은 2.23 ms에서 1.64 ms로 줄었다.

V2-Lite에서도 같은 순서로 `just time-gpu-generate`를 번갈아 돌렸다(깊이 6, 32토큰 생성, ctx 512, graph). 스텝 p50은 4.3336·4.3301 ms에서 4.2698·4.2711 ms로 61 µs(1.4 %) 줄었고, 팔 안 산포는 0.03–0.08 %였다. 라우터 26층의 왕복을 반으로 줄인 몫을 −50 … −86 µs로 유도했었다. P8 블록 0의 op별 표에서 `q_nope2`(K = 128) 행은 그대로였다. 예측도 0이었다. ~~툴킷 ptxas가 그 짧은 행의 꼬리를 이미 풀어 적재를 띄우고 있었기 때문이다.~~ 근거는 툴킷 ptxas 13.3의 SASS다. 거기서는 그 짧은 행의 꼬리가 이미 풀려 적재가 함께 떴다. 실제로 도는 코드는 드라이버(UMD 13.4)가 PTX에서 JIT한 것이고, 그 SASS는 보지 않았다.

<a name="v41-router-four-corpora"></a>
## 라우터의 뜨거운 목록은 도메인을 건너면 29–49 %가 되고, 셋을 합쳐 배우면 처음 보는 넷째에서 40–49 %다

[위 절](#v41-router-coverage)의 두 코퍼스는 한 프로젝트에서 나왔다. 그래서 서빙 부하에 더 가까운 둘을 더 돌렸다. korean은 bloomery `9a339d3`의 `docs/` 가운데 한글이 20 % 이상인 문서(266,143토큰), threads는 ik의 GitHub 이슈·PR 스레드(4,515,000토큰)이고, 둘 다 앞 50,000토큰을 썼다. 설정은 위 절과 같다(2,048토큰 문맥 25개). CPU 임대 아래 810.8 s와 792.5 s가 걸렸다.

아래 표는 층당 hot-64가 받는 선택 몫이다(40층 평균, %, 균등은 16.7). 대각선은 자기 코퍼스의 held-out이다. 나머지 칸은 행 코퍼스 전체에서 배운 목록을 열 코퍼스 전체에 쓴 값이다. 마지막 행은 열 코퍼스를 뺀 나머지 셋의 횟수를 합쳐 배운 목록이다. 코퍼스마다 5만 토큰이라 무게가 같다.

| 목록 \ 쓴 곳 | code | prose | korean | threads |
|---|---:|---:|---:|---:|
| code | **73.5** | 40.1 | 28.6 | 30.9 |
| prose | 49.4 | **61.3** | 40.4 | 41.8 |
| korean | 38.4 | 44.7 | **73.5** | 41.6 |
| threads | 38.8 | 46.2 | 39.3 | **57.6** |
| 나머지 셋 합침 | 44.1 | 48.9 | 40.5 | 40.1 |

새 둘도 층마다 치우쳐 있다. in-sample 몫은 korean 75.1 %, threads 61.8 %다. 한 도메인에서 배운 목록은 다른 도메인에서 크게 흔들린다. 가장 나쁜 칸은 코드에서 배운 목록을 한국어 문서에 쓴 28.6 %다. 셋을 합친 목록은 처음 보는 넷째에서 40.1–48.9 %였다. 가장 나쁜 단일 목록보다는 늘 낫다. 가장 좋은 단일 목록과 비교하면 −5.3에서 +2.7포인트다(code에서는 prose 목록 하나가 49.4로 더 낫다). 정적 목록을 쓴다면 여러 도메인을 합쳐 배워야 한다는 뜻이다. 자기 도메인 held-out과의 차이 약 10–33포인트는 온라인 갱신이 되찾을 수 있는 몫이다[유도]. 합친 목록의 몫은 `counts.tsv`에서 따로 계산했다. 그 계산으로 단일 목록 열두 칸도 냈는데, `router-coverage.py transfer`의 출력과 소수 첫째 자리까지 같았다.

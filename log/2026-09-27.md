# 2026-09-27

<a name="sit10"></a>
## 시팅 10 — V4.1 참조 세트 셋을 공개 파일로 다시 떴다, 4분 42초

V4.1 게이트가 읽는 오라클 세트 가운데 셋이 아직 옛 혼합 양자화 파일(`…-engramQ8-tokembdBF16-attnQ8`)에서 뜬 것이었다. long 게이트 `--free`가 읽는 ik greedy(프롬프트 7번 줄), `gate-ds41-kld`의 KLD 기준 둘, DSpark 게이트 넷이 읽는 드래프트 오라클 `code64_n32_w3`이다. 09-25에 "수 시간"짜리 밤 시팅으로 승인받았는데, 조사 라운드(sit10plan)가 옛 실행의 증거로 다시 값을 매기니 CPU 잡 다섯, 5.5–9.5분이었다[유도]. 03:25:51–03:30:33 KST에 홀드 하나를 걸고 돌렸다. 그 시각에 살아 있던 빌더 라운드는 하나(records)였다. 첫 잡의 사전 증인은 loadavg 0.40에 CPU 압력 0이었고, 시팅 내내 CPU 압력은 0.1 아래였다. 그러니 옆에서 돈 빌드는 없었다. 옛 세트는 지우지 않고 `ikppl/mixed/`, `greedy-ds41/mixed/`, `ref-draft/code64_n32_w3-mixed/`로 옮겼다. 세트 이름과 게이트 상수(`db517b69`)는 그대로다.

| 잡 | 벽시계(러너 `wall_s`, dsref는 레시피) | 예측[유도] | 결과 | 혼합 파일 때 |
|---|---|---|---|---|
| ik-ppl `kldbase-c2048x4` | 133 s | 138–184 s | PPL 2.2379 ± 0.0624, 페이지인 65.8 GB | 1.8989 ± 0.0491, 146 s |
| ik-ppl `kldbase-c512x16` | 103 s | 116–131 s | PPL 2.6235 ± 0.0858, 페이지인 2.7 GB | 2.2025 ± 0.0697, 126 s |
| ik-greedy 7번 줄 | 14 s | 30–45 s | id `16600,4465,260,1014` 같음, 첫 토큰 1031 | 33 s |
| ik-greedy 0번 줄 | 7 s | 23–30 s | id `671,6102,294,8760,344` 같음 | 25 s |
| dump-ref-draft | 14 s | 36–60 s | 블록 11, 제안 31, 수락 19, `# complete 2472 33` | 블록 12, 35, 18, `2705 36`, 39 s |

홀드를 건 시간은 모두 4분 42초로, 예측 대역 5.5–9.5분보다 짧았다. 모든 잡이 대역 아래로 빠졌다. 라운드는 혼합 파일 실행에 맞춘 연산 항을 그대로 옮겨 오면서 배수 f를 0.9–1.05로 가정했다. 첫 잡의 페이지인 65.8 GB는 예측한 차가운 expert 바이트 68.8 GB와 맞았다. 그러니 틀린 항은 연산 쪽이다(고정비 22 s도 두 점 적합이라 약하다). 고정비 22 s와 페이지인 몫(0.145 s/GB)을 빼면 c2048×4의 연산은 약 101 s, c512×16은 약 81 s다. 혼합 파일 때 118 s와 103 s였으니 f는 0.86과 0.80이다[유도]. 혼합 파일은 attention·공유 expert·engram이 Q8_0이었고 공개 파일은 그보다 낮은 비트다. 이것이 f < 1의 후보 설명이다. 가르는 실행은 하지 않았다.

PPL은 공개 파일에서 18–19 % 높다. 이 비교에는 두 가지가 섞여 있다. 혼합 파일 기준은 sink 수정 전 트리(`49ef19d0`)에서 떴고, 이번 것은 수정 뒤 트리(`db517b69`)다. 그래서 파일 몫과 트리 몫을 가를 수 없다. 게이트는 같은 파일 위의 우리 엔진과 ik를 비교하므로, 이 차이가 판정에 들어가지는 않는다.

검증 묶음 10항목도 모두 rc 0이다(933 s). `gate-gpu-dspark-graph`는 09-25부터 공개 파일에서 FAIL 61–63줄을 내는 빨강이었는데, FAIL 0으로 초록이 됐다(`hc_init` 비트 동일 PASS). KLD 게이트는 새 기준 둘을 읽고 통과했고, dspark-kv·hc·experts·dspark-loop·chat·serve의 판정 줄은 시팅 전과 같다. long `--free`의 판정도 그대로다. 첫 차이가 생성 0번째 스텝에 있고 우리 margin은 0.4118이다(통과 규칙은 1.5 미만). 바뀐 것은 ik 쪽이다. 공개 파일에서 ik의 7번 줄 greedy는 60번째 토큰에서 EOS로 끝난다. 혼합 파일 때는 64개까지 EOS가 없었다.

같은 파일 위에서 우리 엔진과 ik를 비교한 첫 Δ_PPL은 +0.105 %다(`run-ds41-ppl kldbase-c2048x4`, 3090 게이트 배치, 4,092위치, 356 s). PPL은 우리 2.2401, ik 2.2378이고, KLD(ik‖ours)는 0.00987 ± 0.00049, top-1 일치는 97.46 %다. 예측 대역 −0.5…+0.5 % 안이고, 빨강 기준은 +1.5 %다. 09-24의 혼합 파일 비교(뜨거운 목록 배치: +0.070 %, KLD 0.00601, 97.87 %)보다 KLD가 크다. 파일과 배치가 둘 다 달라서 원인은 가르지 않았다.

# Rust로 쓴 첫 커널, 같은 스펙을 두 모델에 — 0.40배, 1.05배, 그리고 둘을 합쳐 1.86배

**2026-09-19, 05:40–06:36 UTC, RTX 3090 한 장, DeepSeek-V2-Lite-Chat Q3_K_M의 `blk.1.ffn_gate_exps.weight`.** 엔진 프로젝트 [mulle](https://github.com/midagedev/mulle)의 0단계다. 질문은 하나였다. NVIDIA의 CUDA Rust(cuda-oxide, SIMT 트랙)가 이 카드(sm_86)에서 ggml의 양자화 gemv에 얼마나 다가가는가. 어제 조사에서 sm_86 성능 수치가 어느 트랙에도 없다는 것이 확인됐으니, 이 표가 첫 것이다. 구현은 위임했고 같은 스펙을 GLM-5.3과 muse-spark-1.3에 워크트리를 나눠 보냈다. 변수는 모델 하나다. 게이트는 내가 양쪽 다 다시 돌렸다.

## 잰 것

가중치는 Q3_K 64 expert 스택(90,112행 × 880 B = 79.3 MB, L2 6 MB를 넘는 대역폭 바운드 형상)과 expert 하나(1,408행, 1.2 MB, L2 상주라 지연 바운드). 활성값 M은 1과 8. 참값은 ggml 자체 `dequantize_row_q3_K`로 CPU에서 역양자화한 f32 내적이고, ggml의 CUDA `mmvq`와 Rust 커널을 같은 파일·같은 카드·같은 분에 20 워밍업 + 200 타이밍으로 잰다. 아래는 내 재실행 값이다. 위임자 값과의 차이는 4% 안이다.

| 엔진 | 형상 | M | µs/런치 | GB/s | 최대 상대 오차 |
|---|---|---:|---:|---:|---:|
| ggml mmvq | stack | 1 | 238.0 | 333.1 | 4.1e-3 |
| Rust, A팔 (GLM) | stack | 1 | 1264.9 | 62.7 | 1.5e-6 |
| Rust, B팔 (muse) | stack | 1 | 589.9 | **134.4** | 1.3e-7 |
| ggml mmvq | stack | 8 | 472.6 | 167.8 | 3.9e-3 |
| Rust, A팔 | stack | 8 | 9201.7 | 8.6 | 1.6e-6 |
| Rust, B팔 | stack | 8 | 2428.0 | **32.7** | 1.3e-7 |
| ggml mmvq | expert0 | 1 | 10.4 | 119.0 | 4.4e-3 |
| Rust, A팔 | expert0 | 1 | 30.7 | 40.4 | 1.4e-6 |
| Rust, B팔 | expert0 | 1 | 18.3 | 67.9 | 1.9e-7 |

ggml의 오차 4e-3은 활성값을 q8_1로 양자화하는 설계의 것이고 실패가 아니다. Rust 두 팔은 f32 경로라 1e-6·1e-7이다. 정확도 게이트(≤ 1e-4)는 둘 다 통과했고, 대역폭 목표(stack에서 ggml의 0.9배)는 둘 다 못 넘었다. B가 0.40배, A가 0.19배. 스펙이 허용한 최적화는 1회였고 둘 다 거기서 멈춰 보고했다.

## 두 팔이 갈린 곳

느린 이유는 둘 다 같은 자리, x(활성값)를 어떻게 읽는가였다. A팔은 워프 하나가 행 하나를 맡고 레인마다 x의 사적 64-float 구간을 소유하게 했다. 그러면 워프 단위 x 적재 한 번이 8 KB에 흩어진 32섹터 gather가 되고, A팔 자신이 SASS(`ptxas -v`, 명령 888개, 레지스터 64–71, 스필 0)를 읽어 워프-내적당 스칼라 `LDG.E` 89개(M=8에서는 537개)가 지배한다고 원인을 좁혔다. 허용된 한 번의 패스는 전부 명령 측이었다. 레지스터 윈도, 로컬 메모리 배열을 `match`로 대체, `#[unroll]` while 루프, 분기 없는 hmask 비트. 51.5에서 66.0 GB/s. 매핑은 못 건드렸다.

B팔은 처음부터 x를 블록당 dynamic shared memory에 한 번 스테이징했고, 그 한 걸음이 60에서 124 GB/s였다. 32행/블록과 u32 로드, 스칼라 누산기가 140까지 올렸다. 그래서 같은 스펙에서 B의 커널이 M=1에 2.1배, M=8에 3.8배 빠르다.

다음 레버는 둘의 보고가 같다. ggml `mmvq`처럼 워프가 super-block 하나에 협력하고 x를 셔플이나 레지스터로 브로드캐스트하는 레인 재매핑, 그리고 x 로드를 16 B 정렬로 `LDG.E.128`에 붙이는 것. A의 명령 측 개선은 B 위에 다시 얹을 수 있다. main에는 B를 올렸고 A의 결과 파일은 이름을 바꿔 보존했다.

## 위임 A/B로서 본 것

| | GLM-5.3 (effort high) | muse-spark-1.3 (effort high) |
|---|---|---|
| 벽시계 | 38분 | 20분 |
| 산출물 속도 (stack M=1) | 62.7 GB/s | 134.4 GB/s |
| 진단 깊이 | SASS까지 읽어 원인을 명명 | 패스 표로 어느 걸음이 먹혔는지 명명 |
| 스펙의 틀린 전제(`ggml_get_type_traits`, 실제는 `ggml_internal_get_type_traits`) | 조용히 맞는 이름을 쓰고 보고 안 함 | 정정을 보고 |
| `#[unroll]` | `u32` 카운터 while 루프에서 작동 | 컴파일러 ICE `APInt::shl: bitwidth mismatch (64 vs 32)` — `usize` 카운터 |
| 쓴 토큰(출력, 추론 포함 / 모델 턴) | 357k / 224 | 79k + 추론 45k / 76 |
| 후속 effort 추천 | max | medium |

어느 쪽이 낫다고 한 줄로 쓸 수는 없다. 산출물은 B, 원인 보고는 A. 다만 두 사실은 남는다. 이 과제에서 muse는 절반 시간에 두 배 빠른 커널을 냈고, 스펙의 잘못을 말한 쪽도 muse였다. ICE는 A가 같은 툴체인에서 `#[unroll]`을 돌렸으니 카운터 타입 차이일 가능성이 크고, 재현자를 만들어 NVlabs/cuda-oxide에 보낼 후보다(MUL-7).

## 2라운드 (22:50–23:29 UTC): 같은 두 팔, ggml의 구조로 — 1.05배

1라운드의 두 보고가 같은 레버를 가리켰으니 2라운드 스펙은 그 레버였다. ggml `mmvq`가 하는 대로 x를 q8_1로 한 번 양자화해 `dp4a` 정수 내적을 쓰고, 가중치 워드를 코얼레스해 읽는 것. 설계를 바꾸면 오차 게이트도 바뀐다는 것을 스펙에 미리 썼다. q8_1을 택하면 오차는 ggml 자신과 같은 4e-3대가 되고 게이트는 1e-2다. 1e-7과 q8_1을 동시에 주장하는 보고는 적신호라고도 썼다. 두 팔 다 설계를 명시했고 둘 다 4.2e-3에 왔다. 게이트는 stack M=1에서 0.9배, 최적화 최대 3단계. 워크트리·박스 디렉터리·데이터 디렉터리를 팔마다 분리하고 내가 두 팔을 순서대로 다시 돌렸다.

| 엔진 | 형상 | M | GB/s | ggml 대비 | 최대 상대 오차 |
|---|---|---:|---:|---:|---:|
| ggml mmvq | stack | 1 | 333.0 | — | 4.1e-3 |
| Rust, GLM 팔 | stack | 1 | **348.3** | **1.046배** | 4.2e-3 |
| Rust, muse 팔 | stack | 1 | 303.5 | 0.91배 | 4.2e-3 |
| ggml mmvq | stack | 8 | 167.3 | — | 3.9e-3 |
| Rust, GLM 팔 | stack | 8 | 139.2 | 0.83배 | 3.9e-3 |
| Rust, muse 팔 | stack | 8 | 131.5 | 0.78배 | 3.9e-3 |
| ggml mmvq | expert0 | 1 | 118.7 | — | 4.4e-3 |
| Rust, GLM 팔 | expert0 | 1 | 137.8 | 1.16배 | 4.3e-3 |
| Rust, muse 팔 | expert0 | 1 | 147.8 | 1.24배 | 4.3e-3 |

둘 다 게이트를 넘었다. 그러니 이 카드(sm_86)에서 cuda-oxide로 쓴 Rust 양자화 gemv는 M=1에서 ggml을 따라잡는다. 어제 조사에서 "없다"고 적은 sm_86 수치가 이제 하나 있고, 엔진 계획의 "전부 Rust 커널" 열은 살아 있는 열이 됐다. M=8은 둘 다 0.8배 안팎에서 멈췃고 원인 보고가 같다. 4-weight 필드마다 자기 q8_1 블록 스케일이 붙어 `dp4a` 하나에 FMA 하나가 따라온다. ggml은 32-weight에 float 하나다. 후속은 MUL-8이다.

두 팔이 갈린 곳은 구조다. GLM은 양자화를 별도 커널로 떼어 토큰당 한 번 돌게 했고, gemv는 워프당 한 행에 짝·홀 super-block을 레인 절반씩 나눠 정렬 u32 로드와 funnel shift로 읽는다. 1단계로 `#[unroll]`을 super-block 루프에 걸었다가 35% 느려지는 것을 `ptxas -v`로 잡아(레지스터 60→122, 점유율 67→33%) 되돌렸다. muse는 양자화를 gemv 블록 안에 융합하고 워프가 super-block 28워드를 shared memory 슬롯에 협력 적재한다. 1단계가 명령 바운드라는 것을 `ncu`로(명령 41.3억 대 FMA 1.84억) 보인 뒤 산술을 바꿨다. 커널 하나로는 둘 다 맞지만 엔진에서는 다르다. 융합 설계는 블록마다 x를 다시 양자화하고, 분리 설계는 모든 expert와 층이 q를 재사용한다. main에는 GLM 팔을 올렸다(mulle 9d83f97).

| 2라운드 | GLM-5.3 | muse |
|---|---|---|
| 벽시계 | 39분 | 34분 |
| stack M=1 | 1.046배 | 0.91배 |
| 증거 도구 | `ptxas -v`, 되돌린 단계 포함 | `ncu`, 명령 수 |
| 디버깅 | 양자화 커널의 인덱스 버그를 CPU 시뮬레이션으로 잡음 | 디코드 함정 둘을 측정으로 잡고 256 weight 전부 대조 |
| 규약 충돌 보고 | — | "결과 파일 영어" 지시가 레포 CLAUDE.md의 한국어 규약과 충돌함을 명시 |
| 쓴 토큰(출력, 추론 포함 / 모델 턴) | 443k / 172 | 137k + 추론 93k / 94 |

effort는 두 팔 다 `high`였지만 그 라벨은 벤더 척도 위의 이름이라 통제 변수가 아니다. 토큰 행이 통제 변수고, GLM이 두 라운드 모두 출력 토큰을 3~4배 썼다(z.ai는 추론을 출력에 접고 muse는 따로 센다). 속도 행은 그 옆에서 읽어야 한다.

1라운드와 합치면 두 라운드 네 팔의 결론은 이렇다. 어느 모델이 낫다가 아니라, 같은 스펙을 두 팔에 보내면 원인 보고 둘이 겹치는 자리가 다음 스펙이 되고 그게 한 라운드 만에 0.40배를 1.05배로 만들었다. A/B의 값은 승자가 아니라 겹침이었다.

## 3라운드 (23:44–00:04 UTC): 두 팔 `max`, M=8 — 그리고 둘을 합치면 1.86배

2라운드에서 둘 다 0.8배 안팎에 멈춘 M=8이 과제였다. 이번엔 두 팔을 `max`로 돌렸고 토큰도 적었다. 게이트는 한 런에서 M=8 ≥ 0.9배 **그리고** M=1 ≥ 1.0배. 두 팔이 서로 다른 레버를 잡았다.

| 3라운드 (`max`) | GLM-5.3 (20분) | muse (14분) |
|---|---|---|
| 잡은 레버 | q8_1 블록을 128-value(= Q3_K 반 super-block)로 — 레인의 dp4a 넷이 스케일 하나를 공유, 열마다 FMA 1회 | 디바이스 로드 73곳의 경계 검사 제거(`get_unchecked` + SAFETY 근거), `drow` 호이스팅 |
| stack M=1 (ggml 333 GB/s) | 379.8 (1.14배) | **522.4 (1.57배)** |
| stack M=8 (ggml 167 GB/s) | **178.0 (1.06배)** | 166.4 (0.99배) |
| 레지스터/스필 | 56 / 0 | 40 / 0 |
| 쓴 토큰(출력, 추론 포함 / 턴) | 257k / 125 | 57k + 추론 33k / 70 |
| 스펙 전제 정정 | — | `QR3_K`는 2가 아니라 4(`ggml-common.h`) |

둘 다 두 게이트를 넘었고, 레버가 직교했다. 그래서 이번엔 고르지 않고 합쳤다. GLM의 기하 위에 커널 모듈의 `q`·`d8`·`x` 로드 44곳을 `get_unchecked`로 바꿨다(`w` 로드는 muse 머지로 이미). 내가 3090에서 돌린 결합 커널:

| 형상 | M | ggml GB/s | mulle GB/s | 비율 | 오차 |
|---|---:|---:|---:|---:|---:|
| stack | 1 | 332.7 | **620.3** | **1.86배** | 3.5e-3 |
| stack | 8 | 167.5 | **200.2** | **1.20배** | 4.6e-3 |
| expert0 | 1 | 118.7 | 173.5 | 1.46배 | 4.8e-3 |
| expert0 | 8 | 76.9 | 84.6 | 1.10배 | 3.8e-3 |

620 GB/s는 3090 이론치 936의 66%다. ggml의 333은 36%다. 네 형상 전부 ggml `mmvq`를 넘고 오차는 ggml과 같은 대역이다. 이 카드에서 Rust로 낸 양자화 gemv는 하루 만에 0.40배에서 1.86배가 됐고, 세 라운드 여섯 팔 중 어느 하나가 낸 것이 아니다. 매 라운드 두 보고가 겹친 자리(1→2라운드)나 갈린 자리(3라운드)가 다음 걸음이었다.

경계 검사 한 항목은 따로 적어 둘 가치가 있다. 1라운드 GLM 팔은 `--unchecked-indexing`을 켜 1% 얻었다. 그때 커널은 gather 바운드였다. 3라운드 muse 팔은 같은 스위치를 코드로 박아 47%를 얻었다. 커널이 대역폭 바운드에 가까워지자 검사 분기가 남은 병목이 된 것이다. 같은 레버의 값이 커널 상태에 따라 50배 달랐다. "전에 안 먹혔다"는 이유로 레버를 지우면 안 된다.

토큰 한 줄. `max`인 3라운드가 `high`인 2라운드보다 두 팔 다 적게 썼다(GLM 257k 대 443k, muse 90k 대 230k). 과제가 작고 게이트가 일찍 닫혔기 때문이다. effort 라벨은 벤더 사이에서도, 과제 사이에서도 비교 축이 아니다. 토큰 행이 축이다.

## 4라운드: 과제 유형을 둘 바꿔서

커널 하나를 세 번 다듬은 비교라 유형이 하나였다. 그래서 마지막 라운드는 같은 두 모델에 다른 부류 둘을 줬다. 하나는 CPU다. 이 기계의 CPU expert 티어(plan.md 2단계)를 위해 Q3_K×Q8_K gemv를 Rust AVX2 intrinsics로 쓰고, CCD 네 개에 행 범위를 나눠 핀 고정한 스레드로 돌리고, 같은 텐서에서 ik_llama.cpp CPU 백엔드와 대조한다. 형상에 `big`(blk.1–2 gate+up, 317 MB)을 넣은 것은 L3 128 MB를 넘겨 DRAM을 재려는 것이고, 게이트는 big M=1에서 각 엔진 최적 스레드 수 기준 0.9배 이상이다. 스레드 스윕 8/16/32/64를 양 엔진에서 내는 것이 산출물이다. 다른 하나는 컴파일러다. 1라운드 muse 팔이 만난 cuda-oxide `#[unroll]` ICE(MUL-7)의 최소 재현기를 만들고, 변수 하나씩 바꾸는 개입 표로 원인을 좁히고, 컴파일러 안의 위치를 찍고, 중복 이슈를 검색해 영문 초안까지 쓴다. GitHub에는 쓰지 않는다. 네 팔이 워크트리 넷에서 돌았고, CPU 둘은 기계 전역 `flock` 임대를 도는 러너(`tools/ref/cpu-measure.sh`)로 직렬화했다. 둘 다 `max`.

| 4라운드 (`max`) | GLM-5.3 | muse |
|---|---|---|
| **CPU gemv** 벽시계 | 34분 | 18분 |
| big M=1, 리드 재측정 (ggml 182 / 184 GB/s) | 202.5 (1.11배) | 203.9 (1.11배) |
| big M=8 (ggml 89 / 93) | **77.5 (0.87배)** | 53.0 (0.57배) |
| expert0 M=1 (ggml 92 / 97) | **83.1 (0.91배)** | 11.9 (0.12배) |
| big M=1, 64스레드(SMT) | **202.7, 유지** | 178.8, −12 % |
| 팔이 찾은 것 | 자기 포트의 6비트 스케일 언팩 버그(`<< 4` 누락)를 ggml `to_float` 덤프와 대조해 잡음 | 레버 표: `MADV_HUGEPAGE` 미발동(`AnonHugePages` 0 kB), 2배 언롤 −25 %, SW 프리페치 −10 % |
| 쓴 토큰(출력, 추론 포함 / 턴) | 456k / 160 | 77k + 추론 48k / 52 |
| **ICE 추적** 벽시계 | 16분 | 12분 |
| 원인 위치 | `dialect-mir/src/const_fold.rs:200`(`MirShlOp::check_fold`), `mir-transforms/src/unroll.rs:387`이 SCCP를 돌려 도달 | 같은 두 프레임, 독립적으로 |
| 재현기 | 트립 1 `while`, 변형 파일 14개; 런타임 트립 수의 `#[unroll(4)]`도 같은 ICE | 트립 8 + 누산기; `i << 1u64`는 통과하고 `i << 1u32`는 ICE |
| 스펙 전제 정정 | `#[unroll]` 아래 `for`는 E0658이 아니라 "induction variable 없음" 경고 | 같음 |
| 쓴 토큰 | 129k / 150 | 34k + 추론 13k / 88 |

CPU는 둘 다 게이트를 넘었고 비율까지 같았다. 결정은 게이트 밖 행에서 났다. M=8, SMT 64스레드, 스레드당 22행 미만인 작은 형상에서 GLM 팔이 크게 앞서 그쪽 커널을 채택했고, muse 팔에서는 레버 표만 가져왔다. 그 표는 plan.md에 적어 둔 2단계 설계 항목 하나를 미확인으로 되돌린다. `madvise(MADV_HUGEPAGE)`는 초 단위 벤치에서 THP가 아예 걸리지 않았다. 두 팔이 같은 비율에서 포화하는 게이트는 게이트일 뿐 비교가 아니라는 것을 이 라운드가 가르쳤다. 그리고 두 엔진 모두 big M=1에서 이 리포에 적힌 DRAM 읽기 147.7 GB/s를 넘었다(ggml 153~188, Rust 202~213). 317 MB는 L3에 들지 않으니 캐시로 설명이 안 되고, 8채널 DDR4-3600의 이론치는 230 근처다. 147.7은 천장이 아니라 그때 그 측정기의 값이었다고 봐야 하고, WKS-35에 재측정 항목을 달았다. 기계 사실 하나를 커널 두 개가 정정했다.

ICE는 두 팔이 서로를 보지 않고 같은 두 줄에 도달했다. Rust는 `u32 << usize` 같은 폭이 다른 시프트를 허용하고 cuda-oxide의 로워링(`convert_shift`)도 그것을 처리하는데, 상수 폴더는 처리하지 않는다. `MirShlOp::check_fold`가 pliron `APInt::shl`을 그대로 부르고 그 함수는 두 폭이 같다고 단언한다. `#[unroll]`이 트리거인 이유는 언롤 패스가 언롤 뒤 함수 전체에 SCCP를 돌리기 때문이고, SCCP의 낙관적 격자가 루프 phi를 초기 상수로 평가하므로 트립 수가 런타임 값이어도 터진다. GLM 팔이 이 마지막 조각(런타임 트립의 부분 언롤도 ICE)을 찍었고, muse 팔은 맨 리터럴 `1`이 `i32`라서 `i << 1`이 이미 폭이 섞인 시프트라는 조각을 찍었다. 변형 14개 전부를 내가 박스에서 다시 컴파일했고 매 행 뒤 기준선이 다시 ICE를 냈다. 중복 검색은 `bitwidth mismatch` 0건, `unroll` 이슈 7건 무관. 초안은 mulle `docs/upstream/cuda-oxide-unroll-ice.md`에 있고 아직 올리지 않았다. 열 팔 중 스펙 전제를 정정한 것은 muse 셋, GLM 하나였고, 채택은 한 팔을 고른 네 번 중 GLM 셋이다. 토큰은 열 팔 모두 GLM이 2.7~6배 더 썼다.

## 이 숫자가 아닌 것

카드는 3090이고 A6000은 내내 다른 학습이 100%로 쥐고 있었다. 증인 블록(양 카드 nvidia-smi, loadavg, io 압력)이 각 타이밍 구간을 앞뒤로 감싼다. 두 팔이 같은 카드를 썼지만 B팔은 타이밍 전에 `compute-apps` 질의로 카드가 비었음을 확인했고 두 번 다 빈 목록이었다. cuTile 트랙(cutile-rs)은 툴킷 13.3 라운드(MUL-6) 전이라 재지 않았다. Q6_K·Q4_K와 A6000 행도 아직이다(MUL-9). M=8은 3라운드에서 1.20배로 닫혔다(MUL-8). 그리고 이 커널은 M=1·8 gemv 하나다. 어텐션도, 라우팅도, 엔진도 아니다.

이슈: MUL-1(닫힘)·MUL-8(닫힘)·MUL-3·MUL-7(gadak `--workspace gdk`), 측정 계획은 WKS-35. 코드: mulle `84b0d98`(GPU 커널 결합은 `d2d0627`).

# 2026-09-20

하루 종일 bloomery(DeepSeek-V2-Lite-Chat Q3_K_M, CPU 전용, ik c10fbbcc)의 잔여 배수를 18.7배에서 시작해 1.13배까지 좁혔다. 속성 하나 빠진 AVX2 커널, 스텝 안에서 매 청크가 잡던 락, 스텝마다 14454번의 할당이 차례로 드러났고, 서브에이전트 병렬 라운드 체제와 게이트 사고(빨간 테스트를 초록으로 삼키던 `||`)도 이날 자리 잡았다.

<a name="a-the-attribute"></a>
## 속성 하나로 73%

MUL-26. `dot_q3k_q8k_avx2`에 `#[target_feature(enable = "avx2")]`가 없어 LLVM이 SSE2로 합법화하고 있었다 — 컴파일도 되고 값도 맞아 무죄로 보였다. `qdot-rate` 벤치(단일 코어) 0.3 → 속성 추가 후 10.8 GB/s(36배). 디코드 5.3698→9.2844 tok/s(+73%, 스텝186.2→107.7ms), T=1 미제 회귀 0.2556→0.6723(2.6배)도 같이 풀렸다. 프리필 4.72→13.97 tok/s, 게이트 전 세트 재핀 0. 같은 임대 ik 82.40±0.68, 남은 배수 8.9배(세션 시작 18.7배). 새 지형의 머리는 Q4_K 스칼라 디퀀트(30.4ms/스텝) — 다음 라운드가 여기를 판다. MUL-24의 프리필 회귀는 느린 커널 조건에서 잰 것이라 재검증 대상으로 남긴다.

원본: `git show c27c3a7:log/2026-09-20-a-the-attribute.md`

<a name="b-the-thermal-section"></a>
## 측정 단위가 먼저 무너졌다

MUL-28. 전 라운드 스윕에서 T=64가 T=32를 +13% 앞선 것을 재판정하러 갔다가, 비교 자체의 결함을 먼저 발견했다. 같은 임대·같은 설정인데 **임대 직후 첫 실행**(T=32 10.7729 tok/s)과 **뒤이은 스윕 행**(9.2975)이 16% 차이났다(T=64도 25%) — 증인(loadavg·PSI·GPU)은 조용했고, 남는 설명은 부스트/열 상태뿐이다. 규율: 같은 임대의 같은 섹션 안에서만 비교한다. 열 통제 비교에서는 32가 10.7729로 64(10.2899)를 다시 앞섰고, 우위(4.5%)가 64의 스프레드(44.8%)보다 작아 **32 유지**. 부수적으로 N=96 긴 실행에서 스텝이 ctx와 함께 선형으로 자람을 봤다(ctx6 103ms → ctx101 285ms, ~1.9ms/토큰) — N=8 창에서는 숨는 기울기라 다음 관문으로 남겼다.

원본: `git show c27c3a7:log/2026-09-20-b-the-thermal-section.md`

<a name="c-flat-across-context"></a>
## ctx가 길어도 평탄하다

MUL-29. `flash_attn_latent`의 (토큰,헤드) 쿼리 행은 완전히 독립이라는 논거로 16헤드 풀에 분할했다. N=8: 9.2844→10.6089 tok/s(+14.2%). N=96: 5.7377→10.0991(1.76배), 스텝 폭 176.8%→21.2%, 기울기 ~1.9→**~0.21 ms/토큰**. 로짓 불변(gate-mt 바이트 동일), 사이트 자체는 73.8→6.9ms/스텝(10.7배). 같은 임대 ik 82.38±0.42(N=8) — 남은 배수 약 7.8–8.3배. "평탄"은 기울기 0이 아니라 0.21ms/토큰, 이 창 밖으로 외삽 금지. 새 머리는 여전히 Q4_K(29.8ms, 30.2%) — 다음 라운드로.

원본: `git show c27c3a7:log/2026-09-20-c-flat-across-context.md`

<a name="d-the-real-pairing"></a>
## 진짜 짝을 찾아 배선한다

MUL-27, 커밋 472180e. Q4_K 스테이지에 ik의 `mul_mat_qX_K_q8_2_X4_T<DequantizerQ4K_AVX2,1>`과 활성값 인코더 `quantize_row_q8_2_x4`를 함께 이식했다. 게이트 0: 인코더 출력 2304바이트 전부 비트 동일 — 손 유도 대신 **인트린 에뮬레이터**(maddubs·unpack·madd를 배열 연산으로 재현)로 구조적 보장, ik 커널 직접 덤프 하네스와 64행 1 ULP. N=8: 10.6089→14.8683 tok/s(1.40배), Q4_K 사이트 29.8→3.6ms(8.3배), 프리필 13.97→17.63. 같은 임대 ik 78.07±0.15 — 남은 배수 5.2배. 재핀 둘, 둘 다 ik 방향: argmax 게이트 31/33→33/33(발산 {14, 24}→{}), l_out 24–26 밴드 2.5e-2→7e-2(로짓 밴드 5e-2는 3.2e-2로 통과). 커널률은 이식을 못 넘었다: `qdot-rate`(단일코어) 14.4 GB/s 대 ik 자신 16.3–17.1 — 헬퍼 분리의 `#[target_feature]` 누락이 24배(0.6 GB/s) 손실을 낸다는 것만 확정, 남은 갭은 코드젠 탓으로 돌렸다. 엔진은 스레드당 4.6 GB/s만 요구하므로(147.7÷32) 러너가 판정한 8.3배가 최종값이다.

원본: `git show c27c3a7:log/2026-09-20-d-the-real-pairing.md`

<a name="e-two-tracks-one-lease"></a>
## 두 트랙을 같은 날에 깐다

MUL-31+32, 병합 9ce9800. 사용자 지시("서브에이전트 단위로, 병렬로")로 커널 라운드 둘을 서브에이전트 둘이 워크트리+`BLOOMERY_REMOTE`로 병렬 수행, 측정·통합은 메인이 직렬로. N=8: 14.8683→24.5228 tok/s(1.65배). Q6_K(짝은 Q4_K와 같은 q8_2_x4의 qY 변형, 부호는 활성값에 접힘): 게이트 전부 통과, 커널률 16.7 GB/s = ik 재측정치의 95%. Q5_0: k=1408이 256의 배수가 아니어서 융합 조건을 타입별 블록 계약(`k_granularity`)으로 갈아야 했고, 재핀 1({14}, l_out 꼬리 2.79e-2→4.99e-3 개선)까지 A/B로 입증했다. 병합 충돌 24곳은 양쪽 살리는 방향으로 풀었고 게이트 전 세트 초록. 같은 임대 ik 82.72±0.69 — 남은 배수 3.4배. 세 번째 에이전트의 읽기전용 부검이 다음 머리(q_nope2, 11.0ms)를 예고했다.

원본: `git show c27c3a7:log/2026-09-20-e-two-tracks-one-lease.md`

<a name="f-the-flat-table"></a>
## 스테이지 표가 평탄해졌다

MUL-33+34, 병합 ddbfb7f. 서브에이전트 병렬 둘째 라운드 — 절반은 **에이전트가 죽은 이야기**다. Q5_1 커널(MUL-34)은 예외 없이 깨끗했다: 커널률 15.0 GB/s = ik의 98–99%, 사이트 2.9→0.23ms(12.6배). q_nope2(MUL-33)는 부검 설계대로 셀 병렬로 착륙, 사이트 11.0→1.31ms(8.4배), gate-mt 바이트 동일. 그런데 이 에이전트가 "10분 무활동"으로 두 번 죽었는데, 원인은 코드가 아니라 **행잉된 게이트**였다: 청크 워크가 열 내 상대 인덱스를 절대 셀 인덱스에 잘못 대입해 두 번째 열부터 512에 갇혔고, gate-mt가 매달리자 조용해 보였을 뿐이다. 교훈: 에이전트가 조용하면 먼저 매달린 프로세스를 찾는다 — 병렬 트랙 원격 명령에 기본 타임아웃을 걸었다(`ef9e579`). N=8: 24.5228→35.9990 tok/s(1.47배), 스테이지 표에 지배 사이트가 사라졌다(최대 Q3_K 배치 6.2ms, 22.1%). 같은 임대 ik 82.78±0.10 — 남은 배수 2.30배. 스텝이 읽는 무게 ≈1.31GB÷28ms≈47GB/s, STREAM(147.7)의 3분의 1 — 다음 축은 사이트별 포화도.

원본: `git show c27c3a7:log/2026-09-20-f-the-flat-table.md`

<a name="g-the-saturation-verdict"></a>
## 사이트 포화도 판정

MUL-35. "47GB/s 중 어디가 대역폭이고 어디가 오케스트레이션인가"에 병렬 3트랙(원장/커널벤치/GPU정찰)이 답했다. 판정: **대역폭 벽은 Q6_K(lm_head) 하나**(119.3 GB/s = STREAM의 81%), flash_attn_latent는 스칼라 벽(0.28 GB/s), 나머지 내적 사이트는 전부 오케스트레이션(풀 평균 가동 9.1워커=28%). 커널 MT 상한 122.6–136.3 GB/s(STREAM의 83–92%)로 커널도 풀도 무죄, 디스패치 세금은 1.5ms/스텝뿐. 법칙: 예측 변수는 rows/call이 아니라 **디스패치당 바이트**(0.5MB→13–23, 172MB→119 GB/s의 단조 함수). 다음 축 셋(flash SIMD ~5.0ms, 직렬 quant 이양 ~3.5ms, q_nope2 커널 ~0.85ms)을 도출했다. GPU 정찰(MUL-30)은 cuda-oxide 경로가 지금 가능함을 확인.

> 정정(같은 날 [j](#j-the-lock-at-the-end-of-every-chunk)): 이 기록의 수치는 청크 끝마다 무조건 수집하는 `Mutex`(스텝당 401번)가 있는 엔진에서 잰 것이다. Q6_K·flash 판정은 그대로 서지만, ~~나머지 내적 사이트 전부는 오케스트레이션(풀 평균 가동 9.1워커 = 28%)~~과 "디스패치당 바이트" 법칙, 그 위의 패킹 기대 이득은 락의 비용을 얇은 패킹 탓으로 돌린 것이라 재측정 대상 — 락 제거 후 같은 사이트들은 85–91 GB/s다.

원본: `git show c27c3a7:log/2026-09-20-g-the-saturation-verdict.md`

<a name="h-three-tracks-one-window"></a>
## flash가 무너지고 직렬이 사라졌다

MUL-36/37/38, 병합 11edf2a. [원장](#g-the-saturation-verdict)이 지목한 후보 셋을 서브에이전트 3트랙이 동시에 쳤다(재핀 없는 트랙 먼저 머지, 재핀 트랙 마지막). **MUL-36 flash SIMD**: kq_dot을 F16C+8레인 FMA로, V는 j축 8레인(재배치 없음이 증명). 발산 {14}→{24} 이동을 `BLOOMERY_FLASH_SIMD=0` 한 실행으로 {14} 복귀 확인. 사이트 6.01→**0.40ms(15배)**. **MUL-37** 직렬 quant를 풀로 이양 + moe gate/up 중복 양자화 제거(312쌍 중 156 제거) — 비트 불변. **MUL-38** q_nope2에 vpsignb 부호 접기 커널(비트 동일) — 벽시간은 활용도가 낮아 −0.07ms만. N=8: 35.9990→37.0222(+2.8%). N=96: 31.7909→39.7097(**+24.9%**), 스프레드 35.3%→6.8%. 같은 임대 ik 83.27±0.10 — 남은 배수 2.098배. 세션 누적 4.16→39.71(9.55배). 새 머리는 batch Q3_K(5.82ms, 24.1%) — 다음은 패킹, 그 다음 레버는 스펙 디코딩(추정 1.5–2.5배).

원본: `git show c27c3a7:log/2026-09-20-h-three-tracks-one-window.md`

<a name="i-the-blind-gate-and-the-diet"></a>
## 빨간 게이트가 0으로 끝나고 있었다

임대 아홉, 커밋 e3bb924…f9778a3. 속도의 날이 아니라 재독의 저녁. 새벽 사고 보강(`ef9e579`)이 justfile 게이트 13개에 붙인 `… || echo "타임아웃"`이 타임아웃뿐 아니라 `cargo test`의 실패까지 삼켜, 빨간 테스트도 rc0으로 끝났다 — 그 사이 4시간37분 동안 MUL-36/37/38이 이 레시피로 판정받아 병합됐다. `tools/gate.sh`로 종료코드 소유자를 하나로 모으고 `tools/check-recipes.sh`(FAIL-first, 13줄 검출)를 `just gate`의 첫 의존으로 걸었다. 새 러너로 13게이트를 다시 돌려 병합분이 전부 rc0이었음을 확인 — 운이 좋았을 뿐 구조가 막은 게 아니다. 하드닝에서 부호 접기 커널의 활성값 클램프를 −127로(−128은 sign_epi8이 못 뒤집는 값), i16 포화 경계를 주석에서 코드/에러로 옮겼다. **틀렸던 것 셋**: agy의 "주석만 줄여라" 라운드가 comment-only 게이트는 통과한 채 그 수치 계약 주석을 이력째 지웠다(diff를 "무엇이 사라졌나"로 읽어 발견, 손으로 복원 — 이후 agy는 조사 전용). 같은 날 agy는 `#[inline(always)]` 헬퍼의 `target_feature` 상속도 거꾸로 읽어 HIGH로 잘못 올렸다. GLM은 완료 마커 `DONE-ca`를 "완료-ca"로 번역해 완주가 rc72(마커 없음)로 오판됐다. 리팩터(주석 다이어트+뼈대 분할) 후 조용한 박스에서 재니 N=96 37.40 tok/s로 아침의 39.71보다 5.8% 낮았다(ik는 0.6%만 낮음). 레벨1 프로파일(N=32)을 두 바이너리에 사이트별로 나란히 놓으니 ~~차이의 대부분이 한 줄에 있었다~~ 귀속 가능한 차이는 한 줄이었다(정정, 같은 날: 처음 쓴 표는 macOS `sort -n`이 `+` 접두를 못 읽어 역알파벳순으로 떨어지고 `head`가 matmul 사이트를 전부 잘라 정렬이 깨진 앞 열두 줄만 본 것이었다 — 전 사이트로 다시 대조). 세 바이너리 A/B로 좁히니 moe의 인라인 silu 루프를 `ffn::swiglu` 호출로 합치며 `Tensor2::zeros`+`iter_mut`가 `map().collect()`로 바뀐 것이 원인, 되돌리자(`d9626c4`) 네 바퀴 평균 37.66→39.14(+3.9%, 전승). ~~표의 나머지 네 줄(합 +14.7 ms/32스텝)은 닫지 않았다.~~ 닫지 않은 것: 디코드 쪽 swiglu 남은 절반과, 하드닝 기준보다 높게 남은 moe_trace·wv_b_heads·moe_expert_io(합 +13 ms/32스텝) — 패킹 라운드(MUL-39)로 넘겼다.

원본: `git show c27c3a7:log/2026-09-20-i-the-blind-gate-and-the-diet.md`

<a name="j-the-lock-at-the-end-of-every-chunk"></a>
## 청크 끝마다 락이 하나 있었다

커밋 54ccaab…2b2fdb6. "ik를 따라가기만 하는가, 넘을 지렛대가 있는가"를 재려던 실험 둘은 빈손이었다: n-gram 추측 디코딩은 전문가 공유가 얕아(k=4에서 28%) 수용률이 손익분기(스텝당 2토큰, k=4)에 못 미쳤고, CPU 거버너(`performance` vs 기본)도 클럭만 올릴 뿐 속도는 안 움직였다. 셋째 실험(스레드 스윕)이 하루치 작업보다 큰 것을 찾았다: 8→32스레드로 늘려도 39.7→39.7로 **평평**했다(ik는 8→16에서 60→81). 폭 제한 디스패치를 실험하다 "전폭" 행이 직전보다 40% 빠른 53.7을 찍은 것을 단서로, `run_row_pool`의 청크 클로저 끝마다 프로파일이 꺼져 있어도 잡히던 `collected.lock()...push(chunk)`를 발견했다 — 32워커가 거의 동시에 끝나 같은 락에 스텝당 401번 줄을 섰다. 프로파일/에러 청크만 수집하게 고치자(`4131359`) 네 바퀴 평균 37.79→**54.40**(+44%), 헤드라인 N=96 54.56 tok/s, 같은 임대 ik 82.80±0.02 — **남은 배수 1.52배**(아침 2.10배). 레벨1 프로파일 자체도 락 있는 채로 재고 있었음을 발견해 락 없는 슬롯(`ChunkSlots`)으로 바꾸니 세금 3.5–6%. perf로 페이지 폴트가 malloc 아닌 게으른 mmap임을 확인, `MAP_POPULATE` 프리폴트로 +3.5%(같은 임대 ik ~~tg96~~ tg32 82.90±0.71 — 러너는 처음부터 `-n 32`, 우리 쪽만 N=96이었다는 정정 — **1.48배**). 활성값 양자화의 `round_ties_even`(`rintf`)을 `nearest_int`로 바꿔 +8.3%(네 바퀴 전승) — 헤드라인 N=96 **61.48 tok/s**(16.3ms/토큰), ik 82.88±0.52 — **남은 배수 1.35배**. 이 락은 matmul이 행 병렬이 된 첫 커밋(868485b, 09-19)부터 있었다 — 그 뒤 모든 멀티스레드 기록은 락 아래서 잰 것이고, 고치지 않는다.

원본: `git show c27c3a7:log/2026-09-20-j-the-lock-at-the-end-of-every-chunk.md`

<a name="k-the-main-thread-is-the-step"></a>
## 메인 스레드가 곧 스텝이다

[j](#j-the-lock-at-the-end-of-every-chunk)의 61.48 tok/s에서 이어지는 09-20 밤. 입력을 내용으로 비교해 중복 양자화를 줄이는 시도는 오히려 느렸다(62.83→61.03, 되돌림). ik를 perf로 봤더니 "내적이 이상치"라는 전제가 틀렸다 — 커널 스레드시간은 이미 같은 선(우리 266ms 대 ik 추정 290ms)이고, 다른 것은 워커 이용률(ik 75% 대 우리 51%)이었다: **워커가 노는 시간 = 메인 스레드가 디스패치 사이 혼자 일하는 시간 = 스텝 길이.** 메인 스레드 표본의 절반이 디스패치 자기 몫+장벽(52.7%), 나머지 절반의 절반이 할당·0채우기다. 할당 카운팅 게이트(`gate-alloc`)로 스텝당 **14454번, 24.5MB**를 셌다 — 대부분 토큰과 무관한, 로드 시점에 해도 될 조회(텐서 이름 `format!`, `Gguf::data`의 `ok_or`가 매번 이름을 복제하는 것 등)를 스텝마다 반복하고 있었다. 프리리스트+`scratch`(0 안 채움)로 14454→11463: **속도는 안 움직였다**(할당 1/5 제거는 안 보임). SwiGLU를 8레인 AVX2로 옮겨 +4.0%(발산 {24}→{}), `-C target-cpu=znver3`(qdot 밖 스칼라 루프가 전부 SSE2였다)로 +4.8%. 위임 라운드(GLM-5.3, effort max, 1시간1분)가 토큰-무관 조회 전부를 `Derived::new`의 스텝 플랜으로 옮겨 `gate-alloc` **11463→1291, 16.3MB→0.32MB**(워커 스레드 0회): +9.6% — **이번엔 할당 9/10 제거가 보였다**(1/5 제거는 안 보였던 것과 대비). 밤새 사슬은 같은 임대 상대값으로 61.5→64.8→67.8→**72.5**, 헤드라인 N=96 72.30 tok/s(13.8ms/토큰), ik tg32 81.79±1.02 — **남은 배수 1.13배**(아침 2.10배, 저녁 1.35배). ~~헤드라인 러너를 한 번 돌렸지만 조용한 기계가 아니었다 — bloomery 65.40 tok/s, ik tg32 82.63, 잔여 1.26배.~~ 위임 라운드가 같은 박스에서 빌드·게이트를 돌리던 중의 값이라(증인 loadavg 2.7→5.1) 위의 것으로 대체했다. ik 대조가 러너마다 `-n 32`로 고정돼 있었는데 우리 쪽만 N=96이었다는 정정도 이날 났다(`-n "$N"`로 통일). 남은 후보는 호출당 12–33µs 앞뒤 비용과 디스패치 묶기 — 커널은 이미 ik와 스레드시간이 같아 건드릴 이유가 없다.

원본: `git show c27c3a7:log/2026-09-20-k-the-main-thread-is-the-step.md`

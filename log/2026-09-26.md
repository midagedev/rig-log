# 2026-09-26

<a name="v41-cardroute-queue"></a>
## V4.1 프리필 카드 route의 런치 큐 판정(시팅 15) — 큐 깊이 ≈ 1,120 항목 모형이 맞았다

질문: 시팅 14(#v41-prefill-s14 in 2026-09-25)에서 층·배치당 카드 route 30.4 ms 중 호스트 enqueue 18.8 ms는 무엇인가. 설계 라운드(bloomery `docs/research/cardroute-design-report.md`)는 드라이버 런치 큐가 차서 호스트가 막힌 시간으로 보고 큐 깊이 Q를 1,080–1,170 항목으로 맞췄다[유도]. 가설 셋을 P = 384 한 번으로 가른다: Q 모형, 항목 수와 무관한 고정 꼬리 ≈ 650, 큐 없음.

한 일: bloomery main `de3a086`(`generate_ds41` 임대 밖 사전 빌드), A6000(300 W, 2100 MHz), 배치 (a), 뜨거운 목록 384, lcg 프롬프트, `BLOOMERY_STEP_STATS=1`(카드 이벤트), CARD_EXPERTS 두 팔 1바퀴, 러너 `tools/ref/depth-ds41.sh`. 임대 19:10:28–19:13:28 UTC.

| 팔 | enqueue | wait | card_out | union | 증인 |
|---|---:|---:|---:|---:|---|
| 기본(expert) | 9.02 | 14.07 | 22.50 | 58.35 | `[cpu-busy 1/1]` |
| `slot` | 10.85 | 12.25 | 22.52 | 55.44 | 깨끗함 |
| 예측, Q 모형 [유도] | 8.5–9.9 / 10.1–11.5 | 12.8–14.2 / 11.2–12.6 | 22.4–23.0 | | |
| 예측, 고정 꼬리 ≈ 650 [유도] | 10.7–11.0 | 11.7–12.0 | | | |
| 예측, 큐 없음 [유도] | 5.2–5.4 | 17.0–17.8 | | | |

(층·배치당 ms. 예측 칸의 앞이 expert, 뒤가 slot.) 팔 차이 slot − expert는 enqueue +1.83, wait −1.82로 Q 모형의 +1.6 / −1.6과 맞고, 고정 꼬리(0 / 0)와 큐 없음(+0.3 / −0.3)과는 안 맞는다. 두 팔 모두 Q 모형 대역 안이다. 실측에서 역산한 Q는 expert 1,128, slot 1,116 항목이다[유도: route 항목 수를 코드에서 세고 wait = (Q − shadow 항목) × 항목당 카드 시간].

조건 하나. 기본 팔 행에 다른 트랙의 빌드가 겹쳤다(`[cpu-busy 1/1]`). 판별에 쓴 wait는 큐가 차 있는 동안 카드 속도로 정해지므로 호스트가 카드보다 빠른 한 흔들리지 않는다 — 두 팔의 card_out이 22.50·22.52로 같고, 경합은 호스트 계산인 union(+5 %)에만 보였다. 그래서 이 행의 pp(113.8 대 slot 120.1)는 쓰지 않는다.

판명된 것: 호스트 enqueue의 대부분은 발행이 아니라 큐가 빌 때까지의 대기다. 한 배치가 한꺼번에 큐에 넣는 항목(route ≈ 1,633 + shadow)을 Q 아래로 내리면 한 스레드로도 다음 배치의 route를 union 뒤에 숨길 수 있다 — 설계의 B1(프로젝션 넷을 청크 루프 밖 배치 폭으로)이 그 항목을 949(expert)·1,064(slot)로 내린다[유도].

<a name="q3gemma-ab"></a>
## Qwen3 GEMM 레버 A(Q4_K 가중치를 한 스텝 앞서 공유 메모리로) — 웨이브프런트는 39 % 줄었고 사이클은 그대로다: GEMM은 L1TEX가 아니라 지연에 묶여 있었다

질문. 레버 A는 `gemm_q4k`가 스텝 머리에서 LDG.32로 동기 적재하던 Q4_K 가중치를, 활성값과 같은 commit 그룹의 `cp.async`로 한 스텝 앞서 공유 메모리에 올린다. 예측은 L1TEX 데이터 파이프가 벽이라는 읽기(#q3gemma-base-prof in 2026-09-25) 위에 섰다: 블록-스텝당 웨이브프런트 2,496 → 1,840, 벤치 MoE T = 4096 70.5 → 97.7 TOPS(80–110), pp4096 7,490 → 8,730(7,970–9,170), pp512 6,402 → 8,070(6,850–8,350)[모두 유도].

한 일. bloomery `7bc317c`(main `a27e542` + 레버 A, 출력은 base와 비트 동일 — `gate_gemm` digest 328/328)와 base `a27e542`를 A6000(300 W)에서 쟀다. `depth-gpu-qwen3moe` 3바퀴(A, `bin:` base, llama.cpp `53ed051ce` 기본과 `-ub 4096 -b 4096`, mistral.rs `d5ae0f18f`), 두 트리의 `bench-gpu-kernels`(연달아 한 번씩, 교차 아님), A의 nsys P = 4096과 ncu `gemm_q4k`. 임대 19:02–19:09 UTC, 27행 모두 `[other-busy]` 0.

| pp tok/s @ n = 0, A6000 | P = 512 | P = 4096 |
|---|---:|---:|
| A | 6,402 | 7,446 |
| base | 6,391 | 7,467 |
| A / base(바퀴별 비의 평균, 95 %) | 1.002 ± 0.018 | 0.997 ± 0.005 |
| llama.cpp 기본(ub 512) / ub 4096 | 4,281 / — | 4,199 / 6,882 |
| mistral.rs(flash-attn 없는 빌드, #mrs-noflash) | 3,466 | 1,320 |

| ncu `gemm_q4k` MoE T = 4096, 16런치 중앙값 | base | A | 예측 A |
|---|---:|---:|---:|
| 경과 사이클 | 2,190,925 | 2,193,264 | ≈ 1.58 M[유도] |
| LSU 웨이브프런트/런치(그중 공유 메모리) | 1.278e8 (6.67e7) | 7.76e7 (7.37e7) | 0.938e8 |
| L1TEX / issue-active / tensor | 70.6 / 54.4 / 28.2 % | 42.9 / 61.9 / 28.2 % | |
| 뱅크 충돌/런치 | 1.30e6 | 2.09e6 | 증가 없음 |

벤치 MoE T = 4096은 69.82 → 70.53 TOPS, T = 512는 45.68 → 47.25, dense T = 4096은 73.17 → 73.20이다. nsys 창에서 A의 `gemm_q4k`는 288런치 258.7 ms로 base 255.4 ms와 같다.

판명된 것. 레버는 설계대로 작동했다. 가중치의 전역 적재가 사라져 웨이브프런트가 예측보다 더 줄었고 L1TEX는 42.9 %로 내려갔다. 그런데 경과 사이클이 0.11 % 안에서 같고, 벤치와 pp도 움직이지 않았다. ncu 사이클, 벤치 TOPS, pp 비가 같은 말을 하니 계측 탓이 아니다. base의 블록-스텝은 SM 사이클로 ≈ 3,530이고[유도: 경과 사이클 ÷ SM당 블록-스텝 620], 그 안에서 L1TEX 수요는 ≈ 2,450, tensor ≈ 1,000, 스케줄러당 issue ≈ 1,920이다 — 스텝을 채우는 유닛이 없다. 스텝의 길이는 워프의 의존 사슬(멈춤 1·2위가 wait와 short_scoreboard)이 SM당 16워프로 다 가려지지 않은 지연이다. A가 issue를 14 % 늘리고도 사이클이 그대로인 것이 그 증거다: 늘어난 명령은 비어 있던 발행 슬롯에 들어갔다.

틀렸던 것. 설계와 기준 프로파일이 "최상위 유닛 = 벽"으로 읽었다. 70.6 %는 포화가 아니었다. 물어야 했던 것은 수요를 줄였을 때 사이클이 따라 주는가였다. 같은 읽기 위에 세운 다음 레버(활성값 LDS를 줄이는 A3+C(i), A-Q6K)는 같은 이유로 0일 공산이 커서 열지 않는다. 종이와 실측이 하나 더 어긋났다: 라운드는 staging 쓰기와 가중치 LDS의 뱅크 충돌을 0으로 셌는데 런치당 0.78e6 늘었다(웨이브프런트의 1 %라 결과는 못 바꾼다).

처분. 레버 A는 착륙하지 않는다. 블록당 공유 메모리를 21,504 → 41,984 B로 늘려 SM당 블록 상한을 공유 메모리 기준 4 → 2로 줄이는데, 지연을 가릴 후보 하나(레지스터를 줄여 SM당 3–4블록)가 바로 그 여유를 쓰기 때문이다. 러너 두 모드(`ncu-gpu-gemm`, `nsys-gpu-qwen3moe-prefill`)와 `gate_gemm`의 계약 전사 조항만 착륙한다. 레버 A 코드는 bloomery 브랜치 `q3gemma-leverA`(`7bc317c`)에 남긴다.

<a name="mrs-noflash"></a>
## mistral.rs 프리필 행의 빌드 조건 — flash-attn 없이 빌드한 바이너리였다: P = 4096의 토큰당 2.6배 둔화는 eager attention 몫[유도]

질문. 공개 표의 mistral.rs 프리필은 P = 512에서 3,471, P = 4096에서 1,321 tok/s다(#q3ubatch-ab in 2026-09-25). 토큰당으로 0.288 → 0.757 ms, 2.6배 느려진다. 같은 MMQ 계열인 llama.cpp는 4,318 → 4,205로 거의 그대로다. 결함이면 업스트림 이슈감이고, 아니면 표가 무엇을 쟀는지 밝혀야 한다.

한 일. 조사 라운드(bloomery `mrs-pp4096`, opus, 박스 실행 없음)가 박스의 트리 `/home/user/mistral.rs`(`d5ae0f18f`, 0.9.3)를 읽었다. 리드가 박스에서 직접 다시 읽은 사실: `target/release/.fingerprint/mistralrs-cli-*/bin-mistralrs.json`의 features는 `["code-execution", "cuda", "default"]`이고, fingerprint 디렉터리에 flash 산출물이 하나도 없다. 즉 `--features cuda`로만 빌드됐다.

판명된 것. flash-attn 피처가 없으면 `using_flash_attn()`이 컴파일 타임에 false라(`mistralrs-core/src/utils/mod.rs:308-310`), 인과 마스크가 호스트에서 만든 P × P 텐서가 되고(`layers_masker.rs:227`) 프롬프트 attention은 `naive_sdpa`로 간다(`attention/mod.rs:486`). 이 경로는 query 1,024행씩 32 × P × P 점수를 전부 만들고, 그 위를 일곱 번 지난다(스케일, 마스크 합, f32 변환, softmax 세 번, bf16 변환). P = 4096에서 층당 약 24.7 GB, forward당 약 1.19 TB다[유도]. 선형 항이 토큰당 그대로라고 두면 P = 4096의 attention은 2,019–2,089 ms여야 하고, 이는 그 바이트를 567–587 GB/s로 읽는 속도다 — 격차 0.469 ms/토큰이 전부 이 항에 들어간다[유도]. 런타임 플래그로는 이 경로를 못 바꾼다. mistral.rs의 결함은 아니다: 업스트림 sm_86 릴리스 바이너리, `install.sh`, 문서가 셋 다 Ampere에서 `flash-attn`을 켠다(`release.yml:321`, `install.sh:367-369`, `cargo-features.md:25`). master `2370966bb`의 해당 파일 13개는 우리 트리와 같다.

틀렸던 것. 우리 표가 이 행을 "mistral.rs"로만 적었다. 이 엔진의 권장 빌드가 아니므로 P = 4096의 5.6배 같은 비는 공개 비교로 쓰지 않는다. 디코드 행(`mrs:<D>`)은 이 모델에서 flash 피처와 무관한 paged attention 경로라 영향이 없다(`paged_attention/plan.rs:697`)[코드 읽기]. 다음: flash-attn 빌드를 따로 두고(지금 바이너리는 사본으로 남겨 같은 임대 A/B) 다시 잰다 — 예측 pp512 3,860–4,470, pp4096 3,630–4,190(선형 항이 llama.cpp처럼 줄면 약 6,300까지)[유도]. 빌드가 30분을 넘을 수 있어 사용자 승인 항목이다. 업스트림 이슈는 내지 않는다.

<a name="q3router-ab"></a>
## Qwen3 ubatch 라우터 logits를 레지스터 타일로(q3router) — 런치당 1.19 → 0.281 ms, pp4096 +7.9 %·pp512 +6.8 %

질문. P = 4096 프롬프트에서 `qwen3moe_router_logits`는 48런치 57.2 ms, 런치당 1.19 ms였다(#q3gemma-base-prof in 2026-09-25). 옛 커널은 워프 하나가 전문가 행 하나를 8토큰 청크에 대해 걷는다. 라운드의 분해로는 어느 유닛도 차지 않은 지연 바운드였다(FFMA ≈ 5 %, issue ≈ 25 %, L1TEX ≈ 22 %, DRAM 4 %; 반복마다 L2에 안 들어가는 토큰 열의 DRAM 왕복 하나 — 19.5웨이브 × 64반복 × 0.93 µs ≈ 1.16 ms[유도]). 새 커널은 512스레드 블록이 토큰 32 × 전문가 행 32를 맡고, 워프마다 8행 × 8토큰 타일(레인당 합 64개)을 들고, 행·토큰 값을 `cp.async` 3단으로 공유 메모리에 올린다. 각 (행, 토큰)의 레인 합 순서와 butterfly가 퓨즈드 디코드 라우터와 같아 비트가 같다(`gate_gemm` 12개 T 전부). 예측은 런치당 0.12–0.20 ms, pp4096 8,200–8,270, pp512 6,790–7,120이었다[유도].

한 일. bloomery q3router 트리(main `319e124` + 라우터 커널, `1071194`)와 base `319e124`를 A6000(300 W)에서 쟀다. `depth-gpu-qwen3moe` 3바퀴(A, `bin:` base, llama.cpp `53ed051ce` 기본과 `-ub 4096 -b 4096`, mistral.rs `d5ae0f18f` — flash-attn 없는 빌드, #mrs-noflash), 그리고 A의 nsys P = 4096. 임대 20:05–20:11 UTC, 27행 모두 `[other-busy]` 0.

| pp tok/s @ n = 0, A6000 | P = 512 | P = 4096 |
|---|---:|---:|
| A(q3router) | **6,815** | **8,024** |
| base | 6,383 | 7,439 |
| A / base(바퀴별 비의 평균, 95 %) | **1.068 ± 0.012** | **1.079 ± 0.013** |
| llama.cpp 기본(ub 512) / ub 4096 | 4,267 / — | 4,191 / 6,872 |
| A / llama.cpp 기본 / ub 4096 | 1.60 / — | 1.91 / **1.168 ± 0.007** |
| mistral.rs(flash-attn 없는 빌드) | 3,460 | 1,317 |

디코드는 그대로다(깊이 512에서 1.002 ± 0.009, 4096에서 0.999 ± 0.004). nsys 창(P = 4096)은 511.1 ms이고, `qwen3moe_router_logits`는 48런치 13.5 ms, 런치당 **0.281 ms**다. 나머지 커널은 base 창과 같다(`gemm_q4k` 258.7, `gqa_prefill_flash` 89.0, `gemm_q6k` 43.4 ms).

판명된 것. 커널은 4.2배 빨라졌고 pp512는 예측 밴드 안이다. 런치당 시간은 밴드보다 1.4–2.3배 느리고, 그래서 pp4096은 밴드보다 약 2 % 낮다. 틀린 항은 발행 효율이다. 라운드는 발행 효율을 70–100 %로 잡았다. 60.1 M 워프 명령을 0.281 ms에 내면 스케줄러당 발행은 약 40 %다[유도, 클럭 1.5–1.7 GHz]. 그 안에는 웨이브 꼬리가 있다. 127 레지스터 × 512스레드라 SM당 1블록이고, 512블록이 84 SM에 7웨이브로 돈다(이상적이면 6.1). 나머지는 블록 안 16워프의 지연이다(단계마다 배리어, LDS 뒤 FFMA). 라우터는 이제 창의 2.6 %라, 남은 레버(루프 전개로 주소 명령 줄이기, 레지스터를 줄여 SM당 2블록)는 크기가 작다.

<a name="compact-unevictable-ab"></a>
## `vm.compact_unevictable_allowed` 1 대 0 — 네 팔 모두 녹색이라 재현되지 않았다, 판정 없음

질문. bloomery-ee의 q3fix2 착륙 묶음에서 `gate-gpu-ds41-faults`가 스텝당 minor fault 33을 읽었다. 이 게이트는 호스트 집합을 `BLOOMERY_HOST_LOCK=1`로 mlock하고 스텝당 minor fault를 2 이하로 핀한다. 그때 다른 레인은 A6000에 모델을 올리고 있었고, 조용한 박스에서는 0이었다. 가설은 이 박스의 `vm.compact_unevictable_allowed=1`이 동시 적재 중 compaction에게 mlock된 페이지를 옮기도록 허락한다는 것이었다. 예측은 두 가지였다. 설정 1인 팔은 빨강이고 `pgmigrate_success` 델타가 크다. 설정 0인 팔은 녹색이다. 설정 1이 녹색이면 판정이 없고, 설정 0이 빨강이면 가설을 버린다.

한 일. 사용자 승인(10분, 끝나면 복원)으로 임대를 잡고 1 0 0 1 순서로 네 팔을 돌렸다(20:59–21:10 UTC). 팔마다 3090에서 `gate-gpu-ds41-faults`를 돌렸다(gate 배치, 카드 전문가 1,146개, 호스트 집합 mlock). 그보다 5초 먼저 A6000에서 `gate-gpu-qwen3moe-e2e`를 띄웠다. 이 게이트는 19.0–19.9 GB 적재를 네 번 하고, 한 번에 17.2–25.5 s가 걸린다. 팔 앞뒤로는 `/proc/vmstat`의 이주·compaction 카운터를 읽었다. 첫 팔은 bloomery fixup6 착륙 묶음 직후였고, 그 묶음에는 V4.1 적재 게이트가 들어 있었다.

| 팔 | 설정 | pgmigrate_success | pgmigrate_fail | compact_stall | compact_isolated | compact_daemon_wake | 스텝당 minflt 최대(핀 2) |
|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1 | 1,026,182 | 165,505 | 2 | 2,218,933 | 46 | 2 |
| 2 | 0 | 67,164 | 295,371 | 4 | 429,901 | 4 | 2 |
| 3 | 0 | 0 | 0 | 0 | 0 | 0 | 2 |
| 4 | 1 | 0 | 0 | 0 | 0 | 0 | 2 |

판명된 것. 네 팔 모두 녹색이었다(주 폴트 0, 스텝당 minor 최대 2). compaction은 앞의 두 팔에서만 돌았다. 착륙 묶음이 남긴 메모리 상태가 정리되던 구간이다. 설정 1인 팔 1은 1.03 M 페이지를 옮겼다(팔 2의 15배). `unevictable_pgs_stranded` 502,571도 이 팔에만 있었다. 그래도 스텝 창에는 폴트가 없었다. 팔 3과 4는 compaction이 0이어서 설정이 시험되지 않았다. 그러니 이 A/B는 가설을 확인하지도 기각하지도 못한다.

틀렸던 것. 설계가 "스텝 창 안의 compaction"을 보장하지 못했다. 동시 적재는 Qwen3 19 GB라 V4.1 적재보다 훨씬 작다. 적재가 게이트의 스텝 창(적재 뒤 32스텝)과 겹쳤는지 보여 줄 시각도 로그에 없다. 원래 빨강의 조건은 다른 레인의 적재가 스텝 창과 겹친 것이다. 이것을 다시 만들려면 게이트의 첫 스텝 줄을 보고 나서 적재를 띄워 창을 맞춰야 한다. 묶음 안의 빨강은 이미 구조로 막혀 있다. bloomery `e4ae993`(runnerlanes)의 solo 등급이 `gate-gpu-ds41-faults`를 두 레인이 끝난 뒤 혼자 돌린다. 설정은 1로 되돌렸다(복원 확인 출력 `1`).

<a name="v41-prefill-nsys"></a>
## V4.1 프롬프트 배치를 nsys·ncu로 — route 잔여 10.9 ms는 커널 시간이고 그중 10.0 ms가 프로젝션이다, 셋은 지연에 묶였고 정수 파이프는 아니다

질문. 시팅 14에서 카드 route는 층·배치당 30.4 ms였다. 종이 분해(bloomery `cardroute-design`)는 그중 커널 19.5 ms(16.3–23.4)와 런치 빈틈 1.4–3.6 ms만 설명했고, 10.9 ms가 남았다. 남은 몫이 커널 시간인지 빈틈인지를 가려야 했다. 또 m = 8 프로젝션이 정수 파이프에 묶였는지(B2의 개시 조건)도 봐야 했다.

한 일. bloomery `afe86d5`(nsyspp)의 두 폼으로 A6000(300 W, 2100 MHz)을 쟀다. 조건은 공개 `Q3_K_M`, 배치 (a), 뜨거운 목록 384, lcg P 512, CED 켬이다. nsys 프리필 폼은 21:11:02 UTC에 임대를 잡았고, ncu ds41pp 폼은 21:15:08–21:18:51 UTC에 돌았다. ncu는 3층의 가운데 청크를 대상으로 클럭을 고정(1.40 GHz)하고 패스마다 캐시를 비웠다. 사전 증인에 `rustc 99.6 %`가 찍혔다. 리드의 정적 검사 묶음이 시팅의 첫 약 44초와 겹친 것이다. 적재가 74.6 s라 프롬프트는 약 21:12:19에 시작했고, 그때 rustc는 이미 없었다. 프로파일러 아래 stat 줄(enqueue 19.34, wait 11.76 ms)은 시팅 15의 비프로파일 값(18.8, 11.7)과 3 % 안이다.

| 층·배치 평균, ms | 종이 | nsys | 차 |
|---|---:|---:|---:|
| 프로젝션 넷(qkv·q_b·wo_a·wo_b) | 9.6 | **19.64** | +10.04 |
| attention | 2.9 | 4.16 | +1.26 |
| small | 2.2 | 3.88 | +1.68 |
| route tail + engram | 1.9 | 2.27 | +0.37 |
| 런치 빈틈(카드 유휴) | 2.2 | 1.25 | −0.95 |
| fork 겹침(종이에 없던 항) | 0 | −1.67 | −1.67 |
| route 창 | 19.4 | **30.29** | +10.93 |

route 창 30.29 ms는 비프로파일 stat 줄 30.35와 −0.2 % 차이다. 3층의 청크 하나에서 프로젝션은 318 µs였다(qkv 28.3, q_b 70.8, wo_a 100.0, wo_b 119.1 µs, nsys). 종이가 쓴 160 µs의 두 배다.

| ncu, 3층 청크 32, m = 8 | grid(웨이브) | 레지스터 | 점유율 | issue-active | long_scoreboard | 가장 바쁜 유닛 |
|---|---:|---:|---:|---:|---:|---|
| qkv `q3k_gemv` | 224 (0.44) | 40 | 44.3 % | 18.9 % | 81.0 % | LSU 19.3 % |
| q_b `q3k_gemv` | 4096 (8.13) | 40 | 92.5 % | 68.8 % | 27.7 % | **LSU 82.4 %** |
| wo_a `ds41_q3k_gemv_heads_mcol` | 1024 (4.06) | 80 | 49.4 % | 32.8 % | 69.3 % | LSU 29.6 % |
| wo_b `q3k_gemv` | 640 (1.27) | 40 | 72.5 % | 30.8 % | 75.2 % | LSU 31.1 % |

판명된 것.
- 잔여는 커널 시간이고, 그 92 %가 프로젝션이다.
- 넷 중 셋(qkv·wo_a·wo_b, 청크 318 µs 중 247 µs)은 지연에 묶여 있다. 그리드가 카드를 채우지 못해서다. qkv는 0.44 웨이브, wo_a는 스레드당 레지스터 80개라 SM당 3블록, wo_b는 1.27 웨이브다.
- 어느 런치도 정수 파이프(ALU 14–48 %, fmaheavy 13–44 %)에 묶여 있지 않다. 그래서 B2는 0을 예측하고 열지 않는다.
- q_b 하나만 LSU에 묶였다. 반복이 3회뿐이라 에필로그 셔플이 워프-반복당 LSU 명령 50개 중 18개를 차지한다.
- 워프-반복당 명령 수는 SASS 모형과 맞았다(283.1 / 325.7 / 276.3, 예측 283.2 / 325.7 / 276.4).
- 큐 모형은 실측 상수로 맞았다. 고원 Q는 in-flight activity 1,068개(event까지 호출 1,326개), t_c는 호출당 18.27 µs다. enqueue는 모형 20.04 대 실측 20.15, wait는 12.12 대 12.01 ms다. 카드 전문가가 없는 0층(N_s 321)의 wait도 모형 16.3, 실측 16.30으로 맞았다.
- B1(프로젝션 넷을 청크 루프 밖 배치 폭으로)의 예측 −2.6…−4.5 ms/층-배치는 프로젝션을 9.6 ms로 두고 나온 값이다. 이 몫이 19.6 ms이고 원인이 채워지지 않은 그리드이므로, bloomery `flowmodel` 라운드에서 다시 유도한다.

틀렸던 것.
- 종이의 청크당 160 µs 입력이 실제의 절반이었다.
- "가장 바쁜 유닛은 issue 70–95 %"라는 판독은 틀렸다. 세 런치는 피크 근처에 간 유닛이 없다.
- XU 128이라던 예측은 0이었다. I2FP는 ALU로 간다.
- 큐의 막힘 기준("50호출 중 45개가 막힘")은 한 번도 발동하지 않았다. 드라이버가 슬롯을 묶음으로 풀어서 약 4호출에 한 번씩 긴 호출이 나온다. 그래서 데이터를 본 뒤 in-flight 고원 기준으로 바꿨다. 막힌 호출 수(예측 약 700, 실측 130)는 틀렸지만 벽시계는 맞았다.
- ncu 대 nsys 지속시간 비는 1.38–1.52로 클럭 비와 맞는다. qkv만 2.15로 풀리지 않았다(캐시 비우기가 후보). 그래서 qkv 행은 비관적이다.
- 옛 디코드 폼(`nsys-ds41.sh`)은 배치 feed 트레이스를 거부하지 않고 틀린 표로 통과시켰다. `afe86d5`에서 고쳤다.
- 같은 라운드의 depth 6 디코드 폼 실행(20:45 UTC)은 다른 라운드와 경합해 스텝이 137 ms였다. 경계 증거로만 쓰고 시간 주장에는 쓰지 않는다.

<a name="uniondispatch-ab"></a>
## V4.1 호스트 union을 층-배치당 다섯 디스패치로(uniondispatch) — pp512 +9.8 %, pp4096 +8.3 %, 둘 다 예측 밴드 안; union 77.1 → 66.7 ms/층-배치

질문. 호스트 union은 P = 512 층-배치 하나를 디스패치 161개로 돌았다. x 양자화가 하나, 호스트 전문가 여덟 개짜리 청크 40개마다 gate/up 행 디스패치·결합·down 행 디스패치·stash 복사, 그리고 합이 하나다. 디스패치마다 깨우기와 조인 꼬리가 하나씩 붙는다. uniondispatch(bloomery `9626c7f`)는 이것을 다섯으로 줄였다. x 양자화, gate/up 행(쌍마다 그 전문가의 열 목록 위로, tile 비용으로 자른 레인과 144행 상한의 훔치기 블록), 결합, down 행(결과를 슬롯 저장소에 바로 쓴다), 합이다. 열 여덟 개 이하의 좁은 호출은 claim으로 둘이다. 계산량과 바이트는 그대로이고 대기만 걷어낸다. 깨우기 156번, 행 디스패치 꼬리 80개 중 78개, stash 48.2 MB, 호출 스레드의 zero-fill 24 MB와 비유한 스캔이다. 라운드의 분해는 층-배치당 Δ = −(1.8…5.0) − 15.7·f ms(f는 블록 안 조인 꼬리 몫 0.1–0.5, 곡선 밖 잔차 약 12 ms가 상한)였고, 시팅 14의 벽시계로 옮기면 pp512 +3.2…+12.9 %, pp4096 +2.7…+12.8 %다[유도]. 이 밴드를 임대 카드(bloomery `docs/cards/uniondispatch-pp.card`, sha256 `fa3ff1cc07be`)에 적고 쟀다. 3바퀴의 자는 ±2.49 %(sd 1.1 %, 시팅 14의 바퀴별 비에서)로 밴드의 가까운 끝보다 좁다.

한 일. A6000(300 W, 2100 MHz), 공개 `Q3_K_M`, 배치 (a)(카드 expert 2,668, 호스트 12,692), 뜨거운 목록 없음(러너 기본값), lcg 프롬프트, n = 96, 3바퀴 회전, 임대 22:58:38–23:16:01 UTC. 팔은 uniondispatch 트리(`generate_ds41` sha256 `3c69d5fcf889`)와 main `af929ae`(`bin:` 팔, sha256 `6c43e200af5e` — 라운드의 base와 크레이트 코드가 같다)이고 P = 512·4096이다. 12행 모두 `[cpu-busy]`·`[other-busy]` 0.

| A6000, 3바퀴 평균 | P = 512 | P = 4096 |
|---|---:|---:|
| uniondispatch pp tok/s | **129.63** [128.25..130.98] | **180.02** [177.68..181.92] |
| main pp tok/s | 118.04 [117.32..118.54] | 166.25 [165.97..166.52] |
| 비(바퀴별 비의 평균, 95 %) | **1.098 ± 0.030** | **1.083 ± 0.035** |
| union ms/층-배치(`stat prefill split`), uniondispatch / main | 66.7 / 77.1 | 68.1 / 77.6 |
| tg96 비 @ 깊이 P | 0.993 ± 0.036 | 1.008 ± 0.008 |

판명된 것. 두 길이 모두 밴드 안이고, 카드의 판정대로 착륙했다. P = 512에서 벽시계는 층-배치당 9.7 ms, union은 10.3 ms 줄었다. union 밖의 항(wait 11.6, enqueue 18.6 ms/층-배치)은 두 팔이 같으니, 합 모형대로 union이 준 만큼만 벽시계가 줄었다. 측정 Δ −10.3 ms를 모형에 넣으면 f = 0.34–0.54[유도]다. 사전 범위의 윗끝이고, 옛 흐름은 행 디스패치마다 블록의 절반쯤을 조인에서 기다렸다는 뜻이다. P = 4096의 벽시계 차 1.885 s를 호출당 union 차 9.45 ms로 나누면 union 호출은 약 200번으로, 카드가 쓴 205–225보다 조금 적다[유도]. 디코드는 계산값 0과 맞는다(두 깊이가 반대 방향이고 둘 다 자 안이다).

조건 둘. 이 임대는 뜨거운 목록 없이 쟀다. 시팅 14의 121.4 / 169.4(#v41-prefill-s14 in 2026-09-25)는 목록(`hotlist-384.txt`)을 켠 값이라 한 표에 놓지 않는다. main의 union이 여기서 77.1 ms로 시팅 14의 72.3보다 긴 것도 목록 유무가 후보다(같은 임대에서 가르지 않았다). 그리고 uniondispatch 팔만 바퀴마다 올랐다(128.25 → 129.67 → 130.98, 177.68 → 180.46 → 181.92; main은 평평하다). 비의 구간이 이 흐름을 덮지만 까닭은 모른다.

<a name="mrs-flash"></a>
## mistral.rs를 flash-attn으로 빌드해 다시 잰 Qwen3 프리필 — pp512 4,835, pp4096 7,890 tok/s로 예측(3,860–4,470 / 3,630–4,190)을 넘었고, P = 4096에서 우리와 1.015 ± 0.009

질문. #mrs-noflash가 밝힌 대로 공개 표의 mistral.rs 프리필은 flash-attn 없는 빌드였다. 업스트림이 권하는 빌드(`--features "cuda flash-attn"`)로 다시 재면 얼마인가. 예측은 마스크가 사라지고 FA2 causal attention이 P = 512에서 1.3–2.2 ms, 4096에서 71–86 ms(피크의 50–60 %)라 두고, 선형 항은 no-flash 분해의 값을 그대로 두어 pp512 3,860–4,470, pp4096 3,630–4,190이었다. 선형 항이 llama.cpp처럼 줄면 약 6,300까지다[유도, bloomery `docs/research/mrs-pp4096-report.md`]. 임대 카드 `docs/cards/mrs-flash-qwen3.card`(sha256 `fd6f2bc69068`, kind baseline)에 적고 쟀다.

한 일. 같은 트리 `/home/user/mistral.rs`(`d5ae0f18f`, 0.9.3)를 `--features "cuda flash-attn"`으로 다시 빌드했다(트리 소유자로, nice 19, nvcc 16잡). 50분 35초가 걸렸고 flash 커널은 53개다. 새 바이너리는 sha256 `8da64b84d5e5`, fingerprint features `["code-execution", "cuda", "default", "flash-attn"]`, `flash_fwd` 심볼 43,012개다. 옛 바이너리는 사본(sha256 `94e6b04338c8`)으로 남겼다. A6000(300 W), Qwen3-30B-A3B-Instruct-2507 `Q4_K_M`, `depth-gpu-qwen3moe` 3바퀴 회전으로 쟀다. 팔은 우리(main `af929ae`의 `generate_qwen3moe`, sha256 `7ef28bb42d39`), llama.cpp `53ed051ce` 기본과 `-ub 4096 -b 4096`, mistral.rs 새 빌드다. 임대 23:16:44–23:19:50 UTC, 21행 모두 `[other-busy]` 0. 같은 임대에 no-flash 팔은 넣지 않았다.

| pp tok/s @ n = 0, A6000 | P = 512 | P = 4096 |
|---|---:|---:|
| bloomery | **6,821** | **8,009** |
| llama.cpp 기본(ub 512) / ub 4096 | 4,285 / — | 4,184 / 6,860 |
| mistral.rs, flash-attn 빌드 | 4,835 | 7,890 |
| mistral.rs, flash-attn 없는 빌드(앞 임대, #q3router-ab) | 3,460 | 1,317 |
| bloomery / llama.cpp 기본 / ub 4096 | 1.592 ± 0.016 / — | 1.914 ± 0.015 / 1.167 ± 0.008 |
| bloomery / mistral.rs flash-attn 빌드 | 1.411 ± 0.005 | **1.015 ± 0.009** |

우리 디코드(tg96)는 깊이 512에서 207.5, 4096에서 175.9 tok/s였다(참조 팔 없음).

판명된 것. flash-attn 빌드는 예측 밴드의 윗끝보다 P = 512에서 8 %, P = 4096에서 88 % 빠르다. 약 6,300이라던 경우보다도 25 % 빠르다. 틀린 항은 선형 항 L이다. no-flash 분해는 L(512)를 123.9–130.4 ms, L(4096)를 575–1,044 ms로 두었다. 그런데 이번 전체 벽시계가 P = 512에서 105.9 ms, 4096에서 519 ms다. attention 몫을 빼면 L(512) ≤ 104.6 ms, L(4096) ≤ 448 ms로 둘 다 분해의 아래끝보다 작다[유도]. no-flash 빌드에서 선형으로 셌던 몫 가운데 P = 512에서 적어도 19 ms, 4096에서 적어도 127 ms가 eager 경로의 비용이었다는 뜻이다[유도]. 두 점 맞춤이 P = 512의 P² 항을 34 ms로 냈을 때(유도는 17–23 ms) 이미 보였던 틈이다. 공개 비교로는 P = 512에서 우리가 1.41배이고, P = 4096에서는 1.5 % 차로 사실상 같다. 우리 표에서 가장 약한 칸이 이 칸이 됐다. llama.cpp의 ub 4096에 대해서는 우리가 1.17배, mistral.rs가 1.15배다.

<a name="v41-fixture-gen"></a>
## V4.1 게이트 fixture 전체 생성 — 68.8 GB를 34.6 s에 썼고 예측 38–40 s보다 빨랐다, 되읽기 검증 통과

질문. bloomery의 fixture 생성기(`v41fixture`, 커밋 `1da4390`)가 V4.1 층 종류마다 한 층씩 9층(원본 층 0·1·2·3·14·20·21·24·25)과 DSpark 드래프트를 실제 모양 그대로, 가중치는 유도한 스케일의 난수로 쓴다. 설계가 유도한 크기와 시간이 실제로 맞는가. 목적은 V4.1 엔진 게이트를 RAM에 통째로 들어가는 작은 파일에서 돌려 착륙 묶음을 줄이는 것이다.

한 일. 공개 `Q3_K_M` 첫 샤드와 DSpark 드래프트의 헤더를 원본으로, `/models/v41-fixture/`에 타깃 4샤드와 `draft/`에 드래프트 1개를 썼다. 기계 임대는 측정 없는 전용 임대(`lease-hold.sh`, 카드 `docs/cards/fixture-gen.card`)로 03:08:09–03:08:47 UTC에 잡았고, 같은 임대 안에서 `v41fixture verify`로 되읽었다. 두 카드 모두 컴퓨트 프로세스가 없었다.

| 항목 | 유도 | 측정 |
|---|---|---|
| 파일 바이트(타깃 + 드래프트) | 60,854,838,208 + 7,972,960,256 = 68,827,798,464 | 68,827,798,464(샤드 넷 15,169,411,616 · 15,704,061,600 · 17,000,550,016 · 12,980,814,976, 드래프트 7,972,960,256) |
| 난수 생성 | 2.4 s | 1.43 s |
| 페이지 캐시 쓰기 | 18–20 s | 21.49 s |
| NVMe 플러시 | 16–17 s | 11.49 s |
| 생성 합계 | 38–40 s | **34.59 s** |
| 피크 RSS | 3.19 GB | 3,165,064 KiB |
| 임대 전체(생성 + 검증) | — | 38 s |

검증은 타깃 텐서 258개(블록 15,517,359개)와 드래프트 텐서 78개(블록 10,353,122개)를 부분집합 아닌 전체로 읽었고, 텐서마다 rms가 설계 σ에 맞았다(예: `markov_w1.weight` bf16 rms 6.2498e-2, σ 6.25e-2).

판명된 것. 바이트는 계획과 한 바이트도 다르지 않다. 시간 합계는 예측보다 11 % 빨랐다. 쓰기는 예측보다 느리고 플러시는 빨라서, 둘의 합(33.0 s)이 예측 34–37 s의 아래였다 — 페이지 캐시 쓰기 중에 이미 일부가 디스크로 나갔다고 읽힌다[유도, 쓰기 중 dirty 페이지는 재지 않았다]. 임대 뒤 페이지 캐시는 245.3 → 252.5 GB였다. 69 GB를 쓰면서 원본 파일의 페이지 일부가 밀려났으므로, 다음 실파일 묶음은 그만큼 콜드로 읽는다.

<a name="b1-pp-ab"></a>
## V4.1 프롬프트 배치의 어텐션 프로젝션을 배치 폭으로(B1) — pp512 +10.8 %, pp4096 +10.2 %, 카드 route 29.7 → 19.6 ms/층-배치

질문. bloomery `2f45a79`(ds41proj, B1)는 프롬프트 배치의 어텐션 프로젝션 넷을 8토큰 청크마다가 아니라 128토큰 부분 블록마다 한 번씩 돌린다. 비트는 청크 경로와 같다. 예측은 뜨거운 목록 없는 uniondispatch 팔 위에서 pp512 +5.7…+10.3 %, pp4096 +5.6…+10.0 %였다[유도; 카드 `docs/cards/b1-pp.card`].

한 일. A6000(300 W, 2100 MHz), 공개 `Q3_K_M`, 배치 (a)(카드 expert 2,668, 호스트 12,692), 뜨거운 목록 없음, lcg 프롬프트, n = 96, `BLOOMERY_STEP_STATS=1`. 팔은 main `0bcee2c`의 `generate_ds41`(sha256 `f0dddb6f7667`)과 B1 직전 트리 `74fe84c`의 빌드(`bin:` 팔, `791dec6236ff`)다. 첫 임대(03:11:39–03:28:14 UTC, 3바퀴)의 1바퀴는 옆 세션의 `cargo check`·clippy가 겹쳐 두 행에 `[cpu-busy]`가 찍혀 버렸다. 그래서 같은 카드로 2바퀴를 더 쟀다(03:29:06–03:40:10 UTC, 표시 0). 아래는 깨끗한 네 바퀴(첫 임대 2·3바퀴, 둘째 임대 1·2바퀴)의 바퀴별 비율이다.

| | 바퀴 비율 | B1 / base |
|---|---|---:|
| pp512 | 1.1002 · 1.1032 · 1.1122 · 1.1153 | **1.108 ± 0.011** |
| pp4096 | 1.0999 · 1.0955 · 1.1119 · 1.1006 | **1.102 ± 0.011** |
| 디코드 tg96 @ 깊이 512 | 0.9813 · 0.9854 · 0.9960 · 1.0020 | 0.991 ± 0.015 |
| 디코드 tg96 @ 깊이 4096 | 0.9990 · 0.9937 · 0.9901 · 0.9970 | 0.995 ± 0.006 |

pp 절대값은 B1이 pp512 143.5–145.5, pp4096 200.2–201.6 tok/s이고 base가 130.0–131.1 / 181.3–183.1이다. 층-배치당 `stat prefill split`(ms, B1 / base): `card_out` 19.4–19.7 / 29.6–29.7(P 512), 21.8–22.0 / 32.2–32.3(P 4096); `enqueue` 3.79–3.82 / 18.7–18.8; `card_proj` 12.0–12.3(B1만 찍는다); `union` 두 팔 모두 66–69로 같다.

같은 B1 바이너리로 한 번 더 쟀다(03:41:14 UTC 임대, 카드 `docs/cards/prose-cardin.card`). 산문 프롬프트(`corpus-prose.ids` 앞 512)에 뜨거운 목록 384를 켜고 shadow를 쟀다. `card_in` 45.28 ms/층-배치(예측 45–53), `union` 36.42, `card_out` 22.05, `card_proj` 13.72이고, pp512는 171.6 tok/s였다.

판명된 것. 두 P의 이득이 예측 대역의 윗끝에 걸친다 — 점 추정은 대역 위로 0.2–0.5 %p이고 구간은 대역과 겹친다. 카드 route는 −10.1 ms/층-배치로 라운드의 재유도 −5.4…−9.5보다 조금 더 줄었다. 흐름 모형이 비워 두었던 `card_proj`는 12.1 ms로 기대 14.8(9.2–16.6)의 아래쪽이다. 첫 임대만 보면 깊이 512 디코드가 0.985 ± 0.010으로 1을 벗어났지만, 둘째 임대의 두 바퀴가 0.996·1.002라 네 바퀴로는 효과가 가려지지 않는다. 디코드 PTX와 스텝 토폴로지는 두 빌드가 같으므로 임대 사이 흔들림으로 읽는다. 산문의 shadow 45.3 ms는 슬롯별 재읽기 모형의 아래끝이라, 타일 레버(cardtile)를 설계대로 짓는다.

<a name="q3gemmb-ab"></a>
## Qwen3 `gemm_q4k` 명령 다이어트(q3gemmb) — pp4096 +10.8 %, pp512 +13.3 %로 예측 밴드(+18…+24 %, +30…+38 %)에 못 미쳤다

질문. #q3gemma-ab에서 바이트를 39 % 줄인 레버 A는 사이클을 움직이지 못했다. 그래서 bloomery `docs/research/q3gemmlat-design-report.md`는 블록-스텝을 「워프-스텝 명령 수 × CPI ÷ 2」로 모형화했다. 이 모형대로 스텝 경로 명령을 줄이면 프롬프트가 빨라지는가를 물었다. q3gemmb는 16 B `cp.async` 스테이징, 분기 제거, 반쪽 전개, 출력 누산 축소를 묶은 것이고 비트는 그대로다. 풀타일 워프-스텝 명령이 889에서 630/682로 줄었고, 레지스터 128·spill 0이다. 모형은 T = 4096 런치의 사이클을 ×1.49–1.52로 읽었고, 창에 희석하면 pp4096 +18…+24 %, pp512 +30…+38 %였다[유도]. 임대 카드는 `docs/cards/q3gemmb-pp.card`(sha256 `000544869aaf`, kind ab, 3바퀴, 자 1.36 %)다.

한 일. A6000(300 W), Qwen3-30B-A3B-Instruct-2507 `Q4_K_M`에서 `depth-gpu-qwen3moe`를 3바퀴 돌렸다. 두 팔을 번갈아 돌렸고 순서는 바퀴마다 바꿨다. 트리는 bloomery main `a9fa916` 위의 q3gemmb(sha256 `1f672da31a5a`)이고, 베이스는 같은 main의 바이너리(sha256 `9d48aa1f778f`)다. 임대 시각은 04:37:18–04:41:09 UTC이고 `[other-busy]`는 0/12다. 이 임대에는 참조 엔진 팔을 넣지 않았다.

| @ A6000, n = 0 | 베이스 | q3gemmb | 비 |
|---|---:|---:|---:|
| pp512 tok/s | 6,803 | 7,706 | **1.1327 ± 0.0049** |
| pp4096 tok/s | 7,955 | 8,818 | **1.1084 ± 0.0087** |
| tg96 @ 깊이 512 / 4096 | | | 0.9994 ± 0.0017 / 0.9995 ± 0.0026 |

베이스 pp4096 7,955는 공개 표의 8,024(`ef00f78`, #q3router-ab)와 다른 창에서 잰 값이라 한 표에 두지 않는다.

판명된 것. 효과는 자 밖에 있고, 크기는 예측의 절반 남짓이다. 카드의 decide-below에 따라 착륙하고, `gemm_q4k`의 ncu 소스 페이지 한 번으로 모형이 부풀린 항을 찾는다. 후보는 둘이다. 하나는 CPI 항이다: 명령이 줄면서 사슬의 스톨이 드러났을 수 있다. 다른 하나는 창 희석(설계 보고 §G)에서 GEMM 몫을 잡은 방식이다. 디코드는 GEMM을 읽지 않으니 0이 맞다.

<a name="q3fa-ab"></a>
## Qwen3 프리필 flash 명령 다이어트(q3fa) — 비-HMMA 1,137 → 496인데 pp4096 +1.06 ± 0.37 %, 자(1.36 %) 안이라 보류

질문. bloomery `docs/research/q3tail-design-report.md` §3.2는 `gqa_prefill_flash`의 스텝을 합 모형(텐서 + 발행 + 배리어)으로 설명했다. 이 모형에서 명령 수는 레버다. 내부 타일에 마스크 없는 경로를 두고, u32 인덱스, `max.f32`, f16 가중치 exp에만 `ex2.approx.ftz`, 32비트 주소 증분, max가 안 바뀐 행의 재스케일 생략을 넣었다. 그 결과 워프-타일당 비-HMMA가 1,137에서 496(내부 경로)으로 줄었다. 레지스터는 255 → 194, spill 0, smem 34,816 그대로이고, 비트는 그대로다(ubatch 덤프 md5가 베이스와 같음). 모형 예측은 창 −15.6…−19.5 ms, pp4096 +3.0…+4.2 %였다[유도]. 카드는 `docs/cards/q3fa-pp.card`(sha256 `aa384b76e639`)다.

한 일. 위와 같은 조건, 같은 베이스로 쟀다. 트리는 sha256 `c7655b410030`, 임대 04:41:11–04:45:03 UTC, `[other-busy]` 0/12.

| @ A6000, n = 0 | 베이스 | q3fa | 비 |
|---|---:|---:|---:|
| pp512 tok/s | 6,762 | 6,781 | 1.0029 ± 0.0083 |
| pp4096 tok/s | 7,952 | 8,036 | **1.0106 ± 0.0037** |
| tg96 @ 깊이 512 / 4096 | | | 0.9998 ± 0.0027 / 0.9998 ± 0.0013 |

판명된 것. 바퀴 짝 구간은 0을 빼지만, 효과가 카드의 자(3바퀴에서 1.36 %) 안이고 1 % 근처는 빌드 둘의 링크 배치만으로도 갈린다. 그래서 카드 decide-below의 둘째 가지대로 보류한다. 비-HMMA를 56 % 줄였는데 시간이 약 1 % 움직였다. 레버 A(#q3gemma-ab)와 같은 모양이다: 한 유닛의 수요를 깎았는데 스텝이 거의 그대로다. 합 모형은 flash에서도 틀렸고, 텐서에 묶인 max 모형(또는 지연)이 다시 후보다. 가르는 것은 층 24 flash 런치의 ncu 스톨 구성 한 번이다. q3tail 예측은 클록 1.46–1.55 GHz, 텐서 56–60 %, 스케줄러당 발행 0.36–0.40이다. 같은 날 mistral.rs의 FA2가 같은 일을 런치당 1.276 ms에 한다(아래 #mrs-nsys). 우리 1.854 ms(q3tail의 nsys, `ef00f78`, 다른 창)와의 차는 창에서 약 28 ms이고, 그 몫은 명령 수가 아니라 다른 데 있다.

<a name="mrs-nsys"></a>
## mistral.rs Qwen3 프롬프트 한 번의 커널 분해(nsys) — P = 4096의 MMQ 합 351.9 ms로 예측(300–370) 안

질문. bloomery `docs/research/mrsq3-design-report.md`가 코드로 닫지 못한 한 항은 mistral.rs가 P = 4096 프롬프트에서 쓰는 MMQ 커널 시간의 합이다. 예측은 300–370 ms였다[유도]. 카드는 `docs/cards/mrsq3-nsys.card`(sha256 `8f7d334209a8`, kind profile)다.

한 일. flash-attn 빌드(sha256 `8da64b84d5e5`, `d5ae0f18f`)를 A6000에서 `mistralrs bench --prompt-len P --gen-len 1 --iterations 1 --warmup 1`로 돌렸다. `MISTRALRS_BENCH_CUDA_PROFILER_RANGE=1`로 측정 반복만 nsys(`-t cuda`, capture-range cudaProfilerApi)에 담았다. 임대는 04:46:33–04:46:50 UTC다. 첫 시도(04:45)는 러너 스크립트가 `--format gguf`를 두 번 넘겨 두 실행 모두 인자 오류로 끝났다. 측정은 없었고, 고쳐서 다시 돌렸다.

| 커널 합, ms | P = 512 | P = 4096 |
|---|---:|---:|
| 전체 | 99.1 | 509.1 |
| `mul_mat_q` Q4_K ×288 / Q6_K ×48 / stream-k fixup | 65.1 / 15.1 / 3.5 | 283.0 / 66.0 / 2.8 |
| MMQ 합 | 83.8 | **351.9** |
| `flash_fwd_kernel`(64×64, 4워프) ×48 | 1.9 | 61.2 (1.276 ms/런치) |
| `moe_weighted_reduce` / `quantize_mmq_q8_1*` / qk 노름+rope | 2.7 / 4.2 / 1.9 | 19.9 / 35.5 / 14.9 |

flash 런치는 48개이고, P×P softmax 커널이 없으니 flash 경로가 맞다(카드의 무효 조건 통과).

판명된 것. MMQ 합이 밴드 안이라 사다리 순서(q3gemmb, FA, H1, Q)는 그대로다. 우리 창과 비교하면 두 엔진의 틈이 어디 있는지 보인다. flash는 저쪽이 런치당 1.276 ms, 우리가 1.854 ms(q3tail nsys, 다른 창)로 저쪽이 앞선다. GEMM 몫은 q3gemmb 착륙 뒤 우리 쪽을 다시 재야 비교된다.

<a name="cardtile-ab"></a>
## V4.1 프롬프트 배치의 카드 expert를 타일로(cardtile, T) — 산문 pp512 +24.5 %, pp4096 +16.7 %, 두 P 모두 예측 밴드 안

질문. bloomery `237321a`(cardtile)는 프롬프트 배치의 카드 expert shadow를 (슬롯 ≤ 8개 타일, 가중치 8행) 블록으로 돌린다. 그래서 gate·up과 down의 가중치 행을 타일마다 한 번씩 m열 코어로 읽는다. 비트는 expert별 묶음 순회와 같다. #b1-pp-ab에서 산문 프롬프트의 shadow는 45.3 ms/층-배치로 union 36.4 ms 위에 있었다. 흐름 모형(`tools/flow`)은 타일로 바꾸면 shadow가 17.8 [15.5–19.9] ms로 union 아래로 내려가 산문 pp512가 +17.1…+25.2 %, pp4096이 +15.8…+22.8 % 오른다고 예측했다[유도]. 카드는 `docs/cards/cardtile-pp.card`(sha256 `67474fadf198`, kind ab, 2바퀴, 자 4.7 %)다.

한 일. A6000(300 W, 2100 MHz), 공개 `Q3_K_M`, 배치 (a), 뜨거운 목록 384, 산문 프롬프트(`corpus-prose.ids` 앞 P개), n = 96, `BLOOMERY_STEP_STATS=1`. main `efc202f`의 `generate_ds41`(sha256 `e7870ea97ffb`) 한 바이너리로 `BLOOMERY_CARD_EXPERTS=tile`(기본)과 `=expert`를 번갈아 쟀다. 바퀴마다 순서를 돌렸다. 임대는 08:10:26–08:26:03 UTC이고, `[cpu-busy]`·`[other-busy]`는 0/8이다.

| @ A6000, 산문 | expert | tile | tile / expert |
|---|---:|---:|---:|
| pp512 tok/s | 173.67 · 173.50 | 217.02 · 215.17 | **1.245 ± 0.060** (1.2496 · 1.2402) |
| pp4096 tok/s | 250.24 · 250.35 | 291.80 · 292.37 | **1.167 ± 0.011** (1.1661 · 1.1678) |
| tg96 @ 깊이 512 / 4096 | | | 0.9997 ± 0.0043 / 1.0009 ± 0.1383 |

층-배치당 `stat prefill split`(ms, expert → tile): P 512에서 `card_in` 45.4 → 23.1, `wait` 30.7 → 17.5, `card_out` 22.2 → 20.1, `enqueue` 5.6 → 3.9, `union` 36.5 → 36.6–37.0. P 4096에서 `card_in` 40.8 → 21.7, `wait` 28.2 → 18.6, `card_out` 24.8 → 22.5, `union` 39.0 → 39.1. 증인의 SM 클록은 두 팔 모두 실행 뒤 1920–1935 MHz였다. 두 팔의 디코드 경로는 같다. 디코드(tg96) 절대값은 깊이 512(산문 512 뒤) 44.60 · 44.60 · 44.67 · 44.64 tok/s(p50 22.31–22.41 ms), 깊이 4096(산문 4096 뒤) 39.53 · 39.93 · 39.61 · 39.15 tok/s(p50 24.89–25.26 ms)였다.

판명된 것. 두 P 모두 밴드 안이다. pp512는 윗끝, pp4096은 아래쪽이다. 카드의 decide-in대로 T를 유지하고 G(prefillgroup)로 간다. 벽이 줄어든 것은 `card_in` 자체보다 `wait`다. expert 팔에서는 shadow가 union보다 길었다. 그래서 다음 층의 route가 카드 스트림에서 그 꼬리 뒤에 줄을 섰다. tile에서는 shadow가 union 아래로 들어가면서 그 줄이 사라졌다. 틀린 항도 하나 있다. `card_in`은 P 512에서 23.1 ms로, 예측 17.8 [15.5–19.9]보다 16 % 길다. P 512에서는 union 그늘 아래라 값이 0이다. 그러나 P 4096의 카드 한 층-배치는 `card_out` 22.5 + `card_in` 21.7 = 44.2 ms이고, 호스트는 `union` 39.1 + `enqueue` 4.7 = 43.8 ms다. 둘이 맞선다(카드가 예측한 40 대 42). 그래서 G의 겹침 뒤에는 산문에서 카드 항이 벽에 걸린다. 타일 GT 커널의 점유율(ptxas `-v`, ncu)이 그때 값을 갖는다.

<a name="q3ncu-s16"></a>
## Qwen3 프리필 flash와 `gemm_q4k`의 ncu, FA2 대조군 — 우리 flash는 텐서 56–59 %, FA2는 81 %; 우리는 L2→SM 섹터를 1.49배 더 읽는다

질문. bloomery q3fa(#q3fa-ab)는 flash의 비-HMMA를 56 % 줄이고도 pp4096이 1 % 움직였다. 설계 보고 `docs/research/q3flashlit-design-report.md`는 FA2와 우리 커널이 같은 일을 한다고 유도했고, 우리 쪽에만 설명되지 않는 사이클이 남는다고 했다. 이번 시팅은 층 24, P = 4096 flash 한 런치를 세 커널에서 잰다(카드 `q3ncu-flash.card` sha256 `2e4fa9111a55`, `fa2-ncu.card` `0923fefc3b4f`, 둘 다 kind profile). 같은 자리에서 q3gemmb 착륙 뒤 `gemm_q4k`의 사이클도 잰다(`q3gemmb-ncu.card`, 예측 1.44–1.47 M).

한 일. A6000(300 W), `--clock-control none`, bloomery main `6778196`의 `just ncu-gpu-qwen3-pp 4096 24`를 main 바이너리(sha256 `11ed3969c4c7`)와 q3fa 트리 바이너리(`c7655b410030`)로 한 번씩 돌렸다. mistral.rs flash-attn 빌드(`8da64b84d5e5`)의 `flash_fwd_kernel`은 `--launch-skip 24 --launch-count 1`로 잡았다(grid (64,1,32)×128, 카드 조건 통과). 두 우리 행은 grid 2,048×128과 HMMA 34,078,720으로 런치를 증명했다. 임대는 08:26:27–08:28:51 UTC이고 `[cpu-busy]`는 0이다. 3090에는 옆 라운드의 게이트가 돌았다(`[other-busy]` 2회, 다른 카드).

| @ A6000, 한 런치 | main | q3fa | FA2 |
|---|---:|---:|---:|
| 경과 SM 사이클 (클록) | 2,911,224 (1.771 GHz) | 2,773,303 (1.802) | 2,141,803 (1.745) |
| 텐서 파이프, 활성 사이클의 % | 56.1 | 59.1 | 81.1 |
| wait / math_pipe_throttle / long_scoreboard | 25.8 / 21.4 / 7.4 | 21.7 / 27.4 / 18.1 | 20.8 / 42.7 / 2.1 |
| barrier / short_scoreboard / no_instruction | 5.5 / 3.7 / 0.9 | 6.2 / 4.6 / 0.9 | 7.8 / 1.7 / 0.7 |
| L2→SM 읽기 섹터 (블록-타일당) | 102,959,024 (1,547) | 102,959,116 (1,547) | 69,206,016 (1,040) |
| L2→SM, 피크 대비 | 72.6 % | 77.8 % | 64.8 % |
| 레지스터 · 스케줄러당 워프 | 255 · 1.99 | 194 · 1.99 | 182 · 1.96 |

`gemm_q4k`(`gate_p8`의 `gemm_q4k_moe_t4096`, `--clock-control base`, 두 런치): 경과 1,747,429 · 1,744,087 사이클이다. 착륙 전 2,190,925에서 −20.3 %이고, 카드의 밴드보다 위다.

판명된 것. 우리 두 행의 텐서는 카드 밴드(56–60 %) 안이다. q3fa는 main보다 사이클이 4.7 % 적다. nsys로 환산하면 ≈ 1.77 ms로, 설계가 pp에서 유도한 1.70–1.78 ms 안이다. FA2는 81 %로 카드 밴드(86–96 %) 아래다. 그러니 현실적인 목표는 텐서 천장이 아니라 FA2의 수치다. q3fa는 FA2보다 경과 사이클로 1.30배, 활성 사이클로 1.37배 길다. no_instruction이 세 커널 모두 1 % 아래라 명령 fetch 읽기는 죽었다. q3fa의 short_scoreboard도 4.6 %로 작다. 가장 큰 새 사실은 바이트다. 설계는 L2→SM 바이트가 같다고 유도했다. FA2는 그 유도(K+V 32 KB + bf16 Q)에 정확히 맞지만, 우리는 블록-타일당 약 16 KB를 더 읽는다. q3fa에서는 명령을 깎은 뒤 long_scoreboard가 7.4 → 18.1 %로 드러났다. ~~이 16 KB가 어디서 오는지는 아직 코드 독해로 설명되지 않는다. 설계 라운드 q3fstep에 넘겼다.~~ 정정(같은 날, 리드가 다시 셈): 블록마다 적재되는 키는 hi = t0 + 8개이고, 2,048블록의 합은 4,202,496키다. 여기서 1.5 × (K+V) 67,239,936 + f32 Q 2,097,152 + n_keys 2,048 = 102,959,104섹터가 나오고, 실측과 80섹터 안에서 맞는다. K·V의 cp.async만 1.5배로 세이고 Q는 1배다. 후보 기제는 smem 행 패딩이다. `mma_row_words(128)`가 272 B 행이라 홀수 행의 시작이 32 B 경계에서 16 B 어긋나고, 그 행에서는 전역 32 B 섹터 하나가 smem 두 섹터로 갈라진다(짝수 행 1배, 홀수 행 2배, 평균 1.5배). FA2의 XOR 스위즐은 짝을 한 섹터 안에 두므로 1.00배다. 같은 날 `gemm_q4k`의 소스 페이지에서 ncu 자신의 명령별 지표("L2 Theoretical Sectors Global" 대 Ideal)도 80 B 행 가중치 타일의 LDGSTS를 정확히 1.5배로 보였다. 개입으로 확인한 것은 아직 아니다(패딩 32 B 탐침이나 스위즐로 잰다). `gemm_q4k`는 카드의 decide-above대로다. CPI 항이 절감을 과대평가했으므로, 다음 GEMM 라운드 전에 소스 페이지의 명령별 스톨을 읽는다. 러너 결함도 하나 나왔다. `BLOOMERY_NCU_SOURCE=1`이면 ncu의 `--log-file`에 `==PROF==` 줄만 남아 비어 있지 않은데, 러너가 그것을 보고 details 페이지를 건너뛰어 rc 3으로 끝났다. 수치는 보존된 보고서(`.ncu-rep`)에서 GPU 없이 다시 뽑았다. 러너는 메트릭 행이 있는지로 판정하도록 고쳤다.

<a name="prefillgroup-ab"></a>
## V4.1 프롬프트를 두 배치씩 층 우선으로(prefillgroup, G) — lcg pp4096 +20.4 %(바퀴 1.177 · 1.232), `wait` 17.8 → 0.8 ms/층-배치

질문. bloomery `eb3e08a`(prefillgroup)는 프롬프트의 배치를 둘씩 묶어 층 우선으로 발행한다. 층-배치마다 route를 앞 항목의 shadow 바로 뒤, 그 항목의 호스트 union보다 앞에 넣는다. 층의 마지막 배치에서 다음 층의 첫 배치로 넘어갈 때도 같다(wrap). 그래서 카드의 route가 호스트 union 아래에서 돈다. 비트는 G 1과 같다. 흐름 모형은 lcg P 4096에서 +19.0…+24.1 %를 예측했다[유도]. 카드는 `docs/cards/prefillgroup-pp.card`(kind ab, 2바퀴, 자 4.7 %)다.

한 일. A6000(300 W), 공개 `Q3_K_M`, 배치 (a), 뜨거운 목록 없음, lcg 프롬프트, n = 96, `BLOOMERY_STEP_STATS=1`. main `e690f54`의 `generate_ds41` 한 바이너리로 `BLOOMERY_PREFILL_GROUP` 기본(2)과 `=1`을 번갈아 쟀다. 임대는 09:15–09:21:44 UTC이고, busy 표시는 0/4다. 산문 프롬프트는 돌리지 않았다. T 뒤 산문에서는 카드와 호스트가 맞서므로(#cardtile-ab), 그 G 항은 재지 않고 유도한다.

| @ A6000, lcg, P = 4096 | G 1 | G 2 | G 2 / G 1 |
|---|---:|---:|---:|
| pp tok/s | 199.91 · 200.95 | 235.21 · 247.46 | **1.204** (1.1766 · 1.2315) |
| `chain` s | 20.14 · 20.04 | 16.39 · 16.23 | 0.814 · 0.810 |
| `prologue` ms | 344 · 335 | **1,018** · 323 | |
| tg96 @ 깊이 4096 | | | 0.9997 |

층-배치당(ms, G 1 → G 2): `wait` 17.8 → 0.83, `union` 68.7–69.1 → 68.6–69.3, `enqueue` 4.6 → 4.3–4.4, `card_out` 21.7 → 21.9, `card_in` 11.8 → 11.7. `vram_free_load`는 G 1이 1.020 GB, G 2가 0.783 GB다. 차이 237 MB는 G 2의 `group_bytes=238,191,200`과 같다.

판명된 것. wrap이 설계대로 돈다. 호스트가 route를 기다리던 17.8 ms가 0.8 ms로 사라졌고, `union`은 겹침 아래에서도 느려지지 않았다. 두 바퀴의 `chain` 비는 0.814와 0.810으로 같다. 1바퀴 비가 밴드 아래(+17.7 %)로 떨어진 것은 그 바퀴 G 2 행이 임대의 첫 실행이었기 때문이다. 그 행의 `prologue`가 1,018 ms로, 같은 팔 2바퀴의 323 ms보다 0.7 s 길었다(계획 단계의 파일 읽기가 차가웠다). 이 행을 빼면 2바퀴 +23.2 %가 밴드 안이다. 두 바퀴 평균의 구간(± 0.35)은 이 한 행 때문에 넓다. 판정은 decide-in이다: G 2를 기본으로 두고, lcg P 4096은 이제 호스트에 묶인다(층-배치 벽 약 74 ms 중 `union` 69). 틀린 것 하나: 모형은 G 1 기준값을 208.2로 봤지만 실측은 200.4였고, `union`도 모형의 63.3보다 긴 68.7–69.1이다. 다음 레버는 호스트 쪽(h3tile-b, hoststream)이다.

<a name="padprobe"></a>
## Qwen3 flash의 L2 섹터 1.5배 — smem 행을 32 B로 정렬하자 바이트 하한까지 내려갔다(기제 확인)

질문. #q3ncu-s16에서 `gqa_prefill_flash`는 L2→SM 섹터를 1.5 × (K+V) + Q + 카운트만큼 읽었다. 후보 기제는 272 B로 패딩한 smem 행이다. 홀수 행이 32 B 경계에서 16 B 어긋나 cp.async의 32 B 섹터 하나가 둘로 갈린다는 것이다. 두 가지로 확인한다. 하나는 개입이다. bloomery `e6364f2`에 행을 288 B(32 B 정렬)로 바꾼 한 줄만 얹는다(카드 `padprobe-flash.card`, 예측 69.2–69.5 M). 다른 하나는 빌드 없이 q3fa 바이너리의 ncu 소스 페이지를 뽑는 것이다(카드 `q3flash-srcpage.card`, K/V LDGSTS의 Theoretical/Ideal 예측 1.49–1.51).

한 일. A6000, `just ncu-gpu-qwen3-pp 4096 24`, `BLOOMERY_NCU_SOURCE=1`, 층 24 한 런치. 임대는 09:23:20–09:24:13 UTC이고 `[cpu-busy]`·`[other-busy]`는 0이다.

| @ A6000, 한 런치 | q3fa (272 B 행) | 탐침 (288 B 행, main 기반) |
|---|---:|---:|
| L2→SM 읽기 섹터 | 102,959,110 | **69,339,067** |
| 바이트 하한 (K+V + f32 Q + 카운트) | 69,339,136 | 69,339,136 |
| K/V LDGSTS, ncu Theoretical/Ideal | **1.5008** | 1.0022 |
| Q `LDG.E.64` / 출력 `STG` | 1.00 / 2.00 | 1.00 / 2.00 |
| L2→SM, 피크 대비 | 77.5 % | 47.0 % |
| shared bank conflicts | 175,177 | 68,713,702 |
| 경과 SM 사이클 | 2,753,193 | 3,145,995 |

판명된 것. 두 카드 모두 decide-in이다. 행을 32 B로 정렬하자 섹터가 하한과 69섹터 차이까지 내려갔다. 소스 페이지에서도 q3fa의 K/V 복사만 1.50배이고 Q 로드는 1.00배다. 그러니 초과분은 목적지 smem 행의 정렬에서 온다. 탐침의 사이클이 느린 것은 예상한 대로다. 288 B 행은 ldmatrix를 2-way 충돌로 만들고(뱅크 충돌 6,871만 건), main의 255레지스터 커널 위에 얹었기 때문이다. 이 행은 섹터가 무엇 때문인지를 보여 줄 뿐, 스위즐이 사이클을 얼마나 버는지는 말하지 않는다. 그 값은 구현 라운드 q3swz의 카드(2.14–2.50 M 사이클 예측)가 잰다. 같은 페이지에서 새로 보인 것도 하나 있다. 출력 `STG`가 이상값의 2배라 쓰기 67 MB가 섹터로는 두 배다. 러너 결함도 하나 드러났다. `BLOOMERY_NCU_SOURCE=1`일 때 q3pp 요약은 details 페이지의 사람용 단위("Ghz", "MB")를 원시 단위로 읽는다. 그래서 클록이 0.000 GHz, DRAM이 150 B처럼 찍힌다. 위 표의 섹터와 사이클은 원시 CSV에서 읽은 값이다.

<a name="r8-sidecar"></a>
## V4.1 r8 사이드카 첫 실변환 — 155.7 GB를 118 s에 쓰고 40 s에 원본과 대조했다

질문. bloomery r8conv(`d946e1d`)가 만든 변환기로 공개 V4.1 파일(`Q3_K_M`, 9샤드)의 라우팅 gate/up 80텐서를 행-레인 배치로 옮긴 사이드카를 쓴다. 카드는 `r8-sidecar.card`(kind exclusive, 5–8분[유도: 차가운 원본 4.7–5.2분, 최악 7.2분])다.

한 일. `BLOOMERY_BOX_ENV='BLOOMERY_LEASE_CARD=docs/cards/r8-sidecar.card' just r8-sidecar`, 임대 09:47:02–09:49:40 UTC. 결과는 convert rc 0 118 s(1.35 GB/s), verify rc 0 40 s(4.32 GB/s)다. 파일 크기는 155,713,555,008 B이고 `/models` 여유는 967 → 822 GB다. 원본 NVMe의 읽기는 359,065,984섹터(184 GB), 쓰기는 304,149,192섹터(155.7 GB)다.

판명된 것. 카드의 5–8분보다 짧았다. 원본이 페이지 캐시에 거의 따뜻하게 있었기 때문이다(시작 때 Cached 253 GB). 카드가 따로 적은 따뜻한 convert 예측 63–102 s보다는 길었다. 쓰기(1.35 GB/s)가 그 항을 잡았다. 이 파일을 읽는 호스트 계층 라운드 r8host가 뒤따른다.

<a name="q3swz-ncu"></a>
## Qwen3 flash K/V 타일 XOR 스위즐(q3swz) — 층 24 런치 2,765,883 → 2,319,186 사이클(−16.1 %), 섹터는 바이트 하한

질문. #padprobe가 가려낸 섹터 초과를 FA2식 XOR 스위즐(256 B 행, 16 B 청크를 `row & 7`로 반행 안에서 섞음)로 없애고, 프롤로그 다이어트(쿼리 로드 32개 선발행, `cvt.rn.f16x2.f32` + NaN select)를 함께 넣은 트리가 q3swz다. q3fa 위에 얹었고, 비트는 main과 같다(`--dump` md5 여섯 개 동일, 2³² 전수 게이트 녹색). 카드는 `q3swz-ncu.card`(kind profile)다. 예측은 2.14–2.50 M 사이클이고, 섹터가 69.34 M ±0.1 %가 아니면 무효다.

한 일. A6000, `just ncu-gpu-qwen3-pp 4096 24`, `--clock-control none`, 한 시팅에 q3swz 트리(sha256 `398a906e50b5`, `BLOOMERY_NCU_SOURCE=1`)와 q3fa 바이너리(`c7655b410030`)를 돌렸다. 임대는 09:50:26–09:51:14 UTC다.

| @ A6000, 층 24 한 런치 | q3fa | q3swz |
|---|---:|---:|
| 경과 SM 사이클 (클록) | 2,765,883 (1.799 GHz) | **2,319,186** (1.80 GHz) |
| L2→SM 읽기 섹터 | 102,959,110 (#padprobe) | **69,339,372** (하한 69,339,136) |
| 텐서 파이프, 활성 사이클의 % | 59.1 | **70.4** |
| long_scoreboard / math_pipe_throttle / wait | 17.7 / 27.5 / 21.7 | 7.0 / 33.8 / 23.7 |
| L2→SM, 피크 대비 | 77.2 % | 62.9 % |
| 레지스터 · smem | 194 · 34,816 B | 196 · 32,768 B |

판명된 것. 사이클과 섹터 모두 카드 밴드 안이다(decide-in). main의 2,911,224(#q3ncu-s16)에 대면 −20.3 %다. q3fa 단독의 명령 절감이 +1.06 %에 그쳤던 것은 L2 벽 때문이었고, 벽이 걷히자 그 절감도 함께 드러났다. 창으로 옮기면 flash 48런치 × 약 −0.29 ms로 약 −14 ms, pp4096 약 +3 %[유도]이고 카드 밴드 +1.8…+4.4 % 안이다. 이 값은 착륙 뒤 pp A/B가 잰다. long_scoreboard가 7.0 %로 카드가 정한 5 %를 넘으므로, FA2 스텝 모양보다 L2 순서 레버(블록 순서를 헤드끼리 묶어 DRAM 재읽기 약 55 MB를 줄이기)를 먼저 본다. 소스 페이지의 명령별 섹터 도구(q3swz가 넣은 `q3pp.py source`)는 남은 초과 둘을 짚었다. 부분 타일의 술어 LDGSTS 셋(1.14–1.57배, 양이 작다)과 출력 `STG` 64개(전부 2.0배, 4 B 쌍 저장)다. 전체는 1.031배다. 앞 절의 요약 단위 결함 때문에 이 행의 클록은 원시 CSV에서 읽었다.

## Qwen3 `gemm_q4k`를 스텝당 배리어 하나로(q3gemmc) — T = 4096 런치 1.746 M → 1.715 M 사이클(−1.8 %), 예측(1.41–1.53 M)에 못 미쳤다; 풀타일 루프 −3.7 %, 부분 타일 루프 +8.9 %

질문. #q3ncu-s16의 소스 페이지는 `gemm_q4k`의 스텝이 명령 수와 무관한 세 항(배리어 두 번, IMMA 발행, 복사·LDS 몰림)을 낸다고 읽혔다. q3gemmc는 그 흐름을 바꾼 트리다. F1은 스텝당 배리어를 하나로 줄이고 다음 스텝의 복사를 이번 스텝의 계산 안에서 낸다(풀타일은 n-타일 1 뒤, 부분 타일은 배리어 바로 뒤). F2는 스테이징의 열 주소를 타일당 한 번만 계산한다. 비트는 main과 같다(`gate_gemm` `y_fnv` 328/328, `--dump` md5 여섯 개 동일). 카드는 `q3gemmc-ncu.card`(kind profile)이고, 예측은 1.41–1.53 M 사이클이다.

한 일. A6000, `BLOOMERY_NCU_SOURCE=1 BLOOMERY_NCU_COUNT=2 just ncu-gpu-gemm`(암 `gemm_q4k_moe_t4096`, 건너뛰기 64, `--clock-control base`)을 q3gemmc 트리(`gate_p8` sha256 `0ecbb2537d9b`, q3swz 위; gemm 엔트리를 바꾼 것은 q3gemmc뿐)에서 돌렸다. 임대는 10:21:57–10:22:14 UTC다. 증인에 `[other-busy]`가 찍혔다. 다른 리드의 게이트가 3090에서 돌고 있었고, 잰 카드는 A6000이다. 기준은 #q3ncu-s16의 main 페이지(`…-082817`)다. 위상은 q3gemmc가 넣은 `tools/ref/srcpage.py`로 나눴고, 단위는 워프-스텝당 워프-사이클이다.

| @ A6000, T = 4096 한 런치 | main (#q3ncu-s16) | q3gemmc |
|---|---:|---:|
| 경과 사이클, 런치 둘 | 1,747,429 / 1,744,087 | **1,717,346 / 1,712,564** |
| 발행 명령 | 291,069,258 | 284,536,287 |
| 풀타일 루프, 워프-스텝당 (표본 몫) | 4,899 (78.5 %) | **4,716** (77.2 %) |
| · barrier / mio_throttle / math_pipe_throttle | 562 / 556 / 741 | 345 / 350 / 625 |
| · long_scoreboard / short_scoreboard | 76 / 615 | **349 / 848** |
| 부분 타일 루프, 워프-스텝당 (표본 몫) | 3,243 (11.4 %) | **3,533** (12.7 %) |
| · barrier / short_scoreboard / mio_throttle | 475 / 364 / 372 | 268 / **857** / 506 |

판명된 것. 두 런치의 차이가 0.28 %라 행은 유효하다. 결과는 카드 밴드 위(decide-above)다. 배리어는 예측대로 줄었다(풀타일 562 → 345). 그런데 그만큼이 계산 구간 안의 대기로 옮겨 갔다. 풀타일의 long_scoreboard +273은 대부분 계산 안으로 옮긴 복사 묶음에서 나온다. 복사 주소가 모두 레지스터 한 쌍을 거친다(`IADD3 R52, P1, R1xx, UR…` 다음 `LDGSTS […][R52.64+…]`). R52를 덮어쓰는 명령 여덟 개(IADD3 일곱, I2FP 하나)에 대기가 약 266 쌓였고, 거의 전부 long_scoreboard다. 앞 LDGSTS가 MIO 큐에서 R52를 읽어 갈 때까지 다음 명령이 기다리는 것으로 읽힌다. 레지스터 상한 128에서 계산 구간에 빈 쌍이 모자라 ptxas가 한 쌍을 돌려 쓴 것으로 보인다[추정]. 스텝 머리의 복사 대기(DEPBAR 뒤 WARPSYNC 두 행)는 92다(main의 DEPBAR 행은 표본 13개). 복사가 하나씩 늦게 나가니 늦게 도착한다. short_scoreboard +233은 계산의 LDS 결과가 같은 MIO 큐에 선 복사 뒤로 밀린 몫으로 읽힌다[추정]. 부분 타일은 오히려 느려졌다. 배리어 바로 뒤에 낸 복사 묶음이 첫 n-타일의 LDS를 막은 것으로 보인다(short_scoreboard 364 → 857)[추정]. 라운드 보고의 유도로는 P = 512에서 모든 타일이 부분 타일(nt_live 3–5)이다. 그러니 이 트리는 pp512의 GEMM을 늦출 공산이 크다[유도, 재지 않음]. 그래서 q3gemmc는 이대로 착륙하지 않는다. 다음 라운드는 두 가지를 고친다. 복사 묶음의 주소가 비행 중인 LDGSTS가 읽는 레지스터를 덮어쓰지 않게 하는 것(SASS에서 컴파일 시점에 증명), 그리고 부분 타일의 복사 위치를 되돌리는 것이다. 판정은 T = 4096과 T = 512 두 암을 main과 한 시팅에서 잰다.

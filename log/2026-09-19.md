# 2026-09-19

bloomery(당일 밤까지는 mulle이었다 — 개명)에서 Rust 양자화 gemv 커널을 위임 A/B 네 라운드로 0.40배에서 1.86배(ggml 대비)까지 올렸고, CPU 참조 엔진은 오전 첫 forward(0.0521 tok/s)에서 밤의 디스패치 접기까지 이어져 5.48 tok/s(같은 임대 ik보다 아직 14.8배 느림)에 닿았다. 별도로 mainline PR #29008의 메시지 구분자가 콜드 프리필에 물리는 값(min-step 0에서 3.26배, 서버 기본값 8192에서 1.134배)을 쟀고, DSpark 검증 패스 비용이 라우팅 expert 바이트 몫(34.56%)과 0.13%p 안에서 일치함을 확인해 "검증이 공짜"라는 이전 주장에 선을 그었다.

<a name="a-first-rust-kernel-two-arms"></a>
## Rust로 쓴 첫 커널, 같은 스펙을 두 모델에

**05:40–06:36 UTC, RTX 3090, DeepSeek-V2-Lite-Chat Q3_K_M `blk.1.ffn_gate_exps.weight`.** bloomery 0단계. sm_86에서 cuda-oxide 양자화 gemv가 ggml을 얼마나 따라잡는지, 같은 스펙을 GLM-5.3과 muse-spark-1.3에 워크트리 나눠 위임했다(리드가 게이트 재실행).

1라운드: stack M=1(ggml `mmvq` 333.1 GB/s)에서 GLM 62.7 GB/s(0.19배, 워프당 x 사적 소유로 8KB 32섹터 gather 병목), muse 134.4(0.40배, x를 shared memory 스테이징). muse가 절반 시간(20분 대 38분)에 두 배 빠른 커널을 냈고 스펙 오류도 muse만 보고했다. GLM `#[unroll]`은 `usize` 카운터에서 ICE.

2라운드(22:50–23:29, ggml처럼 q8_1 양자화+`dp4a`): GLM stack M=1 348.3 GB/s(**1.046배**, main 9d83f97), muse 303.5(0.91배). M=8은 둘 다 0.8배 안팎(스케일이 4-weight마다 붙어 dp4a당 FMA 하나).

3라운드(23:44–00:04, `max`): 레버가 갈렸다 — GLM은 q8_1 블록 128-value화(M=1 1.14배, M=8 1.06배), muse는 `get_unchecked` 경계 검사 제거(M=1 1.57배, +47%). 합쳐서 stack M=1 **620.3 GB/s(1.86배)**, M=8 200.2(1.20배), expert0 M=1 173.5(1.46배) — 네 형상 전부 ggml을 넘었다(오차 3.5e-3~4.8e-3대, ggml과 같은 대역). 같은 경계 검사 레버가 1라운드 1%, 3라운드 47% — 커널이 대역폭 바운드에 가까워지자 값이 달라졌다.

4라운드(과제 전환, `max`): CPU AVX2 Q3_K×Q8_K gemv, big(317MB) M=1에서 둘 다 1.11배지만 M=8·SMT 64스레드에서 GLM 채택(0.87배 대 0.57배). muse는 `MADV_HUGEPAGE`가 실제로 안 걸린다는 것(`AnonHugePages` 0kB)을 남겼다 — DRAM 147.7 GB/s를 이 라운드가 넘어(ggml 153~188, Rust 202~213) WKS-35가 잠정 되돌렸다. cuda-oxide `#[unroll]` ICE는 두 팔이 독립적으로 같은 두 프레임(`dialect-mir/src/const_fold.rs:200`, `mir-transforms/src/unroll.rs:387`)에 도달 — 언롤 후 SCCP가 런타임 트립도 낙관적 상수 취급해 폭 다른 시프트에서 터진다. 초안 `docs/upstream/cuda-oxide-unroll-ice.md`, 미제출.

매 라운드 GLM이 출력+추론 토큰 1.9~3.6배 더 썼다(effort 라벨은 벤더 간 통제 변수가 아니다). 카드는 3090뿐(A6000은 다른 학습). 결론은 승자가 아니라 겹침 — 두 보고가 겹치는 자리(1→2)나 갈리는 자리(3)가 다음 스펙이 됐다. 이슈 MUL-1·MUL-8(닫힘)·MUL-3·MUL-7, 계획 WKS-35. 코드 bloomery `84b0d98`(결합 `d2d0627`).

원본: `git show c27c3a7:log/2026-09-19-a-first-rust-kernel-two-arms.md`

<a name="b-delimiters-and-cold-prefill"></a>
## 구분자가 콜드 프리필에 물리는 값

**19:15–19:50. DeepSeek-V4-Flash-0731 UD-Q4_K_XL 155GB, mainline 포크 `313586ff4`, `-ngl 99 -ts 2,1 -t 32 -b 2048 -ub 2048`.** mainline PR [#29008](https://github.com/ggml-org/llama.cpp/pull/29008)(메시지 구분자, 9/17 머지) 뒤 [#25320](https://github.com/ggml-org/llama.cpp/issues/25320)이 "구분자가 긴 세션 프리필을 망친다"고 지적. 그 메커니즘은 min-step 가드(`checkpoint_min_step` 기본 8192)로 이미 7/9에 닫혀 있어, 질문은 "지금 기본값에서 얼마인가"였다.

재빌드 없이 `/v1/chat/completions`(구분자 ON) 대 `/completion`+토큰 배열(OFF)을 대조(`cache_prompt: false`). many 모양(48 160토큰·유저 턴 121개), min_step 0(최솟값 없음)에서 ON/OFF **3.26배**(438 120ms 대 134 450ms, 청크 123 대 24) — 보고자가 본 3배 그대로. 서버 기본값 8192에서 **1.134배**(청크 31), 65536에서 1.044배(청크 27). agent 모양(17 522토큰·유저 턴 1개)은 전 설정 1.00~1.02배 — 끊을 자리가 하나뿐이라 PR 본문 표가 이 비용을 못 봤다.

구분자가 값을 버는 재요청(캐시 히트) 쪽은 별도다(#29008 본문: 13 167토큰 편집 후 재요청 236초 → 5.0초). 8192 many 비는 콜드 1.149배·웜 1.134배로 1.3% 안에서 재현됐다.

원본: `git show c27c3a7:log/2026-09-19-b-delimiters-and-cold-prefill.md`

<a name="c-the-first-forward"></a>
## 우리 엔진이 처음으로 토큰을 골랐다

**11:20–11:57 UTC, CPU 전용, DeepSeek-V2-Lite-Chat Q3_K_M, ik c10fbbcc.** bloomery 1-4, 어텐션·dense FFN·MoE·출력 헤드를 `forward` 하나로 엮었다. 모듈 게이트는 전부 오라클 입력을 먹고 통과한 것이라 "내 출력이 내 입력일 때도 맞는가"를 물은 적이 없었다.

블록 27개의 잔차를 각 블록 피크 대비 상대값으로 대조하니 블록 3~23에서 드리프트가 6e-4로 평평(`inp_embd`는 비트 정확), 마지막 세 블록에서 자라 `result_output` 2.2e-2. 판정은 로짓이 아니라 토큰 — 프롬프트 32개(2~11토큰)로 ik argmax와 대조하는 전용 바이너리 `argmax_ref`를 새로 짰다. **31/32 일치**, 어긋난 5번은 ik 1·2위 차 0.262에 우리 오차 0.285(마진 안 뒤집힘).

첫 tok/s: bloomery(6토큰 프롬프트, 8토큰 생성, 1스레드, 캐시 없음) **0.0521**, ik tg32(32스레드) 83.72, tg16(1스레드) 10.19, pp512 680.62. 스텝이 13.5초(컨텍스트 6)에서 24.9초(컨텍스트 13)로 자란다 — KV 캐시가 없어 매 스텝이 전체 재프리필. `is_moe=false` FAIL-first에서 `l_out-1`이 밴드의 197배로 죽고 argmax도 뒤집혀 잡혔지만, 라우터를 꺼도 에러가 안 났다는 게 더 볼 만하다 — `ffn.rs`가 텐서 **존재**로 삼중항을 골라 MoE 전부가 조용히 shared expert로 폴백했다.

원본: `git show c27c3a7:log/2026-09-19-c-the-first-forward.md`

<a name="d-the-cache"></a>
## 디코드가 평평해졌다 — KV 캐시로 5.26배

**13:15–13:22 UTC, CPU 전용, 같은 모델.** bloomery 1-5 첫 조각. `kv.rs`가 행을 `(seq, pos)`로 키 잡고(투기 디코드 되감기 대비) 슬롯 표를 블록별이 아닌 단일 표로 둬 `push`가 매번 행 수·슬롯 수를 `assert!`로 대조한다. 오라클 경로를 캐시 경로의 껍데기로 만들어 두 경로를 하나로 합쳤다.

한 방 캐시 대 무캐시, 프리필+스텝 대 한 방, 토큰 하나씩 여섯 번 — 세 대조 전부 최대 차 **0**(허용오차 아님). 56토큰 프롬프트로 32키 러닝 맥시멈 갱신 분기(ik glibc `expf` 대 우리 `f32::exp`)를 처음 건드렸으나 안 터졌다(argmax 32/33 유지).

tok/s: 캐시 **0.2730**(스텝 폭 0.5%), 무캐시 0.0519(폭 84.5%) — **5.26배**, 생성 토큰 동일. ik 83.48(32스레드)·10.19(1스레드). 남은 것: `matmul_q`가 단일 스레드 스칼라, 호출마다 가중치 재디퀀트, GPU 0바이트.

원본: `git show c27c3a7:log/2026-09-19-d-the-cache.md`

<a name="e-where-the-time-goes"></a>
## 시간이 어디로 가는지 먼저 잰다 — 디퀀트가 내적보다 크다

**14:30–14:40 UTC, CPU 전용.** bloomery 1-5 두 번째 조각. 계측 전 도출: 토큰당 활성 2.44G·평균 3.9비트로 약 1.26GB/토큰, 147.7 GB/s 대비 천장 117 tok/s. 실측 유효 대역폭 0.34 GB/s(1코어 12.9 GB/s의 1/38). 토큰당 4.9 GFLOP/3663ms=1.3 GFLOP/s인데 Zen 3 스칼라 f32 피크 4~8이므로 "디퀀트가 내적과 같거나 크다"고 예측했다.

계측기 `crate::profile`(레벨 0/1/2), FAIL-first로 커버리지 게이트 검증(`matmul_q` 훅 제거 시 7.9%로 붕괴, `q_nope2_absorbed` 제거는 site 단언이 "훅 죽음"으로 잡음). 실측 바이트 1.305GB(예측 1.26GB, 3.6%차). **디퀀트 4128.5ms 대 내적 3286.2ms — 56:44**(예측 적중, 답은 ik `iqk_mul_mat`처럼 디퀀트·내적 융합). 예상 밖: `q_nope2_absorbed` 614.6ms 중 592.1ms가 토큰 무관 `wk_b` 재양자화(디코드 스텝마다 27블록×16헤드 매번 새로). Q4_K는 바이트 11%인데 시간 18.8%(파라미터당 디퀀트 비용 Q3_K의 4.3배, 융합 라운드가 대체할 항이라 손대지 않음).

암달 계산으로 "91.4%를 32코어로 나누면 26배"가 틀렸음을 잡았다 — `q_nope2_absorbed`(스레드 무관) 포함 시 8.2배, 캐시로 없앤 뒤라야 24배(둘 다 도출). 가중치 준비 캐시가 병렬화 8배와 24배를 가르는 항임이 산수로 먼저 드러났다.

원본: `git show c27c3a7:log/2026-09-19-e-where-the-time-goes.md`

<a name="f-fifteen-times"></a>
## 15.3배 — 행을 32코어에 나누고, 토큰 무관 일을 로드 시점으로

**15:06–15:20 UTC, CPU 전용.** bloomery 1-5 세 번째. `matmul_q`가 출력 행을 상주 스레드 풀에 분할(행 분할만이 비트 동일의 전부). `q_nope2_absorbed`의 `wk_b` 재양자화(가중치만의 함수)는 `Derived`로 로드 시점 한 번(30.1MB, 313ms)으로 옮겼다. 비트 단언(스레드 1/3/32 로짓 바이트 동일, `Derived` 대 즉석 생성 바이트 동일) 전부 통과.

**0.2730 → 4.1639 tok/s**(생성 토큰·32/33 argmax 불변). 같은 임대 ik 77.76±0.24 → 남은 배수 18.7배. 스레드 스윕: 1 0.2976, 8 1.7345(효율 73%), 16 3.0764(65%), **32 4.2503(45%)**, 64(SMT) 1.1433(3.7배 느림 — 초과 구독에서 스핀 워커가 대역폭을 뺏음, `BLOOMERY_SPIN=0`으로 64스레드 2.1805로 회복, 32스레드에선 같은 플래그가 손해). `q_nope2_absorbed`는 23.2ms(dequant 0.00)로 확인, 디퀀트:내적 비율 56:44 그대로.

예측 24배 대신 실측 15.3배(병렬 효율 45%와 계측 밖 8.4%가 원인). 러너 증인 함수의 스레드 수 버그(다섯 행이 "기본값" 증인)를 이 라운드에서 잡았다 — 숫자는 맞았고 증인만 거짓이었다.

원본: `git show c27c3a7:log/2026-09-19-f-fifteen-times.md`

<a name="g-the-fused-kernel-runs"></a>
## 융합 커널이 돈다 — 4.16에서 4.54 tok/s, 게이트는 밴드를 지킨다

**22:31–22:34 UTC, ik c10fbbcc(4904).** 커버리지·커널·배선 세 라운드를 묶는다. **4.1639 → 4.5369 tok/s**, 같은 임대 ik 82.27±0.40 → 남은 배수 18.1배(루프라인 117의 3.9%).

커버리지: 이름 없는 20ms/토큰(ik 스텝 전체보다 큼)에 훅 21개 추가해 접착부 스무 개로 특정(`flash_attn_latent` 9.6ms 최대). 임계 80%→98%(wall 25% 흔들려도 비율 98.04~98.11%로 독립).

커널: `crates/qdot`, ik `ggml-quants.c`를 그대로 이식한 AVX2 Q3_K×Q8_K 융합 내적(f32 미생성). **예측하고 실측**: 융합이 f64 정확해에 더 가까워야 함 → k=2048에서 932/1024 행이 더 가까움(8.542e-5 대 2.768e-4, 3.2배), k=512에서 903/1024(2.596e-6 대 5.387e-5, 20.7배) — 빠르면서 더 정확했다. FAIL-first 하나는 예측대로 안 떨어짐(`iscale` 음수 극값에서 `d`와 함께 뒤집혀 비트 동일 — 값 복원 게이트가 원리적으로 못 보는 클래스).

배선: `matmul_q`가 `qdot::supports(ty) && k%256==0`에서만 융합(Q3_K만). 이 라운드부터 "로짓 비트 동일" 습관이 깨진다 — 융합이 더 정확해 로짓이 움직이는 게 설계다. gate-attn 빨간불로 `max_cols` 1→2 재핀, gate-prompts 발산 집합 {5}→{14, 24} 재핀(둘 다 ik margin 0.108~0.151), gate-profile 임계 98→97 재기준 — 전부 `fused=false` 통제 실험으로 확인 후 리드가 재핀.

스레드 32 4.5274(기본 4.5369), 64 4.5715(처음으로 32 역전, 스텝 폭 35.0%로 불안정, 원인 미상). T=1 0.2550(전날보다 14% 느림, 원인 미상). 배선 후 Q3_K dequant **0.00**, wall 116.20ms 중 dot 워커합/32는 87.2ms — 차액 약 28ms 디스패치·동기·게더(도출)가 다음 표적.

원본: `git show c27c3a7:log/2026-09-19-g-the-fused-kernel-runs.md`

<a name="h-dissecting-the-join"></a>
## 합류를 분해한다 — 12ms를 되찾고, 남은 41ms에 이름을 붙였다

**23:17–23:26 UTC, ik c10fbbcc(4904).** MUL-23. **4.5369 → 4.8046 tok/s(+5.9%)**, 스텝 220.4→208.1ms, 로짓 불변(재핀 0). 같은 임대 ik 80.77±0.46 → 남은 배수 16.8배.

풀 카운터: 디스패치 1089/스텝에 워커 파킹 899/스텝, 디스패처 파킹 1 — 어텐션급 접착부 뒤 파킹했다가 futex로 깨면 가장 늦게 깬 워커가 wall이 된다. 스핀 스윕: 0에서 −9.4%(파킹 비용 상한), 기본 20000이 근처 최적, 10배·100배는 오히려 손해(노는 스핀이 대역폭을 훔침) — 답은 "더 참기"가 아니라 디스패치 수 자체를 줄이는 것.

워커가 힙 할당·뮤텍스 push·정렬·transpose 대신 `out`에 직접 쓰게 바꿨다(행 분할=셀 분할이 안전 논거). 게이트 재핀 0. 게더 열 7.4→2.1ms/스텝, 스텝 12.3ms 감소. 스윕 32 4.7993(기본 4.8046), 64 4.9789(역전 확대, 스텝 폭 32.1%). T=1 0.2552 불변 — 전날 T=1 회귀가 융합 경로의 단일 스레드 비용임이 이걸로 확정됐다. 잔여 ~41.4ms/스텝(파킹 899 불변) — MoE 전문가 루프의 세 번 왕복을 다음이 접는다.

원본: `git show c27c3a7:log/2026-09-19-h-dissecting-the-join.md`

<a name="i-folding-the-dispatches"></a>
## 디스패치를 접는다 — 4.80에서 5.48 tok/s

**23:36–23:37 UTC, ik c10fbbcc(4904).** MUL-24, MoE 배칭. 전문가마다 세 번 오가던 풀 왕복을 층당 2회(게이트+업 한 디스패치, 다운 한 디스패치)로 접었다. `matmul_q_batch`가 같은 (k,n) 뷰 목록을 연결 행 공간으로 취급, `PairWork::compute_rows`가 단일·배칭의 행 루프를 공유.

**4.8046 → 5.4834 tok/s(+14.1%)**, 스텝 208.1→182.4ms, 로짓·생성 토큰 불변, 재핀 0(신규 배칭 증명 테스트로 실제 전문가 4페어가 순차 `matmul_q`와 비트 동일). 풀 디스패치 1089→673/스텝, 배칭 사이트 잔여 3.4ms뿐. 같은 임대 ik 81.39±1.11 → 남은 배수 14.8배(측정 시작일 대비 18.7배).

원인 미상 둘: 프리필이 5.98→4.72 tok/s로 27% 느려짐(배칭 Q3_K 게이트+업이 순차 대비 +66% — 디코드와 달리 페어별 토큰 수가 1..6으로 섞임). 64스레드 역전이 6.3860 tok/s(스프레드 52.7%)로 확대. "잔여는 횟수의 함수"는 방향만 증명 — 디스패치 38% 감소에 잔여는 41.4→34.2ms로 비례보다 덜 줄었다.

원본: `git show c27c3a7:log/2026-09-19-i-folding-the-dispatches.md`

<a name="j-the-seventh-fold"></a>
## 디스패치를 또 접는다 — 그리고 접기의 한계가 보였다

**23:49–23:53 UTC, ik c10fbbcc(4904).** MUL-25, 어텐션 배칭. `wv_b_heads`의 헤드당 128행 뷰 호출(432 디스패치/스텝)을 `matmul_q_batch`로 한 디스패치로 접었다(비트 동일). 풀 디스패치 673→**268/스텝**, 어텐션 matmul 잔여 13.9→2.9ms, 레벨1 프로파일 스텝 193.7→182.7ms(차액 11.0ms, 예측한 wv_b 잔여 몫과 일치).

**비프로파일 스텝은 안 움직였다**(186.2/186.3ms, MUL-24의 182.4ms보다 오히려 ~2% 느림) — 세 임대 ik가 81.39→81.80→83.23으로 ±2% 흔들려 미확정으로 남긴다. 확정된 것: 디스패치를 673→268로 접어도 워커 파킹은 ~930/스텝로 안 줄었다 — 파킹은 디스패치 수가 아니라 접착부(어텐션·swiglu) 길이에 묶여 있다. 다음 지렛대는 Q4_K 디퀀트 17.4ms(융합 확장), 그다음 스핀 정책 재설계. T=1 0.2556 불변(네 라운드 연속).

원본: `git show c27c3a7:log/2026-09-19-j-the-seventh-fold.md`

<a name="verification-does-not-amortise"></a>
## 검증 패스는 공짜가 될 수 없다

**16:58–17:16. DeepSeek-V4.1-Flash Q3_K_M engramQ8 445GB, 서빙 배치(expert 7층 GPU·33층 DDR4, engram NVMe), 팔 넷 각각 새 서버, 고정 프롬프트 여덟, temperature 0·`cache_prompt False`.** 투기 디코딩은 k토큰이 같은 가중치를 한 번 읽어야 이득인데, DSpark 검증 패스 비용이 k에 상수가 아니라 자랐다.

여덟 프롬프트 합산: 드래프트 없음 19.92 tok/s(50.21ms/패스), `n_max` 1 21.83(77.63ms, 수락률 0.702), 2 22.24(95.46ms, 0.570), 3 21.46(112.20ms, 0.478). 최소제곱 `ms/패스(k)=D+50.21+m·k`로 D=10.32ms(드래프트 자체, k에 평평 — DSpark가 드래프트 패스를 한 번에 내는 구조의 확인), **m=17.28ms**(잔차 ≤0.36ms). m은 전체 패스의 **34.43%**, `docs/throughput-model.md`가 실행 전 써 둔 라우팅 expert 바이트 몫이 **34.56%** — 0.13%p 차. dense는 완벽히 상각, 라우팅 expert는 전혀 상각 안 됨(인접 토큰도 겹치는 expert 거의 없음: 층당 384개 중 6개면 기대 겹침 0.09개).

> ~~검증이 공짜였다면 56.5 / 2.18 = 25.9 ms/토큰 = **38.6 tok/s**. 22.8 → 38.6은 커널이나 포맷이 아니라 검증 패스의 expert 디스패치 하나에 걸려 있다.~~ **2026-09-19 선 그음.** MoE에서 검증은 공짜가 될 수 없다. 바닥이 34.56%이고, 디스패치 결함이 아니라 라우팅이 토큰별이라는 사실이다. 초과분 없음.

m=0 가정 시 상한은 28.00/35.08/39.78 tok/s(도달 불가능). 팔 셋(n_max 1/2/3) 차이는 프롬프트별 산포(18.7~31.3%)에 묻혀 못 가른다 — 가를 수 있는 건 드래프트 유무(19.92→약22, 11%)뿐. [09-17-b](2026-09-17.md#b-where-the-four-stream-gap-actually-is)의 "배치 2가 배치 1보다 느리다" 질문은 닫히지 않았지만, 초과분이 CUDA `MUL_MAT_ID` 경로(GPU 상주 expert)에 산다는 가설로 좁혀졌다 — 호스트 `mul_mat_id`는 expert별 행 묶음이라 초과분이 없었다. [09-13](2026-09-13.md#deepseek-v41-on-ik-llama)의 드래프트 이득 29%와 이번 11%는 러너·프롬프트 셋이 달라 직접 비교 대상이 아니다.

원본: `git show c27c3a7:log/2026-09-19-verification-does-not-amortise.md`
